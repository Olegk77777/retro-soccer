// Все константы игры и баланса — только здесь (наш аналог Params.ini из Simple Soccer).
// Менять числа — можно смело; хардкодить их в логике — нельзя.

export const CONFIG = {
  field: {
    length: 105,          // метры, по оси X (стандарт ФИФА)
    width: 68,            // метры, по оси Z
    apron: 12,            // зелёная зона за разметкой
  },

  // Рекламные бортики по периметру: и визуал, и стенка отскока берут эти числа
  boards: {
    marginX: 5.5,         // отступ щитов от лицевой линии (за воротами)
    marginZ: 4.5,         // отступ от боковой линии
    height: 1.0,          // физическая высота щита
    visualHeight: 1.3,    // ТВ-преувеличение: бренды читаются в широком плане
    bounce: 0.45,         // сколько скорости мяча остаётся после отскока
  },

  // Олимпийская беговая дорожка лежит за рекламными щитами и заполняет
  // тёмный коридор до трибун. Только визуал: физика поля/мяча не меняется.
  track: {
    straightHalf: 42.2,   // половина прямого участка овала (≈84,4 м)
    innerRadius: 39.0,    // сразу за боковыми щитами z=±38,5
    lanes: 8,
    laneWidth: 1.22,      // стандартная ширина легкоатлетической дорожки (м)
    shoulder: 6.5,        // внешняя зона до нижнего ряда трибуны
    lineWidth: 0.18,      // ТВ-преувеличение: линии переживают рендер 480p
    color: '#92394b',     // под CRT становится глубоким бордовым, не чёрным
    shoulderColor: '#6f2939',
    lineColor: '#e5dcc4',
  },

  goal: {
    width: 7.32,          // ЧИСТЫЙ проём между внутренними гранями штанг (стандарт FIFA)
    height: 2.44,         // ЧИСТЫЙ проём от газона до низа перекладины
    postRadius: 0.12,     // толще реальных, чтобы читались с ТВ-дистанции
    // Граней в трубе каркаса. На 480p хватало восьми, на 1080p восьмигранник
    // виден как гранёный карандаш, а рёбра двух труб на стыке ловят блик и
    // рисуют лишний угол. Двенадцать — предел, за которым разницы уже не видно.
    frameSegments: 12,
    depth: 1.9,           // глубина сетки за линией ворот
    frameBounce: 0.72,    // алюминиевая стойка: живой, но не резиновый отскок
    frameTangent: 0.94,   // доля касательной скорости после удара о каркас
    collisionIterations: 6, // быстрый мяч не проскочит стойку между двумя кадрами
    contactEpsilon: 0.002,
    resetDelay: 1.75,     // после гола мяч и волна сетки остаются в кадре подольше
    net: {
      cellSize: 0.38,       // реальная решётка, а не нарисованная прозрачная плоскость
      ballSpring: 280,      // мягкая сила, которой полотно тормозит продавивший его мяч
      ballDamping: 24,      // сетка принимает удар и не работает как батут
      hardStopSpring: 900,  // резко крепнет после предела растяжения
      tangentDamping: 2.5,  // трение мяча вдоль полотна
      contactMaxTime: 1.2,  // страховка от вечного контакта при нестандартном спавне
      spring: 50,           // связь соседних узлов: натяжение полотна
      restore: 7.5,         // возврат сетки в исходную форму
      damping: 3.0,         // меньше = волна дольше бегает и заметнее трясётся
      impactTransfer: 0.18, // удар запускает расходящуюся волну; продавливание считается отдельно
      impactReferenceSpeed: 18, // м/с: опорная скорость для кривой силы удара
      impactExponent: 1.45, // >1: сильный удар растягивает сетку непропорционально сильнее
      impactMaxSpeed: 35,   // защита пружин от отладочных сверхскоростей
      impactRadius: 1.75,   // локальный карман захватывает несколько видимых ячеек
      followRate: 28,       // как быстро ближайшие узлы следуют за продавившим их мячом
      physicalMaxStretch: 1.25, // реальный предел хода мяча до жёсткого натяжения полотна
      visualStretch: 2.2,   // ТВ-преувеличение: карман читается с игровой камеры
      visualMaxStretch: 2.25, // визуальный предел без выворачивания сетки наружу
    },
  },

  ball: {
    radius: 0.16,         // чуть крупнее реального (0.11) для читаемости, но соразмерно игроку
    gravity: -14,         // мягче реальной в 1.4 раза: дуги характером как в PES (см. ресёрч 08)
    bounce: 0.55,         // сколько скорости остаётся после отскока (PES-моды: 0.5–0.6)
    rollFriction: 0.988,  // затухание качения за кадр (при 60 fps)
    dragK: 0.013,         // сопротивление воздуха ~|v|²: быстрый мяч тормозится сильнее.
                          // Постоянная доля скорости «за кадр» — это баг floaty-FIFA (ресёрч 08)
    stopSpeed: 0.12,      // ниже этой скорости мяч останавливается
    magnus: 0.045,        // сила эффекта Магнуса (подкрутка уводит мяч вбок)
    spinDecay: 0.995,     // затухание закрутки за кадр — tau ≈ 3.3 с (цель PES: 2–4 с)
    afterTouchTime: 0.9,  // сек после удара, пока мяч можно докручивать направлением
    afterTouchRate: 8,    // скорость набора докрутки (ед. спина/с)
    afterTouchMax: 4.5,   // предел суммарной закрутки от aftertouch

    // Маркер точки падения (просьба Олега 18.07.2026): пока мяч летит верхом,
    // маленькое ненавязчивое кольцо показывает, куда он опустится, — легче
    // врываться под навес. Точка — из predictLanding (drag + Магнус)
    landingMark: {
      minY: 1.1,          // мяч ниже — маркер не нужен (низовой мяч читается сам)
      minFlight: 0.3,     // осталось лететь меньше — гаснет (мельтешил бы)
      innerR: 0.3,        // кольцо: внутренний/внешний радиус (м)
      outerR: 0.44,
      opacity: 0.38,      // ненавязчивость
      pulse: 0.12,        // лёгкое дыхание размера
    },
  },

  player: {
    speed: 6.8,             // м/с на максимуме (быстрый футболист ~8 м/с)
    accel: 10,              // отзывчивость разгона (1/с)
    turnRate: 14,           // скорость разворота
    // Спринт (E / ⚡ / RB): быстрее, но мяч хуже контролируется — как R1 в PES
    sprintFactor: 1.35,     // множитель скорости на спринте
    sprintTurnFactor: 0.55, // развороты тяжелее
    sprintInertia: 0.9,     // сек: отпустил спринт — темп спадает ПЛАВНО, по инерции
                            // (отпустил ⚡ перед ударом — скорость ещё живёт, удар с лёта)
    // Дриблинг на спринте — ТОЛЧКАМИ (реализм, фидбек Олега 17.07.2026):
    // мяч пинается вперёд, катится, тормозит, игрок догоняет — ритм «то у ног,
    // то на отдалении», а не мяч на жёсткой палке фиксированной длины.
    sprintTouchMinSpeed: 3.5,  // медленнее — липкое ведение (мяч у ноги)
    sprintTouchPush: 1.5,      // скорость мяча после толчка = скорость игрока × это
    sprintTouchTrigger: 0.95,  // мяч подкатился ближе этого впереди — новый толчок
    sprintTouchInterval: 0.26, // мин. пауза между толчками (сек), чтобы не долбить
    sprintTouchLateral: 0.18,  // боковое удержание на линии бега (мягкое, для поворотов)
    sprintTurnPushMin: 0.3,    // доля силы толчка при развороте ≥90°: мяч
                               // «притормаживается под разворот», а не улетает
                               // на полной скорости вбок (фидбек Олега 22.07)
    dribbleChaseDist: 1.2,     // дальше этого корпус смотрит на мяч до касания;
                               // сам курс погони теперь держит player.approach ниже
    // Единое «обязательство завершить касание»: после спринтерского толчка
    // и после переключения на свободный мяч ввод хранит будущий поворот,
    // а ноги до контакта продолжают погоню. Мяч остаётся отдельным телом.
    approach: {
      contactRadius: 0.95,     // м: настоящее касание, после которого руль отпускается
      departRadius: 1.15,      // после толчка мяч должен сначала выйти из зоны контакта
      settleTime: 0.10,        // сек устойчивого контроля: отделился ли мяч вообще
      settleSpeed: 0.35,       // м/с: ниже этой полной относительной скорости мяч у бутсы
      switchTimeout: 2.2,      // сек на попытку подбора после смены игрока
      dribbleTimeout: 2.2,     // сек на следующее касание после сильного толчка
      maxSwitchDist: 16,       // дальше автодобегание не включаем — слишком много автопилота
      intentDeadZone: 0.25,    // слабый шум стика не считается намерением повернуть
      autoSprintDist: 4.0,     // до далёкого свободного мяча выбранный сам ускоряется
      accel: 18,               // погоня быстро гасит старый боковой импульс, но не телепортирует
      missArmDist: 1.6,        // подойдя настолько близко, начинаем различать «промахнулся»
      missMargin: 0.5,         // дистанция снова выросла на столько — попытка проиграна
      maxBallY: 0.7,           // верховой мяч не тянет игрока к своей тени на газоне
      strikePursuitRange: 7,   // м: во время замаха/окна тапов ноги ещё догонят свой мяч
      strikeHoldRadius: 0.4,   // вплотную сохраняем прежний разбег, не дёргаемся вокруг мяча
      episodeGrace: 1.4,       // сек: столько после последнего касания игрок ещё «хозяин
                               // эпизода» — замах навеса/удара держит разбег к оттолкнутому
                               // на спринте мячу, а не даёт стику-прицелу увести вбок
    },
    stickyRadius: 1.25,        // липкое ведение хватает мяч только с этого расстояния:
                               // издалека мяч НЕ примагничивается — игрок добегает сам
                               // (честно для дриблинга и будущего отбора)
    radius: 0.42,           // толщина капсулы-заглушки
    height: 1.85,
    modelScale: 1.15,       // масштаб glb-модели (рост 1.8 м × 1.15 ≈ 2.07) — крупнее для читаемости с ТВ-камеры
    baseHeightCm: 180,      // рост «средней» модели: от него считается рост из JSON состава

    // === Анимация: лестница локомоции, разброс между игроками, живой корпус ===
    // Предыстория. В паке Mixamo ровно ОДИН клип бега вперёд (трусца 0.533 с).
    // Раньше он растягивался на все скорости одним timeScale = speed/4.0,
    // зажатым в 0.6…1.9. Замер по риггу: настоящая скорость этого клипа не 4.0,
    // а 4.57 м/с, и на медленном ходу (1 м/с) ноги крутились ВТРОЕ быстрее
    // земли — это и читалось как «полупарализованные роботы».
    // Теперь из бега производятся ходьба и спринт (src/anim.js), соседние
    // ступени смешиваются по скорости, а темп каждой считается от ЕЁ
    // собственной длины шага. Скорости клипов не хардкодятся — они меряются
    // на риге при загрузке (см. anim.js → calibrate).
    anim: {
      // Ступени лестницы. Две независимые ручки на каждую:
      //   leg/arm/spine/root — размах движения по группам костей = ДЛИНА ШАГА;
      //   stretch            — растяжка клипа по времени = ЧАСТОТА ШАГА.
      // Скорость ступени = длина шага / длительность цикла. Её здесь НЕТ: она
      // меряется на риге при загрузке (src/anim.js → calibrate), потому что
      // руками такое число всегда врёт (прежние 4.0 против настоящих 4.57).
      // Исходный цикл в glb сжат: 0.533 с при шаге 2.44 м — 225 шагов в минуту
      // на скорости трусцы. Поэтому у бега stretch > 1: тот же шаг, но темп
      // человеческий, а не заводной игрушки.
      derive: [
        // Ходьба: короткий шаг, спокойный темп, спина сама выпрямляется
        { name: 'walk', from: 'run', leg: 0.66, arm: 0.55, forearm: 0.5, spread: 0.05, spine: 0.45, root: 0.55, stretch: 1.95 },
        // Бег: шаг шире оригинала, темп спокойнее — «сильный», а не семенящий
        { name: 'jog', from: 'run', leg: 1.28, arm: 1.5, forearm: 0.95, spread: 0.26, spine: 1.1, root: 1.15, stretch: 1.32 },
        // Спринт: широкий мах, завал корпуса вперёд, частый шаг
        { name: 'sprint', from: 'run', leg: 1.55, arm: 1.85, forearm: 1.0, spread: 0.3, spine: 1.5, root: 1.3, stretch: 1.0 },
        // Спиной и боком — размах почти оригинальный, поправляем только темп
        { name: 'back', from: 'run_back', leg: 1.08, arm: 1.1, forearm: 0.9, spread: 0.2, spine: 1.0, root: 1.0, stretch: 1.3 },
        { name: 'side_l', from: 'strafe_l', leg: 1.05, arm: 1.0, forearm: 0.9, spread: 0.18, spine: 1.0, root: 1.0, stretch: 1.28 },
        { name: 'side_r', from: 'strafe_r', leg: 1.05, arm: 1.0, forearm: 0.9, spread: 0.18, spine: 1.0, root: 1.0, stretch: 1.28 },
        // Медленные ступени для тех же направлений. Без них игрок, смещающийся
        // боком на 1 м/с, был на 94% стойкой и просто ЕХАЛ по газону стоя —
        // ровно то, что заказчик назвал «полупарализованные».
        { name: 'back_slow', from: 'run_back', leg: 0.6, arm: 0.55, forearm: 0.5, spread: 0.05, spine: 0.55, root: 0.55, stretch: 1.9 },
        { name: 'side_l_slow', from: 'strafe_l', leg: 0.6, arm: 0.55, forearm: 0.5, spread: 0.05, spine: 0.55, root: 0.55, stretch: 1.9 },
        { name: 'side_r_slow', from: 'strafe_r', leg: 0.6, arm: 0.55, forearm: 0.5, spread: 0.05, spine: 0.55, root: 0.55, stretch: 1.9 },
        // Вратарь ходит по линии приставным шагом в НИЗКОЙ стойке — это своя
        // походка, а не полевой strafe. Клипы gk_step_* добавлены в модель
        // 26.07.2026; направление замерено по опорной стопе, а не по имени
        // файла (в паке они назывались наоборот).
        { name: 'gk_side_l', from: 'gk_step_l', leg: 1.0, arm: 1.0, forearm: 1.0, spine: 1.0, root: 1.0, stretch: 1.25 },
        { name: 'gk_side_r', from: 'gk_step_r', leg: 1.0, arm: 1.0, forearm: 1.0, spine: 1.0, root: 1.0, stretch: 1.25 },
        { name: 'gk_side_l_slow', from: 'gk_step_l', leg: 0.6, arm: 0.6, forearm: 1.0, spine: 0.7, root: 0.6, stretch: 1.85 },
        { name: 'gk_side_r_slow', from: 'gk_step_r', leg: 0.6, arm: 0.6, forearm: 1.0, spine: 0.7, root: 0.6, stretch: 1.85 },
      ],
      // Лестницы по направлениям. Порядок обязателен: от медленного к быстрому,
      // первый элемент 'idle' подменяется стойкой вратаря, если игрок в раме.
      ladder: ['idle', 'walk', 'jog', 'sprint'],
      ladderBack: ['idle', 'back_slow', 'back'],
      ladderSideL: ['idle', 'side_l_slow', 'side_l'],
      ladderSideR: ['idle', 'side_r_slow', 'side_r'],
      // Вратарь вдоль линии ходит своим приставным шагом (низкая стойка).
      // Вперёд/назад (выход из ворот и обратно) он бегает обычными ступенями.
      ladderSideLKeeper: ['idle', 'gk_side_l_slow', 'gk_side_l'],
      ladderSideRKeeper: ['idle', 'gk_side_r_slow', 'gk_side_r'],

      // Выбор направления с гистерезисом: без него на грани (движение под ~60°
      // к взгляду) режим дребезжал, и в позе одновременно висели шаг влево и
      // шаг вправо — ноги превращались в кашу
      sideEnter: 0.62,      // доля боковой скорости, с которой переходим на приставной шаг
      sideExit: 0.42,       // …и ниже которой возвращаемся к бегу вперёд
      sideMin: 1.1,         // м/с вбок: медленнее — знак «влево/вправо» шумит,
                            // и сторону менять нельзя (иначе оба шага сразу)
      sideKeep: 0.6,        // доля sideMin, при которой боковой режим ещё держится
      backEnter: -0.5,      // доля продольной скорости для перехода на бег спиной
      backExit: -0.28,
      dirMinSpeed: 0.9,     // медленнее — направление не различаем, идём обычным шагом

      // Опорной считается стопа не ниже фиксированной высоты, а в узкой полосе
      // над САМОЙ НИЖНЕЙ точкой этого клипа: фиксированный порог врал на
      // производных (нога висит или тонет), и в замер попадал быстрый мах
      groundBand: 0.05,     // м над нижней точкой — это ещё опора
      calibrateSamples: 96, // кадров на клип при замере длины шага
      groundSamples: 24,    // кадров на клип при приземлении (нужна только нижняя точка)
      logCalibration: true, // печатать замеренные скорости в консоль

      // Темп ног = скорость игрока / длина шага ступени. Зажим узкий: лестница
      // и так покрывает диапазон, а широкий зажим — это и есть скольжение.
      rateMin: 0.72,
      rateMax: 1.42,
      // Спринт на пределе (9.2 м/с) выше потолка клипа — небольшой недобор
      // темпа там честнее, чем ноги-вертолёт.

      // Ширина зоны смешивания соседних ступеней, доля разницы их скоростей.
      // 1.0 = плавно всю дорогу; меньше — ступени дольше играют «начисто»
      // (дешевле: два клипа сэмплятся только в переходе).
      blendBand: 0.85,
      idleTo: 1.15,         // м/с: полностью встали (ниже — только стойка)

      weightRate: 16,       // 1/с: как быстро веса догоняют цель (мягкие переходы)
      weightFloor: 0.02,    // ниже этого вес обнуляется: «хвост» уходящей
                            // ступени и сэмплится зря, и портит длину шага смеси
      oneShotRate: 16,      // 1/с: то же для одноразовых клипов (удар, подкат)

      // Разброс между игроками: без него 22 фигуры маршируют строем в ногу
      // (замер до правки: все 20 полевых стояли на кадре t = 0.663 ровно).
      rateJitter: 0.05,     // ±5% личного темпа — глазу не видно скольжения,
                            // но строй рассыпается
      // Фаза у каждого своя (0..1) — раздаётся при загрузке модели.

      // === Удары и передачи ===
      // Было: КАЖДОЕ касание — пас, прострел, навес, удар, стандарт — играло
      // один и тот же клип `kick` длиной 0.333 с. Хуже того, замер по риггу
      // показал, что `kick` машет ЛЕВОЙ ногой (правая стоит мёртво), а игра при
      // этом считает удар правой в 80% случаев. Два готовых правоногих клипа —
      // `kick_run` и `penalty` — лежали в модели и не проигрывались НИ РАЗУ.
      //
      // Кадры контакта вымерены по риггу (пик скорости носка):
      //   kick     — 0.131 с (длина 0.333), ЛЕВАЯ, компактный тычок
      //   kick_r   — 0.467 с (длина 0.533), ПРАВАЯ, с места (добавлен 26.07)
      //   kick_run — 0.262 с (длина 0.767), ПРАВАЯ, с шагом в мяч на бегу
      //   penalty  — 0.467 с (длина 0.933), ПРАВАЯ, с места, полный замах
      // `at` ставится НА 2–3 КАДРА РАНЬШЕ контакта: короткий замах читается как
      // удар, а не как судорога. `end` обрезает проводку, чтобы любое касание
      // укладывалось примерно в 0.16 с и не тормозило эпизод.
      strike: {
        // Пас в ноги: коротко и резко
        pass: [
          { clip: 'kick', foot: 'L', rate: 1.7, at: 0.05, end: 0.30 },
          { clip: 'kick_r', foot: 'R', rate: 1.5, at: 0.38, end: 0.53 },
        ],
        // Пас на ход: чуть больше проводки. У правой ноги два варианта —
        // с места и с шагом в мяч, чтобы два паса подряд не были копией
        through: [
          { clip: 'kick', foot: 'L', rate: 1.5, at: 0.04, end: 0.32 },
          { clip: 'kick_r', foot: 'R', rate: 1.3, at: 0.36, end: 0.53 },
          { clip: 'kick_run', foot: 'R', rate: 1.8, at: 0.16, end: 0.50 },
        ],
        // Навес: широкий мах под мяч
        cross: [
          { clip: 'kick', foot: 'L', rate: 1.3, at: 0.03, end: 0.33 },
          { clip: 'kick_run', foot: 'R', rate: 1.5, at: 0.14, end: 0.55 },
        ],
        // Тычок носком «в касание»: почти без замаха
        toe: [
          { clip: 'kick', foot: 'L', rate: 2.2, at: 0.09, end: 0.28 },
          { clip: 'kick_r', foot: 'R', rate: 2.1, at: 0.41, end: 0.53 },
        ],
        // Удар подъёмом на скорости
        instep: [
          { clip: 'kick', foot: 'L', rate: 1.35, at: 0.03, end: 0.33 },
          { clip: 'kick_run', foot: 'R', rate: 1.5, at: 0.13, end: 0.58 },
        ],
        // Щёчка с места: прицельный, читаемый замах
        side: [
          { clip: 'kick', foot: 'L', rate: 1.25, at: 0.03, end: 0.33 },
          { clip: 'kick_r', foot: 'R', rate: 1.15, at: 0.34, end: 0.53 },
          { clip: 'penalty', foot: 'R', rate: 1.6, at: 0.33, end: 0.70 },
        ],
        // Стандарты (аут-удар, от ворот, штрафной): игрок стоит, время есть —
        // тут полный замах penalty к месту, шаг в мяч выглядит правильно
        setpiece: [
          { clip: 'penalty', rate: 1.35, at: 0.28, end: 0.75 },
          { clip: 'kick', foot: 'L', rate: 1.2, at: 0.02, end: 0.33 },
        ],
      },

      // Живой корпус: доворот на мяч и завал в поворот. Дёшево (пара
      // кватернионов на игрока), а «деревянность» снимает сильнее всего.
      look: {
        enabled: true,
        maxYaw: 0.85,       // рад: предел доворота головы в сторону (~49°)
        maxPitch: 0.42,     // рад: вверх/вниз (следим за верховым мячом)
        headShare: 0.62,    // какую долю доворота берёт голова…
        chestShare: 0.3,    // …и какую грудь (остальное — не доворачиваем)
        rate: 7,            // 1/с сглаживания — голова не дёргается
        maxDist: 45,        // м: дальше мяча не высматриваем
      },
      lean: {
        enabled: true,
        turn: 0.9,          // рад·с/рад: завал в поворот от угловой скорости
        turnMax: 0.3,       // рад: потолок завала (~17°)
        accel: 0.045,       // рад на 1 м/с² продольного ускорения
        accelMax: 0.26,     // рад: потолок «упал вперёд на разгоне»
        speedRef: 4.5,      // м/с: ниже этого завал слабеет (стоя не качаемся)
        rate: 9,            // 1/с сглаживания
      },
    },
    dribbleSpeedFactor: 0.85, // с мячом бежим медленнее
    controlRadius: 1.35,    // ПОДОБРАТЬ мяч — только с этого расстояния
    dribbleReclaim: 5.5,    // м: мяч, оттолкнутый на спринте своим ведущим, числится
                            // за ним (не «свободен») пока он ближайший к мячу в этом
                            // радиусе — курсор не убегает к партнёру (фидбек Олега 22.07)
    controlKeepRadius: 2.4, // но раз подобрал — «поводок» дриблинга тянется дотуда:
                            // на спринте (мяч в 1.7 м) контроль не рвётся, мяч
                            // доворачивает за дугой игрока (гистерезис, как в PES)
    kickRadius: 2.3,        // зона удара: пас/удар/навес проходят и по отпущенному мячу
    kickMaxBallY: 0.6,      // мяч не выше этого — бить можно (низкий отскок тоже)

    // Выбор бьющей ноги (решение Олега, 17.07.2026): мяч слева от корпуса —
    // бьёт левая, справа — правая. Корпусом рулит игрок — ногу выбирает игра.
    dominantFoot: 'R',      // мяч по центру — бьёт доминантная (потом из JSON: левши!)
    footDeadZone: 0.15,     // м вбок от оси корпуса — «по центру»
    // Внешняя сторона стопы («шведка», стиль Роберто Карлоса): когда нужная
    // крутка «наружу» от бьющей ноги. Хлыст мощнее, но капризнее
    trivela: {
      power: 1.06,          // чуть мощнее удар
      curl: 0.85,           // крутка похуже контролируется
      noise: 1.3,           // и точность шумнее
    },
    strikeBufferTime: 0.45, // «удар с хода»: нажал чуть раньше — событие ждёт, пока
                            // мяч войдёт в зону ноги (фундамент удара с паса на ход)

    // Приём верхового мяча корпусом (фидбек Олега 22.07.2026): опускающийся
    // свой пас/перевод игрок гасит грудью/бедром себе В НОГИ — как обычный
    // приём паса, а не даёт мячу отскочить от «деревянного» корпуса.
    // Если заказан удар (D) или игрок в финишной зоне у чужих ворот —
    // авто-приём молчит: там мяч замыкают, а не принимают.
    // Приём МАКСИМАЛЬНО простой и привязанный к месту (фидбек Олега 23.07):
    // никаких клипов и прыжков — игрок остаётся в обычной локомоции, мяч
    // гасится и опускается ему под ноги. Работает на всей высоте досягаемости
    // (до aerial.maxY): адресат перекидки больше не «упрыгивает» от мяча
    // скидкой в прыжке — он его ПРИНИМАЕТ.
    trap: {
      minSpeed: 4,       // медленнее — мяч и так примется обычным контролем
      maxOut: 3,         // м/с: ЖЁСТКИЙ кап скорости мяча после приёма. Ниже
                         // minSpeed и радиуса удара — приём НЕ срабатывает
                         // повторно, анимация не дёргается (фидбек Олега 22.07)
      dropSpeed: 3.2,    // м/с вниз: мяч сходит с корпуса ПОД НОГИ, а не висит
                         // на уровне груди (иначе приём триггерился снова)
      settle: 0.28,      // сек «мяч опускается с груди» — нога ждёт (и держит
                         // паузу от повторного приёма)

      // Приём — это КАСАНИЕ ТЕЛОМ (фидбек Олега 24.07.2026: «мяч отскакивает от
      // игрока как от дерева»). Раньше приём срабатывал, едва мяч опускался
      // ниже потолка зоны: замер показал контакт на высоте 2.33 м — ВЫШЕ ГОЛОВЫ
      // и в 1.1–1.4 м от груди, причём скорость мяча всегда обнулялась до 3.4
      // м/с, а направление могло развернуться на 157° — мяч буквально отлетал
      // назад в пасующего из пустоты. Теперь точка контакта берётся на корпусе
      // (стопа → бедро → грудь → лоб по высоте мяча), мяч ставится на неё и
      // сходит ПО СВОЕМУ ходу под ноги, без разворотов.
      bodyAhead: 0.22,      // м: рабочая линия корпуса чуть впереди центра
      bodyLowY: 0.25,       // низ рабочей зоны тела (стопа)…
      bodyTopY: 1.85,       // …и верх (лоб): выше мяч не принимают, а замыкают
      underFoot: 0.2,       // мяч чуть ниже стопы — всё ещё под контролем
      contactRadius: 0.5,   // м до точки корпуса — это касание
      snap: 0.4,            // м: на столько мяч подтягивается к точке касания
      keepIn: 1.0,          // м/с: мяч сходит с корпуса ПО СВОЕМУ ходу…
      keepRun: 0.5,         // …плюс доля бега игрока (мяч катится вместе с ним)
      dropRefY: 1.35,       // высота, на которой падение с корпуса полное:
                            // приняв мяч у самой земли, вниз его не вколачиваем
      cushion: 0.55,        // доля скорости, которую гасит подсед корпуса —
                            // короткая «мягкость» в ногах, видно как приём
      cushionTime: 0.22,    // сек этой мягкости
      cushionTilt: 0.20,    // рад отклона корпуса на приёме (~11°)
    },

    // Замыкание верхового мяча (ресёрч 11: PES 5/6 first-time finishing).
    // Мяч выше kickMaxBallY, но ниже maxY — играется В ОДНО КАСАНИЕ: удар
    // с лёта (volley) или головой (header), обычный контроль невозможен.
    // Главный принцип PES: мощь даёт ВРЫВАНИЕ — скорость бега в момент
    // контакта конвертируется в силу и точность; статичный прыжок — слабый
    // кивок с шумным прицелом. Стоять и ждать в штрафной невыгодно.
    aerial: {
      maxY: 2.35,       // потолок замыкания (с прыжком); выше — мяч не достать
      headerY: 1.6,     // мяч выше этого — головой, ниже — с лёта ногой. Порог
                        // ровно посередине между потолком бутсы (0.52 + подскок)
                        // и высотой лба (1.83): по обе стороны удар физически
                        // достаёт мяч. На прежних 1.15 мяч на груди назначался
                        // «головой», и лоб проходил на полметра выше мяча
      reach: 1.5,       // горизонтальный радиус замыкания (шире зоны ноги)
      buffer: 0.9,      // буфер удара при летящем мяче: жми D заранее,
                        // игрок сам добежит и замкнёт в момент прилёта
      contactY: 1.0,    // рабочая высота контакта для прогноза точки перехвата
      // Замыкание — РЕЗКОЕ и быстрое (фидбек Олега 23.07): удар головой/с лёта
      // «выстреливает», а не медленно парит. Прыжок несимметричный: короткий
      // толчок вверх (jumpRise) и падение (jumpFall), а ВЕРХНЯЯ ТОЧКА ставится
      // ровно на миг контакта — голова встречает мяч на пике, а не «после».
      jumpRise: 0.20,   // сек толчка до верхней точки (подстраивается под контакт)
      jumpRiseMax: 0.30, // дольше не тянем: замах на месте выглядит вязко
      jumpFall: 0.26,   // сек приземления после удара
      jumpHeight: 0.52, // чуть выше — компенсирует короткое время (энергичный кивок)
      jumpChargeH: 0.35, // доля высоты прыжка от силы нажатия (тап ниже, полный — выше)

      // Синхрон анимации с реальным ударом (фидбек Олега 24.07.2026:
      // «чтобы по таймингу и по позициям идеально совпадали»). Кадры контакта
      // ИЗМЕРЕНЫ по риггу models/player.glb — момент, когда бутса/голова
      // проходят сквозь мяч. Темп клипа подбирается под прогноз прилёта мяча,
      // так что кадр удара приходится ровно на касание — а не «когда повезёт».
      sync: {
        hitKick: 0.175,   // сек клипа `kick` (длина 0.333): бутса на мяче
        hitHeader: 0.62,  // сек клипа `header` (длина 1.2): голова идёт сквозь мяч
        endKick: 0.30,    // докуда доигрываем проводку, потом сразу бег
        endHeader: 0.86,  // (обрезка держит темп: ноги не мёрзнут в клипе)
        rateMin: 0.9,     // пределы темпа клипа: медленнее — «вязко»…
        rateMax: 2.9,     // …быстрее — смазанный кадр вместо удара
        leadMin: 0.05,    // сек: короче окна замаха не бывает — бьём сразу
        servo: 9,         // как резво прогноз контакта подтягивает темп клипа
        followRate: 1.7,  // темп ПРОВОДКИ после удара: не зависит от того, как
                          // сильно тянулся замах — нога всегда допрямляется резко
        // Доводка мяча на бутсу/лоб в кадре удара. Сдвиг ВДОЛЬ полёта мяча
        // глазу не виден (мяч и так летит по этой линии) — на него бюджет
        // щедрый; поперёк и по высоте — скупой, иначе будет заметен телепорт.
        snapAlong: 1.1,   // м вдоль траектории мяча
        snap: 0.45,       // м поперёк траектории
        // Где именно у игрока «точка удара» — по риггу player.glb в кадре удара.
        // Бутса вынесена почти на метр ВПЕРЁД, голова стоит над корпусом. Отсюда
        // же считается, куда игроку встать: под волей — на метр ЗА точку прилёта,
        // чтобы мяч пришёл на бутсу, а не в живот (фидбек Олега «по позициям»).
        bootAhead: 1.0,   // м перед центром: носок бьющей ноги
        headAhead: 0.12,  // м перед центром: лоб
        hitRadius: 0.5,   // м: столько до точки удара — это уже контакт
        missRadius: 1.1,  // м: дальше этого мяч бутсы/лба не коснулся — ПРОМАХ.
                          // Замах доигрывает вхолостую, мяч летит дальше своим
                          // курсом (честнее «выстрела из воздуха» в метре от ноги)
        // Подскок под ВЫСОКИЙ волей. В клипе `kick` бутса поднимается лишь на
        // ~0.52 м (замерено по риггу), а зона волея тянется до headerY=1.15 —
        // мяч на груди бить было нечем. Игрок подпрыгивает ровно настолько,
        // чтобы нога дошла до мяча: и позиция сходится, и удар читается живее.
        bootHitY: 0.52,   // м: высота бутсы в кадре удара (rig × modelScale)
        headHitY: 1.83,   // м: высота лба в кадре удара (rig × modelScale)
        volleyHopMax: 0.5, // потолок подскока под волей
        volleyHopSlack: 0.12, // мелкое расхождение добирает доводка мяча, не прыжок
        reachTol: 0.18,   // допуск по высоте: выше этого удар физически не достаёт,
                          // и игрок ЖДЁТ, пока мяч опустится в рабочую зону, —
                          // как в PES дают мячу упасть под удобный волей
        lateWait: 0.12,   // сек добавочного ожидания, если мяч отстал от клипа
      },

      // Удар с лёта/головой В ОДНО КАСАНИЕ по-PES-6 (фидбек Олега 23.07.2026):
      // мяч НЕ замирает у игрока — замах начинается, пока мяч подлетает, и в
      // момент реального контакта приходящий мяч перенаправляется в ворота
      // (сохраняя импульс). Раньше мяч замораживался на время замаха и «зависал».
      prepareRadius: 3.0,   // м: с этой дистанции игрок начинает замах под замыкание.
                            // Шире прежних 1.8: на прострел 18 м/с те 1.8 м давали
                            // 0.1 с — замах не успевал прочитаться, удар «телепортился»
      maxWait: 0.6,         // сек: страховка — не ждём контакт дольше (мяч прошёл мимо)
      interceptT: 1.6,      // сек горизонта прогноза для ВРЫВАНИЯ: пока держим удар,
                            // ноги бегут в ту же точку встречи, которую потом возьмёт
                            // замах, — иначе погоня и удар целились в разные места
      oneTouchMomentum: 0.28, // доля скорости ПРИХОДЯЩЕГО мяча в силу удара — «выстрел»
                              // с лёта живой: чем сильнее прострел, тем мощнее замыкание
      runPower: 0.05,   // +5% силы за каждый м/с скорости бега В СТОРОНУ удара
      runPowerCap: 0.4, // предел бонуса (спринт ~8 м/с → +40%)
      standSpeed: 3.0,  // медленнее — считаем «стоя»
      standNoise: 1.9,  // статичное замыкание — шумный прицел (слабое оружие)
      runNoise: 0.75,   // на скорости корпус вложен в удар — точнее обычного
      downLift: -6,     // головой можно бить В ПОЛ (кивок в газон — классика)

      // Удар в падении (просьба Олега 18.07.2026): не успеваешь к мячу на
      // ноги — бросок корпусом: рывок ~2 м + вытянутый корпус. Реалистичные
      // зоны: дотянуться можно на reach (обычный 1.5) + бросок до dive.reach,
      // никаких «полётов на 10 метров». Цена риска: слабее, шумнее, и после
      // удара игрок лежит recover секунд (промахнулся — тоже лежит)
      dive: {
        reach: 2.7,       // м до мяча, с которых запускается бросок
        stretch: 0.55,    // вытянутый корпус удлиняет зону контакта (м)
        minY: 0.35,       // мяч почти у газона — «ласточка» ногой
        maxY: 1.7,        // выше в падении не дотянуться (корпус горизонтален)
        headerY: 1.15,    // в падении корпус горизонтален, и голова достаёт ниже,
                          // чем в стойке: порог свой, не общий aerial.headerY
        time: 0.38,       // сек полёта корпуса
        lunge: 7.5,       // м/с скорость броска
        tiltMax: 1.15,    // рад наклона фигуры (ласточка)
        powerFactor: 0.85,// бьёшь без опоры — слабее
        noise: 1.5,       // и заметно шумнее
        recover: 1.0,     // сек на газоне + подъём (getup)
      },
    },

    // Борьба корпусом (просьба Олега 18.07.2026, ресёрч 12): кнопка ПАСА,
    // когда мяч НЕ у нас, — навал плечом. Сбоку/спереди у владельца —
    // сбивает ему касание (мяч отскакивает — окно отбора); под верховым
    // мячом — оттеснение соперника от точки падения. Толчок В СПИНУ —
    // нечестный: сам спотыкаешься (дух фолов PES без свистка — пока)
    challenge: {
      range: 1.7,        // м до соперника: дальше — навал не достаёт
      backCos: 0.55,     // косинус «в спину»: толкаю по направлению его взгляда
      pushTarget: 3.6,   // импульс оттеснения сопернику (м/с)
      pushSelf: 1.6,     // сам вкладываюсь в навал — движение в него
      looseBall: 4.5,    // мяч отскакивает от сбитого владельца (м/с)
      targetLock: 0.55,  // сбитый не играет мячом (кулдаун) — окно отбора
      cooldown: 0.8,     // между навалами
      stumble: 0.55,     // сек спотыкания за навал в спину
    },
    // Подкат (ресёрч 09/12/13: кнопка ○ в PES 5/6 — high risk / high reward).
    // У нас — кнопка НАВЕСА, когда мяч не у нашей команды. Скольжение с
    // вытянутой ногой: чистый контакт ВЫБИВАЕТ мяч в 50/50 (владение не
    // телепортируется — принцип PES), подкат сзади — грубый снос (жертва
    // падает; свисток и карточки придут с фолами Фазы 5, пока цена — сам
    // лежишь дольше всех). Промазал — лежишь и выключен из эпизода.
    tackle: {
      time: 0.45,        // сек скольжения (короче — не «уезжает» далеко, фидбек Олега)
      speedMin: 5.2,     // м/с слайда с места (дальность ~1.8 м)
      speedMax: 7.5,     // потолок: подкат с разгона летит чуть дальше (~2.3 м)
      runBoost: 1.05,    // скорость слайда = скорость бега × это (инерция)
      speedEnd: 2.5,     // к концу скольжение затухает до этого
      aimLeadMax: 0.55,  // потолок упреждения прицела: слайд целит в точку,
                         // где МЯЧ будет к приходу ноги (перехват курса)
      reachOwner: 4.0,   // м: подкат человека доступен, только если соперник-
                         // владелец ближе этого. Дальше — добегаем, не падаем
                         // в подкат (фидбек Олега 21.07: «противник рядом»)
      reachClosing: 1.6, // …но дистанцию мерим С УЧЁТОМ СБЛИЖЕНИЯ: соперник
                         // бежит НА меня — за время слайда он сам приедет.
                         // Без этого самый естественный подкат (шаг навстречу
                         // прущему форварду) движок отказывался исполнять
                         // (замер 24.07: «tryTackle отказал» с 4.5 м на встречных)
      victimAimDist: 4.5,// м: владелец-соперник ближе — целимся В НЕГО, а не
                         // в мяч (иначе упреждение обходило тело, и грубый
                         // подкат сзади был невозможен — фидбек Олега)
      // Клип Mixamo `soccer tackle (2)` (1.8 с): вход в подкат ногой вперёд
      // (0.28–0.55) → скольжение/лёжа (0.6–1.0) → вставание (1.0–1.8).
      // Стартуем сразу с фазы скольжения, без ручного наклона (тот превращал
      // подкат в «падение на спину», фидбек Олега 21.07)
      // Замерено по риггу player.glb: носок вынесен вперёд и лежит НИЗКО
      // (таз на газоне) с 0.45 до 0.90 с клипа — это и есть подошва, метущая
      // мяч. Стартуем клип ровно на входе в эту фазу, чтобы слайд длиной
      // tackle.time целиком прошёл по газону, а не «выпадом стоя».
      clipStart: 0.42,   // сек клипа: таз касается газона, нога вытянута
      clipRate: 1.0,     // темп клипа: слайд + вставание умещаются в клип
      sweepFrom: 0.45,   // сек клипа: подошва пошла мести…
      sweepTo: 0.92,     // …и ушла с газона (после — подъём)
      activeFrom: 0.1,   // активное окно ног: контакт только в средней фазе
      activeTo: 0.9,     // слайда — но решает всё равно ПОЛОЖЕНИЕ НОГИ (ниже)
      legAhead: 1.05,    // м: вынос носка вперёд от таза в фазе подметания
      legReach: 0.62,    // м от НОСКА до мяча — это контакт. Раньше мерили от
                         // центра игрока (1.35 м), и нога оказывалась в 0.56 м
                         // от мяча: подкат «выбивал» мяч, не касаясь его
      ballReach: 1.35,   // м от центра — грубый предфильтр перед проверкой ноги
      ballMaxY: 0.75,    // выше — мяч ногой в подкате не сыграть
      bodyReach: 1.0,    // м до соперника: корпусы сталкиваются (ноги вытянуты)
      backCos: 0.5,      // косинус «в спину»: подкат по направлению взгляда жертвы
      sideClear: 0.55,   // м: мяч настолько сбоку от жертвы — чисто и сзади-сбоку
      knockBase: 6.5,    // м/с мяча после выбивания (GFootball: ~6)…
      knockRun: 0.45,    // …плюс доля скорости слайда в момент контакта
      knockSpread: 40,   // ° случайного разброса отскока — подбор 50/50
      knockLift: 1.2,    // лёгкий подскок выбитого мяча
      victimTrip: 0.55,  // сек: сбитый при чистом отборе спотыкается (trip)
      victimDown: 1.15,  // сек: снесённый грубым подкатом лежит (fallen)
      victimPush: 2.8,   // м/с: импульс жертве по направлению слайда
      recoverHit: 0.55,  // сек на газоне после чистого отбора (вскочил)
      recoverMiss: 1.1,  // после промаха — выключен из эпизода (RoboCup: 1 c)
      recoverFoul: 1.5,  // после грубого — лежишь дольше всех
      cooldown: 1.1,     // сек между подкатами (спам бесполезен)
    },
    dribbleAhead: 0.9,      // мяч держится в этой точке перед игроком (м)
    dribbleStrength: 8,     // насколько резво мяч подтягивается к ноге
    kickCooldown: 0.4,      // после удара мяч не липнет к ноге
    chargeTime: 0.42,       // замах НАВЕСА (полоска выбирает адрес — ей нужно время)
    passChargeTime: 0.26,   // шкала ПАСА — очень быстрая (фидбек Олега 18.07.2026):
                            // пас в игре не «зажимают», тап = уже резкая передача,
                            // короткое удержание = максимум; передержал — улетел
    chargeOverCap: 1.3,     // передержка: шкала копит до 130% — сила выше задуманной,
                            // пас улетает за поле (наказание за плохой тайминг, как в PES)
    // Пас мощнее (фидбек Олега + ресёрч 15): диапазон подобран так, чтобы
    // полный замах доносил мяч через полполя ЖИВЫМ, а не умирающим у ног
    pass:    { powerMin: 13, powerMax: 32, lift: 0.8 },  // S
    through: { powerMin: 17, powerMax: 35, lift: 0.3 },  // W: настильный
  },

  // Навесы по-PES-овски (ресёрч 08 + разбор PES 5/6, 17.07.2026).
  // Главное правило PES: из флангового коридора навес НАВОДИТСЯ В ШТРАФНУЮ САМ,
  // направление бега не важно — можно нестись по бровке и резать дугу не
  // разворачиваясь. Полоска силы выбирает АДРЕС (ближняя → центр → дальняя),
  // передержка утаскивает мяч за дальнюю штангу. Тип дуги — числом нажатий A.
  // Скорость мяча подбирается баллистикой под адрес (в PES полоска — это зона,
  // а не метры в секунду). Нога решает форму дуги: правая с правого фланга —
  // аутсвингер (от вратаря к набегающим), с левого — инсвингер (к воротам).
  cross: {
    tapWindow: 0.3,       // окно «замаха ноги» после отпускания A для доп. тапов
    zone: {
      wideZ: 0.38,        // доля полуширины поля: |z| дальше — фланговый коридор
      depthX: 0.06,       // доля полудлины: глубже этого в чужой половине навод работает
    },
    aim: {
      nearZ: 5.0,         // «ближняя штанга»: метров от центра ворот в сторону подающего
      farZ: 5.5,          // «дальняя»: метров за центр в противоположную сторону
      overZ: 12,          // передержка: адрес утаскивается дальше за дальнюю штангу
      depth: 10,          // адрес по глубине: метров перед линией ворот (около 11-метровой).
                          // Бегущий к бровке держит стрелку вперёд — она прижмёт глубже
      depthMin: 2.5,      // ближе к линии ворот адрес не прижимаем (вратарь съест)
      depthMax: 16,       // и не оттягиваем дальше линии штрафной
      aimDepth: 4.5,      // стрелки вдоль поля двигают глубину (к вратарской / оттянуть)
      aimSide: 3.0,       // стрелки поперёк поля сдвигают адрес между штангами
      minDist: 9,         // сам стоишь почти на адресе — навод глушится, бей по взгляду
    },
    dragFudge: 1.15,      // надбавка к баллистической скорости: воздух съедает дальность
                          // (подобрано симуляцией 17.07.2026: при 1.07 недолёт ~14%)
    curlComp: 0.33,       // упреждение прицела под снос Магнуса. Теория хорды даёт 0.5,
                          // но закрутка затухает в полёте (spinDecay) — фактический снос
                          // меньше; 0.33 подобран симуляцией 17.07.2026
    overPower: 0.6,       // передержка добавляет скорости сверх баллистики —
                          // мяч перелетает ВСЕХ и уходит за дальнюю бровку (наказание PES)
    high: { powerMin: 13, powerMax: 25, angle: 30, curl: 3.5 }, // A×1: свеча
    mid:  { powerMin: 17, powerMax: 33, angle: 14, curl: 2.6 }, // A×2: настильный, под голову
    low:  { powerMin: 21, powerMax: 34, angle: 5,  curl: 1.4 }, // A×3: низовой прострел

    // Навес ВНЕ флангового коридора (фидбек Олега 22.07.2026): не слепой
    // заброс, а АДРЕСНЫЙ верховой мяч. Полоска выбирает дальность адресата:
    // короткий замах — мягкий заброс на ближнего в конусе (примет грудью или
    // ногой), полный — дальний перевод на противоположный фланг. Адресат
    // назначается приёмщиком и встречает мяч, как обычный пас.
    longPass: {
      minDist: 6,        // ближе — это обычный пас, не заброс (было 10:
                         // короткую перекидку через соперника было не отдать)
      maxDist: 55,
      coneCos: 0.4,      // конус поиска адресата (±66°) вокруг направления
      coneWiden: 0.6,    // слайдер «Помощь в пасах» расширяет конус на level×это
      wantNear: 9,       // м: дальность адресата при коротком замахе…
      wantFar: 46,       // …и при полном (перевод через полполя)
      lead: 0.7,         // упреждение на бег адресата (доля времени полёта)
      curlK: 0.5,        // доля природной крутки ноги в забросе
      // Короткая перекидка (фидбек Олега 23.07: «перекинуть соперника и
      // отдать ближнему верхом»): на малой дистанции дуга КРУЧЕ — мяч
      // перелетает голову защитника и падает рядом, а не улетает на 12+ м.
      // Угол тает от chipAngle (короткие) к углу типа навеса (длинные).
      chipAngle: 50,     // град дуги на самой короткой перекидке
      chipDist: 9,       // м: дистанция, где дуга максимально крутая…
      chipFar: 24,       // …и где возвращается к обычному углу типа
      powerFloor: 6.5,   // мин. сила заброса (powerMin типа душил короткие:
                         // 30° при мин. силе 13 всё равно летел ~12 м)
    },
  },

  // Удар (D). БЕЗ магнита в створ: базовый прицел — куда смотрит игрок
  // (пересечение взгляда с линией ворот), стрелки сдвигают его, промах возможен.
  // Замах управляет высотой; шкала быстрая — передержал и мяч ушёл выше.
  shot: {
    chargeTime: 0.55,       // сек до полного замаха — быстро, по-PES-овски
    powerMin: 15,
    powerMax: 30,
    assistAngle: 55,        // град: в этом конусе к воротам работает прицельная баллистика
    assistDist: 45,         // м: с большей дистанции — просто сильный удар
    aimRange: 3.2,          // м: насколько стрелки сдвигают прицел от точки взгляда
    aimSlack: 6,            // м: прицел может уходить за штангу — мимо бить МОЖНО
    heightMin: 0.25,        // высота цели без замаха — стелется низом
    heightMax: 2.25,        // при замахе ~90% — впритирку под перекладину
    overchargeFrom: 0.9,    // после этой доли замаха появляется риск
    overchargeRise: 1.4,    // насколько (случайно) мяч может уйти выше цели
    freeLiftMin: 0.6,       // подъём обычного удара без замаха
    freeLiftMax: 7.5,       // и с полным
    maxLift: 14,            // предел вертикальной скорости
    noiseZ: 0.35,           // разброс прицела по горизонтали (м) — рычаг баланса
    noiseY: 0.25,           // и по вертикали
    swipeCurl: 4,           // максимум подкрутки от изгиба свайпа (тач)

    // Помощь в ударах (слайдер в НАСТРОЙКАХ, 10–30%; решение Олега, 18.07.2026).
    // Принцип PES basic shooting / FIFA assisted: прицел ВНУТРИ створа не
    // трогаем вовсе (управляемость прежняя) — помощь работает только на грани:
    // прощает небольшой промах (стягивает к штанге/под перекладину), глушит
    // шум исполнения и усиливает докрутку направлением в полёте.
    assist: {
      level: 0.2,         // живое значение слайдера (0.10–0.30), хранится в localStorage
      pullMeters: 3.0,    // прощаемый промах = level × это (30% → до 0.9 м мимо → в створ)
      tuck: 0.35,         // спасённый удар уходит от штанги вглубь створа на долю
                          // недоиспользованного прощения — без него все спасённые
                          // мячи липли в одну точку впритирку к штанге (робото-повтор)
      noiseCut: 1.0,      // шум прицела глушится на level × это
      touchRate: 2.0,     // докрутка направлением быстрее на level × это
      touchMax: 1.0,      // и потолок суммарной докрутки выше на level × это
    },

    runKeepSpeed: 4.0,      // быстрее этого — замах НЕ тормозит бег (удар с хода);
                            // медленнее — прицельная стойка с торможением (щечка)

    // Три типа удара (решение Олега, 17.07.2026). Тип выбирается сам:
    // короткий тап -> носок; на скорости или по приходящему мячу -> подъём;
    // с места / медленного ведения -> щечка.
    styles: {
      // Носок («в касание»): мгновенный тычок без замаха — король добиваний.
      // Сила почти не зависит от замаха, точность шумная.
      toe: {
        maxCharge: 0.32,    // замах короче этого = тычок
        effCharge: 0.55,    // «виртуальный замах» силы тычка (сила середины шкалы)
        powerFactor: 0.85,
        liftFactor: 0.45,   // низом
        heightFactor: 0.5,  // и в прицельном режиме цель по высоте — у газона
        noiseFactor: 1.6,   // непредсказуемый
        animTs: 2.1, animAt: 0.22,  // анимация: резкий короткий тык
      },
      // Подъём («с лёта», driven): на бегу или по мячу с паса на ход.
      // Биомеханика: угол вылета 5–15° (ниже щечки), скорость выше, почти без
      // вращения — на излёте drag «подламывает» траекторию, мяч ныряет.
      instep: {
        minRunSpeed: 5.0,   // м/с игрока — с этой скорости бьём подъёмом
        minBallRel: 6.0,    // м/с мяча относительно игрока — «мяч приходит» (пас на ход)
        powerFactor: 1.22,  // заметно мощнее
        liftFactor: 0.72,   // настильнее
        heightFactor: 0.75, // прицельная цель по высоте ниже — прошивает
        noiseFactor: 1.35,  // сложнее исполнить точно
        animTs: 1.35, animAt: 0.13, // полный мах на скорости
      },
      // Щечка (плассированный): с места или медленного ведения — точность.
      side: {
        powerFactor: 1.0,
        liftFactor: 1.0,
        noiseFactor: 0.75,  // самый точный
        curl: 1.25,         // щечка «вырезает» мяч внутрь бьющей ноги:
                            // правая — завиток влево, левая — вправо
        animTs: 1.2, animAt: 0.16,
      },
      // Удар головой (замыкание верхового мяча выше player.aerial.headerY).
      // Слабее ноги, но с врыванием добирает мощь (aerial.runPower)
      header: {
        powerFactor: 0.74,
        liftFactor: 0.5,
        heightFactor: 0.55, // цель по высоте ниже — кивок в угол, не свеча
        noiseFactor: 1.1,
        anim: 'header', animTs: 1.5, animAt: 0.12,
      },
      // С лёта (volley): мяч между коленом и грудью — самый мощный и самый
      // капризный удар игры; король замыкания прострелов
      volley: {
        powerFactor: 1.25,
        liftFactor: 0.6,
        heightFactor: 0.7,
        noiseFactor: 1.5,
        anim: 'kick', animTs: 1.7, animAt: 0.13,
      },
    },
  },

  // Матч: длительность, розыгрыш с центра. Игровые 90 минут сжимаются
  // в realMinutes реальных — темп аркадного матча.
  match: {
    realMinutes: 6,       // реальных минут на все 90 игровых
    gameMinutes: 90,
    kickoffDelay: 0.8,    // сек: AI-команда разыгрывает мяч с центра после паузы
    fulltimePause: 6,     // сек финального свистка перед новым матчем
    // Подпись под заставкой матча (ТВ-титр перед стартовым свистком)
    venue: 'СТАД ДЕ ФРАНС · СЕН-ДЕНИ · 12 ИЮЛЯ 1998',
    goalCardTime: 4.2,    // сек: сколько висит плашка автора гола
    hintFade: 18,         // сек игры, после которых шпаргалка управления гаснет
    crowdFrom: 0.45,      // доля половины поля, с которой зал начинает заводиться
  },

  // Стандарты (Фаза 2, 21.07.2026): ауты, угловые, удары от ворот.
  // Мяч, полностью ушедший за линию, мёртв: короткая ТВ-пауза «свистка»,
  // установка на точку, подход исполнителя, исполнение. Человек исполняет
  // своими обычными кнопками (полоска силы работает), AI — сам.
  restart: {
    outDelay: 0.7,        // сек после выхода мяча: он ещё катится за линией, потом установка
    walkTimeout: 5,       // исполнитель застрял в толкучке — исполняем с места
    snapDist: 7,          // м: исполнитель дальше — «монтажная склейка» ТВ, он сразу у точки
                          // (было 14: подход из-за кадра выглядел как «игра зависла»)
    aiDelay: 1.0,         // сек готовности AI перед исполнением (читаемая ТВ-пауза)
    keepAway: 6.5,        // м: соперники не давят точку стандарта (дух правила 9,15 м)
    flashTime: 1.4,       // сек надписи АУТ / УГЛОВОЙ / ОТ ВОРОТ на телеграфике
    lineInset: 0.35,      // мяч ставится чуть внутрь поля от линии
    goalKick: { x: 5, z: 9.16 }, // удар от ворот: угол вратарской со стороны выхода мяча
    corner: {             // AI-подача с угла (человек подаёт своей полоской навеса)
      angle: 24,          // угол дуги подачи
      powerFudge: 1.15,   // надбавка к баллистике: воздух съедает дальность
      powerMin: 16,
      powerMax: 30,
      farPostZ: 5,        // в штрафной пусто — подача на дальнюю штангу
    },
    throwIn: {            // вбрасывание руками
      releaseY: 1.85,     // бросок из-за головы
      lift: 2.2,          // лёгкая дуга: руками не простреливают
      powerMin: 8,
      powerMax: 17,       // дальний аут; передержка полоски добросит чуть дальше
      aiPowerMax: 15,     // AI бросает ближнему, а не через полполя
      aiMaxDist: 26,      // м: дальше адресата не ищем (недолёт докатится)
      // Клип `throw in` длится 1.733 с (замерено по модели): занос за голову
      // (0.6–1.0) → БРОСОК, мяч уходит из рук (~1.03 с, пик разгиба рук) →
      // follow-through (1.05–1.5) → хвост с шагами (обрезаем). Мяч вылетает не
      // по таймеру, а по КАДРУ клипа (releaseClip) — руки и мяч всегда синхронны,
      // даже если сменить темп (фидбек Олега 22.07: мяч отставал от рук — тайминг
      // был посчитан под неверную длину клипа 2.17 с, а реальный клип короче).
      clipStart: 0.6,     // сек клипа на старте: руки уже пошли на занос
      clipRate: 1.2,      // темп клипа (чуть резче — прострельный дух PES)
      releaseClip: 1.03,  // сек клипа: мяч покидает руки (пик разгиба) — момент strike
      followTime: 0.22,   // сек follow-through после выпуска, затем клип гасится
    },
  },

  // Формация 4-4-2 — «домашние регионы» по Бакленду (Simple Soccer):
  // роль = точка в долях полуполя для команды, атакующей +X.
  // x: −1 = своя лицевая, +1 = чужая; z: −1..+1 = ширина. Вторая команда
  // получает зеркало автоматически. Первая запись обязана быть вратарём.
  // defOff — метры ПЕРЕД линией защиты в обороне (ресёрч 09): защитники стоят
  // на самой линии, опорные — второй линией, форварды не бегут назад
  // (готовы к контратаке — приём GameplayFootball «mixup по ролям»)
  formation: {
    roles: [
      { id: 'GK',  x: -0.93, z: 0,     defOff: 0 },
      { id: 'LB',  x: -0.60, z: -0.58, defOff: 0 },
      { id: 'CBL', x: -0.66, z: -0.20, defOff: 0 },
      { id: 'CBR', x: -0.66, z:  0.20, defOff: 0 },
      { id: 'RB',  x: -0.60, z:  0.58, defOff: 0 },
      { id: 'LM',  x: -0.20, z: -0.64, defOff: 12 },
      { id: 'CML', x: -0.28, z: -0.18, defOff: 10 },
      { id: 'CMR', x: -0.28, z:  0.18, defOff: 10 },
      { id: 'RM',  x: -0.20, z:  0.64, defOff: 12 },
      { id: 'STL', x:  0.02, z: -0.18, defOff: 25 },
      { id: 'STR', x:  0.02, z:  0.18, defOff: 25 },
    ],
  },

  // AI (Фаза 2, каркас Simple Soccer). Слои: тренер (team.js) → голова
  // (fieldplayer.js / goalkeeper.js) → ноги (steering.js). Все рычаги
  // баланса счёта — здесь, как в Params.ini Бакленда.
  ai: {
    coachTick: 0.25,      // сек: как часто «тренер» пересматривает назначения
    attackShift: 0.30,    // регионы вперёд при владении (доля полуполя)
    ballPullX: 0.33,      // в атаке команда тянется за мячом вдоль поля
    ballPullZ: 0.22,      // и поперёк; в обороне глубину даёт defence.line*
    homeSlow: 4,          // м: радиус торможения при подходе к своей точке
    waitFaceBall: true,   // стоя дома, игрок разворачивается лицом к мячу
    decideInterval: 0.28, // сек между решениями игрока с мячом (не каждый кадр)
    speedFactor: 0.94,    // AI чуть медленнее человека — честный гандикап
    sprintDist: 7,        // м до мяча, с которых догоняющий включает спринт

    // Удары AI: шум прицела — ГЛАВНЫЙ рычаг против дешёвых голов (ресёрч 03)
    shootRange: 28,       // м до ворот, дальше — AI вообще не думает об ударе
    shootBest: 17,        // м: ближе — бьёт почти всегда (убойная зона). Было 14:
                          // трейлер у линии штрафной (18 м) выпадал из убойной зоны
    shootFalloff: 1.5,    // степень падения желания бить с дистанцией. Была 2
                          // (квадрат): с 22 м желание 0.18, с 25 м — 0.05, и весь
                          // диапазон 18–25 м, где живёт половина реальных ударов,
                          // был выключен
    shotBlockWidth: 1.0,  // м: защитник ближе к линии удара — момент испорчен
    // …но ТОЛЬКО если он реально между мной и воротами. Сдерживающий защитник
    // стоит в jockeyDist (2.1 м) РОВНО на линии удара к центру ворот — значит
    // «блок» находился почти в каждом эпизоде, желание бить резалось вчетверо,
    // и правило «в убойной зоне удар обязателен» отменялось. Опекун вплотную —
    // это ДАВЛЕНИЕ (режет точность), а не блок (запрещает удар): принцип
    // Physical Pressure из PES, ресёрч 09
    shotBlockMin: 2.5,    // м вдоль линии удара: ближе — давление, а не блок
    shotPressNoise: 1.2,  // + м разброса прицела за опекуна вплотную
    shootKillFloor: 0.6,  // в убойной зоне под блоком желание бить не ниже этого
    shotBlockedK: 0.70,   // во столько раз падает желание бить сквозь защитника
    shootMaxZ: 20,        // не бьёт с совсем острого угла
    shotNoise: 1.8,       // м разброса прицела — ГЛАВНЫЙ рычаг «голы не
                          // дешевеют». Поднят с 1.5 вместе с открытием
                          // завершения: ударов стало впятеро больше, и на
                          // прежнем шуме счёт уходил к 3:2 и выше
    shotCornerMin: 0.62,  // доля полуствора: ближе к центру AI не целится —
                          // угол выбирается ДАЛЬНИЙ от вратаря
    shotNearSide: 0.25,   // доля ударов в ближний угол (иначе кипер читает схему)
    shotHighChance: 0.28, // доля ударов верхом; остальное — низом в угол
    shotPowerBase: 15,
    shotPowerPerM: 0.45,  // сила растёт с дистанцией
    shotPowerMax: 28,

    // Пасы AI
    passPressure: 3.2,    // м до ближайшего соперника: под давлением — отдай
    passUrge: 0.35,       // вероятность паса на такте решений и без давления
    passMin: 4.5,         // короче — не пас, а толчея. Было 7: короткая скидка
                          // под прессингом (самый частый пас в футболе) была
                          // движку недоступна вовсе
    passMax: 36,
    // Проходимость паса — МОДЕЛЬ ПЕРЕХВАТА ПО ВРЕМЕНИ, а не плоский коридор.
    // Прежний порог «соперник ближе 2.6 м к линии — коридор закрыт» отсекал
    // 80% всех вариантов (замер 24.07: choosePass возвращал null в 300 случаях
    // из 373, причина — «коридор закрыт»). В компактном блоке такого чистого
    // коридора почти не бывает, и AI не пасовал вовсе: 3 паса за 15 минут,
    // 88% времени кто-то просто вёл мяч. Отсюда же не было ни приёмов, ни
    // подкатов — некому принимать и не на чем ловить ошибку владельца.
    passIntercept: {
      hardBlock: 1.0,     // м: соперник практически НА линии — коридор закрыт
      react: 0.35,        // сек: защитник не срывается с места мгновенно
      speedK: 0.55,       // доля его спринта, что реально идёт поперёк линии
      body: 0.9,          // м: корпус плюс вытянутая нога
      riskUnderPress: 1.2, // м запаса, который прощается под прессингом:
                           // деваться некуда — отдавай, пусть и рискованно
    },
    // СКОРОСТЬ ПАСА задаётся ПРИХОДЯЩЕЙ, а не начальной (ресёрч 15, раздел 1).
    // Прежняя формула v0 = d·0.55 + 5.5 давала пас на 15 м, который летел
    // 2.15 с и приходил на 2.9 м/с — медленнее, чем добежать спринтом (1.63 с).
    // Из-за этого дриблинг обыгрывал пас арифметически, и никакая модель
    // выбора адресата этого не лечила. Теперь v0 = arrive + λ·d.
    passArriveSoft: 6,    // м/с на приёме: скидка в ноги стоящему партнёру
    passArriveNormal: 9,  // обычный пас
    passArriveDriven: 13, // прострельный/настильный, под давлением
    passSpeedMin: 12,
    passSpeedMax: 30,
    longPassDist: 26,     // дальше — пас верхом
    longPassLift: 5.5,

    // Модель выбора паса (ресёрч 15, раздел 3). S = P^safety · Q · V · F
    passModel: {
      // Перехват по ВРЕМЕНИ: логистика вокруг нулевого запаса.
      // k = π/(√3·σ) при σ = 0.45 с — подгонка по 10 875 реальным пасам АПЛ
      interceptK: 4.03,
      oppReact: 0.35,     // сек реакции защитника
      oppAccel: 4.5,      // м/с² разгона поперёк линии паса
      bodyWidth: 0.9,     // м: корпус, которым он и так перекрывает линию
      onLine: 1.0,        // м: ближе — соперник ФИЗИЧЕСКИ на линии, пас закрыт
      pressBonus: 0.18,   // сек прощения под прессингом («деваться некуда»)
      maxCheck: 5,        // сколько ближайших к линии соперников считаем
      // ПОСЛЕДНИЙ МЕТР — ЗОНА АДРЕСАТА. Персональный опекун стоит goal-side,
      // то есть в 1.2 м ЗА адресатом — и модель считала его перехватчиком с
      // вероятностью 0.69: он «уже на линии», бежать ему некуда, время ноль.
      // Из-за этого проходимость паса в штрафную падала с 0.95 до 0.30, и
      // ЛЮБОЙ пас в убойную зону отвергался порогом. Физически это абсурд:
      // мяч приходит к адресату РАНЬШЕ, чем к стоящему за ним защитнику
      receiverOwn: 1.2,   // м: соперник в этой зоне у цели перехватчиком не считается

      // Безопасность в степени: ГЛАВНЫЙ рычаг «не дешевеют ли голы».
      // 1.6 — баланс; 2.2 — осторожное тики-така; 1.0 — аркада
      safety: 1.6,
      minScore: 0.55,     // ниже — пас не отдаём (ведём или выносим)
      pressScoreK: 0.60,  // под прессингом порог мягче
      // Порог в финальной трети мягче: логика «риск только под прессингом»
      // перевёрнута. Свободный игрок с поднятой головой у чужой штрафной
      // обязан рисковать ОХОТНЕЕ, а не реже — иначе мяч катается поперёк
      finalThirdK: 0.70,
      finishZone: 20,     // м от чужой лицевой: зона завершения (штрафная + трейлер)
      temperature: 0.35,  // softmax: лучший вариант выбирается ~в 65% случаев —
                          // и это не «шум ради шума»: предсказуемый пас читается

      // Качество приёма Q
      qSpeedRef: 8,       // м/с прихода, при которой приём идеален…
      qSpeedSpan: 22,     // …и шкала ухудшения до минимума
      qSpeedMin: 0.45,
      qBodyBase: 0.70,    // приём спиной к воротам хуже приёма лицом
      qPressDrop: 0.35,   // сколько съедает опекун вплотную
      qPressRange: 4.5,   // м: с этой дистанции начинает давить
      qHeightLow: 1.00,   // земля
      qHeightDrive: 0.86, // настильный верховой
      qHeightLoft: 0.78,  // навес
      qRunBonus: 1.15,    // мяч положен ПЕРЕД бегущим — он не теряет скорость
      qFeetToRunner: 0.85, // …а пас в ноги бегущему заставляет тормозить

      // Ценность продвижения V из xT
      valueK: 6,          // V = clamp(1 + valueK·ΔxT, valueMin, valueMax)
      valueMin: 0.40,     // пас назад дешевле, но НЕ запрещён. Ниже 0.55:
                          // при высоком поле AI зацикливался на безопасных
                          // поперечных передачах — «стерильное перекатывание»
      valueMax: 3.0,

      // Множители семейств F — из голов на 100 доставок в зону 17 (ASA)
      fThrough: 2.2,      // пас за линию обороны: 13.2 гола/100
      fCutback: 1.6,      // ПРОСТРЕЛ: 6.4 гола/100 — второе оружие игры
      // Доставка В ЗОНУ ЗАВЕРШЕНИЯ. Семейства ресёрча считаны по голам на 100
      // ДОСТАВОК В ЗОНУ 17 — то есть ценность даёт попадание в штрафную, а не
      // факт пересечения линии обороны. Без этого множителя пас в убойную зону
      // получал скромный fProgress 1.1 и арифметически проигрывал безобидному
      // поперечному пасу (замер: 0.90 против 0.905)
      fIntoBox: 1.7,
      fProgress: 1.1,     // прогрессивный пас: 4.5
      fNormal: 1.0,       // обычный: 4.1
      fCross: 0.6,        // навес в штрафную: 2.5 — самое переоценённое действие
      fKeeper: 0.7,       // пас назад вратарю не любим (порт из gfootball)
      // Приоритет паса на рывок. Раньше эти множители были ЗАХАРДКОЖЕНЫ в
      // team.js (1.25 и 1.15) — то есть рычаг баланса из config.js ни на что
      // не влиял, а забегание почти не кормили (правило проекта: константы
      // баланса живут только здесь)
      fRunner: 2.0,       // пас на забегание за спину / стеночку
      fOverlap: 1.6,      // пас на подключившегося фулбека
    },
    receiveGiveUp: 3.0,   // сек: приём паса не сложился — возвращаемся к роли

    // Пас-ассист ЧЕЛОВЕКА (принцип PES: полоска задаёт силу, игра лишь
    // доворачивает направление на партнёра в конусе взгляда; партнёр
    // бросается встречать мяч). Никого в конусе нет — пас летит как нарисован.
    humanPass: {
      minDist: 4,         // ближе — не пас, бей по мячу как есть
      maxDist: 48,
      lead: 0.8,          // упреждение на бегущего адресата (доля времени полёта)
      leadThrough: 1.5,   // пас на ход ведёт сильнее — мяч кладётся в зону бега
      // Помощь в пасах (слайдер в НАСТРОЙКАХ, 10–30%, как у ударов):
      // уровень расширяет конус поиска адресата и подтягивает силу паса
      // к дистанции до него (передержку прощает лишь частично)
      assist: {
        level: 0.2,       // живое значение слайдера, хранится в localStorage
        coneBase: 0.92,   // косинус конуса при нулевой помощи (узкий, ±23°)
        coneWiden: 0.6,   // конус расширяется на level × это (30% → ±43°)
        powerPull: 2.0,   // сила тянется к идеальной на level × это (30% → 60%)
        // Идеальная сила считается ПО ПРИХОДУ (ресёрч 15): v0 = arrive + λ·d.
        // Прежняя формула «дистанция × 0.83» на 15 м давала 12.5 м/с — мяч
        // приходил еле живым, и пас проигрывал дриблингу арифметически
        idealArrive: 9.5, // м/с, с которыми мяч должен ПРИЙТИ к партнёру
      },
    },

    // АТАКА (ресёрч 10-Атака: Support Spot Calculator Бакленда, забегания
    // из GameplayFootball/agent2d, свободные зоны, PES-принципы открываний)
    attack: {
      // Оценка свободной зоны: 1 = пусто, 0 = толпа (формула GameplayFootball)
      freeSpaceSafeDist: 5,   // м: соперник ближе — «давит» на точку

      // Support spots: сетка точек на половине соперника, лучшая = куда
      // открывается поддерживающий. Веса — прямо из Params.ini Бакленда
      spot: {
        cols: 7, rows: 6,     // 42 точки — оценка стоит копейки
        updateSec: 0.5,       // пересчёт лучшего спота (у Бакленда 1 с)
        passSafeScore: 2.0,   // можно безопасно получить пас
        canScoreScore: 1.0,   // из точки простреливаются ворота
        distScore: 2.0,       // близость к оптимальной дистанции паса
        optimalDist: 30,      // м (у Бакленда 200 px ≈ 30 м)
        spaceScore: 1.0,      // бонус за свободную зону вокруг точки
        switchHysteresis: 3,  // м: новый суппорт должен быть ближе на столько
      },

      // Забегания за спину (триггер из GameplayFootball, цель — за линию
      // защиты соперника; пас на ход кладётся в зону рывка)
      runs: {
        checkSec: 0.5,        // как часто тренер думает о рывке
        focusAhead: 26,       // раннер ищется у точки впереди владельца
        maxDist: 38,          // дальше — пас на рывок не долетит
        // Плотность мерится В ЦЕЛИ рывка (за линией обороны), а не за спиной
        // раннера: раньше замер стоял ровно там, ОТКУДА игрок уходит, — то есть
        // в самой гуще блока. Прежние числа давали произведение 0.15 при пороге
        // 0.5, и забегание в финальной трети не срабатывало НИКОГДА
        densityRadius: 11,    // соперники в этом радиусе глушат рывок
        densityPenalty: 0.18,
        trigger: 0.32,        // порог «условия для рывка созрели»
        durationSec: 4.0,     // длительность забега
        behindLine: 6,        // м за линию защиты соперника (дальше — в руки вратарю)
        passBonus: 3.5,       // приоритет паса на бегущего (choosePass)
        leadRun: 1.6,         // упреждение паса бегущему — в зону, не в ноги
        riskFactor: 0.7,      // пас на рывок терпит более узкий коридор
                              // (принцип Gliders2d: реальные защитники не
                              // перехватывают идеально — щель = шанс)
      },

      // AI-навес с фланга (принцип PES: вингер упёрся — подача в штрафную)
      cross: {
        // Прежние пороги физически не совпадали: домашняя точка вингера при
        // владении даёт ему 34–37 м до лицевой, а зона подачи начиналась с
        // 32 м — то есть по регионам вингер в неё не попадал НИКОГДА
        flankZ: 15,           // |z| дальше — фланговый коридор
        finalThird: 38,       // м от чужих ворот
        blockedDist: 8,       // защитник по курсу ближе — прохода нет
        // Навес — САМОЕ ПЕРЕОЦЕНЁННОЕ действие (2.5 гола на 100 против 6.4 у
        // прострела). Раньше он исполнялся ДО модели паса и потому имел
        // абсолютный приоритет. Теперь подача идёт, только если модель не
        // нашла передачи лучше этого счёта
        overPass: 1.1,
        minDist: 11,          // ближе не навешиваем (GameplayFootball: 10 м)
        angle: 22,            // угол дуги подачи
        deepX: 9,             // м до лицевой: глубже — подача низом (прострел)
        lowAngle: 10,         // угол дуги прострела с лицевой (разнообразие подач)
        farPostZ: 5,          // дефолт: дальняя штанга, если в штрафной пусто
      },

      wingerWide: 1.06,       // вингеры при владении держат ширину у бровки
                              // и НЕ стягиваются к мячу (растяжка обороны)
      wingerPush: 9,          // м: вингеры при владении стоят ГЛУБЖЕ остальных —
                              // иначе их домашняя точка не доходит до зоны подачи

      // ДВИЖЕНИЕ БЕЗ МЯЧА (ресёрч 15, раздел 5). Реальный сплит рывков:
      // 72% «в пространство» / 28% «в ноги». Бюджет жёсткий: 2 рывка в
      // пространство + 1 в ноги, потолок 3. Больше — кадр 320×240 не читается,
      // и оборона перестаёт успевать (голы дешевеют).
      offBall: {
        maxInSpace: 2,
        maxToFeet: 1,
        cooldown: 5.0,      // сек на игрока между рывками
        commit: 1.0,        // сек: начатый рывок не отменяется (анти-дрожание)

        // ПРИХОД В НОГИ (coming short) — 10.7% всех рывков и самый недостающий
        // элемент нашей игры: под прессингом владельцу было НЕКОМУ отдать,
        // и он вёл мяч до потери. Партнёр показывается накоротке сам.
        short: {
          pressDist: 3.6,   // м: соперник ближе — владельца прессингуют
          dist: 8,          // м от владельца, куда открывается партнёр
          offset: 2.5,      // м сдвига прочь от ближайшего соперника
          minGap: 5,        // м: ближе к владельцу не показываемся
          ttl: 1.8,         // сек жизни открывания
          safeOption: 0.80, // есть пас с такой проходимостью — приход не нужен
        },

        // ЗАНЯТИЕ ШТРАФНОЙ. Четыре разные точки четырём игрокам — иначе все
        // бегут в одну и подача некому. Координаты по x — от лицевой линии.
        // ТРЕЙЛЕР у линии штрафной обязателен: именно он замыкает ПРОСТРЕЛ,
        // а прострел даёт 6.4 гола на 100 против 2.5 у навеса (ресёрч 15).
        box: {
          // Триггер меряется по ГЛУБИНЕ мяча, а не радиусом от центра ворот.
          // Радиус съедала ширина: классическая позиция для подачи (17 м от
          // лицевой, 25 м от центра) давала гипотенузу 30.5 > 27 — и в момент
          // навеса штрафная была ПУСТА. Число согласовано с зоной подачи
          // (attack.cross.finalThird)
          fromGoal: 34,     // м от чужой лицевой ЛИНИИ: глубже — занимаем штрафную
          maxZ: 30,         // мяч вынесло к самой бровке — врываться не под что
          stickBonus: 5,    // м форы прежнему исполнителю точки: назначения
                            // пересчитываются каждые 0.25 с и без этого дрожат
          nearPost: { x: 5.5, z: 2.5 },
          golden:   { x: 10.0, z: 0 },   // между вратарской и точкой пенальти
          farPost:  { x: 6.0, z: -5.0 },
          trailer:  { x: 16.0, z: 0 },   // у линии штрафной — под прострел
        },
      },
    },

    // Комбинационная игра (ресёрч 14, 22.07.2026): стеночки и подключения.
    // Принцип — МАЛО одновременных рывков, каждый читаемый с трибуны
    combo: {
      oneTwo: {           // стеночка (give-and-go): пас-и-рывок вперёд
        maxPassDist: 12,  // рывок только после короткого паса (учебные 5–12 м)
        pressDist: 4.5,   // прессер ближе — пас-и-рывок оправдан (ресёрч: ~3 м)
        minX: -18,        // не из своей глубокой трети (x по ходу атаки)
        runDepth: 13,     // рывок пасующего за спину опекуну, метров
        ttl: 2.6,         // сек: окно возврата мяча на ход
        chance: 0.75,     // доля пасов под прессингом, после которых бежим
        // Ручная стеночка (Q/LB + ПАС, фидбек Олега 22.07.2026): игрок сам
        // заказал «отдал — и рванул», без проверок прессинга и дистанции
        manualDepth: 16,  // рывок глубже автоматического
        manualTtl: 3.2,   // и окно возврата чуть дольше
      },
      // ИГРА ТРЕТЬЕГО (third man run, ресёрч 14 §1.2 и 15 §8). Самая ценная
      // комбинация футбола и главный ответ на низкий блок, где забегание за
      // спину бессмысленно: А отдаёт В в ноги, а мяч получает ТРЕТИЙ, на
      // которого А даже не смотрел. Триггер рывка С — ПЕРВОЕ КАСАНИЕ В, а не
      // момент паса А→В: иначе оборона читает связку заранее
      thirdMan: {
        maxFirstPass: 14, // м: первый пас короткий, «в ноги»
        markerDist: 4.5,  // м: у В на спине опекун — иначе комбинация не нужна
        runAhead: 11,     // м рывка третьего от позиции В
        turnDeg: 32,      // ° доворота рывка к воротам от линии паса А→В
        armTtl: 1.2,      // сек ожидания касания В, дальше связка гаснет
        ttl: 1.6,         // сек жизни самого рывка
      },
      // ЛОЖНЫЙ РЫВОК (decoy, ресёрч 14 §1.7 и 15 §5.2 тип 10). Единственный
      // тип движения, который оптимизирует пространство ПАРТНЁРА, а не своё.
      // Ресёрч отдельно отмечает: ни gfootball, ни Simple Soccer, ни agent2d
      // обман не моделируют — все занимают хорошее место, никто не заставляет
      // защитника занять плохое. Обманщик НИКОГДА не запрашивает мяч
      decoy: {
        ttl: 2.0,         // сек жизни ложного рывка
        dist: 13,         // м рывка (уводим опекуна за собой)
        awayZ: 11,        // м увода вбок от канала главного раннера
      },
      overlap: {          // подключение крайнего защитника по бровке
        // …или ВНУТРЬ канала (underlap, ресёрч 14 §1.4): занята бровка —
        // забегаем в полупространство, занято полупространство — по бровке.
        // Андерлап заканчивается УДАРОМ, а не подачей, и это ответ на низкий
        // блок, где внешнее подключение упирается в ту же стену
        underlapZ: 13,    // м от оси: середина полупространства
        underlapBias: 0.08, // фора внутреннему коридору при сравнении свободы:
                          // полупространство ВСЕГДА плотнее бровки (блок стоит
                          // именно там), и по голому freeSpace андерлап не
                          // случался ни разу за десять матчей. Но заканчивается
                          // он УДАРОМ, а внешний забег — подачей, поэтому риск
                          // оправдан
        flankZ: 15,       // мяч шире этого |z| — фланговый эпизод
        minX: -8,         // подключаемся не раньше подступов к середине
        triggerDist: 22,  // фулбек не дальше этого от владельца
        ahead: 15,        // забегает на столько метров глубже владельца
        wideZ: 2.5,       // цель прижата к бровке
        durationSec: 3.5, // сек жизни подключения
        passBonus: 2.2,   // приоритет паса на подключение (choosePass)
        leadRun: 1.3,     // упреждение паса бегущему фулбеку
      },
    },

    // Замыкание в одно касание (фидбек Олега 22.07.2026): назначенный под
    // подачу игрок у чужих ворот бьёт низовой прострел ПЕРВЫМ контактом,
    // без такта решений — подачи завершаются ударом, а не отскоком
    firstTouch: {
      shotRange: 20,     // м до чужих ворот: ближе — замыкаем в касание.
                         // Было 16 — трейлер под прострел (16–18 м) не имел
                         // права бить с ходу, а он и есть главный замыкающий
      minRel: 6,         // м/с мяча относительно игрока: мяч ПРИХОДИТ, а не лежит
    },

    // Игра AI на втором этаже (ресёрч 11): верховой мяч в досягаемости —
    // играется в одно касание. Своя треть — ВЫНОС; у чужих ворот — удар
    // головой/с лёта в створ (сила от разбега — врывание бьёт сильнее);
    // середина поля — скидка вперёд (борьба после выносов, как в PES)
    aerial: {
      clearThird: 30,      // м от своих ворот: ближе — верховой мяч выносим
      clearPower: 20,
      clearLift: 6,
      headerRange: 14,     // м до чужих ворот: ближе — замыкаем в створ
      headerPower: 15,     // базовая сила кивка (статичного)
      headerPowerRun: 0.9, // + за каждый м/с разбега — врывание бьёт на треть сильнее
      headerPowerMax: 24,
      headerNoise: 2.2,    // разброс прицела AI-головы — рычаг «голы не дешевеют»
      headerTargetY: 0.6,  // целимся низом, под штангу
      flickPower: 11,      // скидка головой в борьбе в середине поля
      flickLift: 3.5,
    },

    // Открывание под пас: фолбэк, пока лучший спот не посчитан
    supportDist: 12,      // м впереди мяча, куда бежит поддерживающий

    // Ведение AI
    dribbleAvoidDist: 4,  // м: соперник по курсу ближе — обвести в сторону
    dribbleSprintFree: 6, // м чистого коридора ПО КУРСУ, с которых владелец
                          // включает спринт. Без этого он бежал 5.43 м/с
                          // против 8.63 у спринтующего защитника и не мог
                          // уйти вообще ни от кого

    // Расталкивание: игроки не слипаются в кучу (все 22, дёшево)
    separationRadius: 1.6,
    separationPush: 0.55,

    // ОБОРОНА (ресёрч 09-Оборона: RoboCup/UvA линия, pressure-cover из
    // Code the Classics, containment из MarliK, разбор из GameplayFootball).
    // Принцип PES: оборона держит позиции и сдерживает, а не бросается —
    // отбор случается на ошибке владельца (плохое касание), голы не дешевеют.
    defence: {
      // Линия защиты: высота считается ОТ МЯЧА, а не от своих ворот —
      // лечит «защитники жмутся к ленточке» (фидбек Олега 18.07.2026)
      lineMinDepth: 9,    // ближе к своим воротам линия не опускается (м)
      lineRange: 38,      // насколько линия ходит за мячом по полю
      mentality: 0.9,     // 0.6 = глухой автобус … 1.2 = высокая линия
      // Зазор «линия глубже мяча». Был 12 — и при мяче ближе 21 м линия
      // ПРИБИВАЛАСЬ к минимуму 9 м: восемь полевых стояли в полосе 9–21 м,
      // между линиями не оставалось ни метра, цель забегания за спину
      // схлопывалась внутрь вратарской, а пас вразрез (самое опасное
      // семейство, 13.2 гола на 100) становился недостижим ПО ПОСТРОЕНИЮ.
      // Реальный низкий блок держит последнюю линию на 14–18 м
      lineBallGap: 6,
      lineSpeed: 5,       // м/с «дыхания» линии (не телепорт)
      defWidth: 34,       // ширина четвёрки защитников (компактность)
      zCompact: 0.78,     // сжатие ролей по ширине в обороне
      // Сползание блока к мячу. Раньше в обороне использовалась ОБЩАЯ с атакой
      // ballPullZ 0.22: при мяче у бровки блок сдвигался всего на 5.5 м и
      // оставался почти симметричным — обе полосы закрыты одинаково плотно.
      // Реальный компактный блок сползает на 8–10 м, и именно это открывает
      // перевод на дальний фланг (то есть даёт атаке свободного адресата)
      ballShiftZ: 0.38,

      // Pressure–cover: первый прессингует (chaser), второй страхует
      presserLead: 1.5,   // прессинг целится на курс дриблинга (soccer.py)
      coverDist: 5.5,     // страхующий за спиной прессингующего (м)
      coverSide: 3.0,     // со сдвигом к центру поля (угол ~30–45°)
      // Страхующим назначался просто второй по близости к мячу — то есть
      // регулярно НАПАДАЮЩИЙ, и он уезжал за спину прессингующему. При отборе
      // впереди не оставалось никого, кому отдать: контратака гибла на месте
      coverRoleK: 0.35,   // штраф к «стоимости» страхующего за каждый метр defOff
      // Первый защитник переназначался каждый кадр по голой дистанции: два
      // равноудалённых защитника мигали ролью, а ОБЫГРАННЫЙ оставался
      // ближайшим к мячу и держал роль — страховки при обыгрыше не было вовсе
      chaseBehindPen: 4,  // м штрафа тому, кто НЕ между мячом и своими воротами
      chaseHold: 1.5,     // м форы действующему прессингующему (гистерезис)
      // Экран перед воротами на прострел. Точка страхующего считается ОТ МЯЧА
      // и вместе с ним уезжает на лицевую — зона 11 метров оставалась пустой
      // ровно в тот момент, когда туда идёт прострел
      screen: {
        depth: 26,        // м от своих ворот: мяч глубже — эпизод наш
        wideZ: 14,        // …и шире этого — значит идёт подача/прострел
        x: 11,            // м от своих ворот: точка экрана (район 11 метров)
      },
      // Трекинг забегающих. Персоналка (updateMarks) включалась только когда
      // мяч уже ближе markThird к воротам и разбирала БЛИЖНИХ К ВОРОТАМ, а
      // игрока, спринтующего из середины поля за спину, не брал никто: он
      // далеко от ворот, значит в сортировке последний
      track: {
        speed: 4.0,       // м/с сближения с нашими воротами — это рывок
        range: 34,        // м до наших ворот: дальше не разбираем
        hold: 2.0,        // м форы прежнему опекуну (гистерезис назначения)
      },

      // Jockey/containment: у владельца не выбрасываемся, а сдерживаем
      jockeyDist: 2.1,    // блок-точка между владельцем и своими воротами
      jockeyMirror: 0.85, // у блок-точки скорость капится скоростью владельца
      badTouchDist: 1.5,  // мяч отлетел от владельца дальше — рывок в отбор

      // AI-подкаты (грубые 90-е): в окно плохого касания владельца
      // прессингующий может пойти в подкат — сбоку, никогда в спину
      tackle: {
        range: 3.6,       // м до мяча: из этой зоны AI решается на подкат.
                          // Было 2.7 — уже, чем реально достаёт вытянутая нога
        rangeMin: 1.3,    // вплотную не скользим — обычный рывок в отбор
        ratePerSec: 1.2,  // интенсивность решения в окне ОШИБКИ владельца
        rateNormal: 0.35, // …и в обычном окне, когда слайд просто достаёт мяч.
                          // Без второго окна AI не подкатывался вовсе: своё же
                          // ведение держит мяч у ноги, и «плохого касания»
                          // не случается почти никогда (замер 24.07)
        desperateDepth: 30, // м от своих ворот: ближе — решаются злее…
        desperateK: 2.2,    // …во столько раз (цена потери мяча выше)
      },

      // Персональный разбор в своей трети (goal-side, гибрид MarliK)
      markThird: 35,      // м от своих ворот: ближе — переходим на разбор
      markDist: 1.9,      // опекун между подопечным и воротами
      markBallSide: 0.7,  // поправка в сторону мяча (успеть на прострел)
    },

    // ВРАТАРЬ (ресёрч 16-Вратарь, полная переработка 26.07.2026).
    // Принцип: сейв — это ГЕОМЕТРИЯ И ВРЕМЯ, а не «магнит радиусом 1.7 м».
    // Кипер читает удар с задержкой реакции, физически бросается корпусом и
    // достаёт мяч рукой — или не достаёт. Пойманный намертво мяч заслужен:
    // сильный удар отбивается, скользящий контакт летит на угловой, лёгкий
    // может быть выронен. Все рычаги «голы не дешевеют / кипер не клоун» — тут.
    keeper: {
      // --- ПОЗИЦИЯ (дуга вратаря) ---
      // Кипер стоит на линии «мяч → центр ворот», глубина растёт с дистанцией
      // до мяча: мяч в штрафной — почти на ленточке, мяч у центра поля —
      // выдвинут (свипер 90-х умеренный, не Нойер).
      depthNear: 0.9,     // м перед линией, когда мяч рядом (≤ depthRefNear)
      depthFar: 6.5,      // ...и когда мяч далеко (≥ depthRefFar). Было 9.0:
                          // такую дугу вратарь физически не удерживал — чтобы
                          // вернуться на линию за отскоком с 45 м, ему нужно
                          // 0.25 от скорости мяча, а кап возврата вдвое ниже.
                          // В момент выхода мяча на линию он оставался в 3.5 м
                          // впереди — и медленный мяч спокойно шёл над ним
      depthRefNear: 12,   // м до мяча: ближе — минимальная глубина
      depthRefFar: 40,    // м до мяча: дальше — максимальная
      maxZ: 3.16,         // м от центра ворот: дальше не смещается. Было 4.2 —
                          // ШИРЕ штанги (3.66): вратарю разрешалось встать
                          // ЗА штангой, полностью распахнув дальний угол
      stepSpeed: 4.6,     // м/с приставного хода по дуге (кипер не спринтер)
      recoverGap: 1.2,    // м: дальше этого от своей точки кипер возвращается
                          // полным ходом, а не «в стойке» — иначе он не
                          // успевает на линию под дальний удар и отскок
      setDist: 26,        // м до мяча: ближе — кипер «в стойке», шаги мельче
      setSpeed: 2.6,      // м/с в стойке готовности (не переступает через себя)
      // Сколько кипер успевает ПЕРЕСТУПИТЬ после удара, прежде чем бросаться.
      // Без потолка дальний удар «лечился» так: сначала пройти полтора метра
      // приставным шагом, потом ещё бросок во всю длину — и створ снова
      // накрыт целиком. В жизни бросок идёт из стойки, а не с разбега.
      setStepMax: 0.55,   // м от позиции в момент удара

      // --- РЕАКЦИЯ И ЧТЕНИЕ УДАРА ---
      // Зрительная реакция человека ~0.2 с; за это время мяч с 11 м уже пролетел
      // половину пути — отсюда и берутся незабираемые углы.
      react: 0.24,        // сек базовой реакции на новый удар
      reactJitter: 0.08,  // ± разброс реакции (кипер живой, а не автомат)
      reactScreen: 0.12,  // + сек, если удар нанесён из-за спин (мяч закрыт)
      screenRadius: 2.2,  // м от линии удар→ворота: игрок ближе — мяч закрыт
      // Ошибка чтения точки прилёта: чем сильнее и ближе удар, тем хуже читается.
      // Это ЕДИНСТВЕННЫЙ источник «дайв не в тот угол» — никаких бросков монеты.
      // Держим ошибку МАЛЕНЬКОЙ: вратаря побеждает ВРЕМЯ, а не слепота.
      // На 0.7 м разброса кипер начинал «промахиваться мимо мяча» даже на
      // ударах в живот — это выглядит как поломка, а не как футбол (замер 26.07)
      readNoise: 0.12,    // м базовой ошибки
      readNoiseSpeed: 0.010, // + м за каждый м/с скорости удара
      readNoiseClose: 7,  // м: с этой дистанции ошибка удваивается (близкий удар)
      readRefresh: 0.09,  // сек: как часто кипер уточняет прогноз в полёте
      // Порог «это удар». Был 4 м/с — и четверть мячей, РЕАЛЬНО влетающих в
      // ворота, вратарь вообще не читал как удар (замер Монте-Карло по 200 000
      // отскоков: средняя скорость мяча на линии 8.3 м/с, но медленный откат
      // порог не проходил). Он просто стоял на дуге. Это и есть жалоба Олега
      // «случайные отскоки чуть ли не с центра поля пропускает»
      onTargetSpeed: 1.2,

      // --- БРОСОК ---
      // Предельная дальность сейва = handReach + diveSpeed × diveTime ≈ 2.9 м.
      // Это МЕНЬШЕ полуширины ворот (3.66 м) — и именно поэтому у ворот есть
      // углы: из центра створа кипер физически не достаёт до штанги, сколько
      // бы времени ему ни дали. С прежними 3.6 м он накрывал весь створ и
      // вытаскивал даже идеальные девятки с 30 м (замер 26.07).
      handReach: 0.95,    // м: вытянутая рука от корпуса (полуразмах ~1.9 м)
      standReach: 0.62,   // м: сколько добирает шаг без броска
      // Пока идёт ВРЕМЯ РЕАКЦИИ, руки ещё не пошли — мяч можно поймать только
      // корпусом. Раньше проверка контакта не смотрела на reactLeft вообще:
      // вратарь играл мяч руками с первого кадра, и цилиндр 1.9 × 2.45 м
      // накрывал 26% ПЛОЩАДИ ворот (реальный силуэт вратаря — 6–8%). Отсюда и
      // «тянет мощные удары впритык»: с 8 м мяч летит 0.43 с, реакция 0.23 с —
      // сдвинуться он не успевает, но мяч всё равно оказывался в руках
      bodyReach: 0.38,    // м: корпус до истечения реакции
      // Тянуться вверх дороже, чем вбок: «девятка» потому и девятка. Раньше
      // потолок рук (2.45 м) равнялся высоте ворот — верхних углов физически
      // не существовало даже для стоящего вратаря
      standMaxY: 2.05,    // м: выше стоя (без броска и прыжка) не достать
      reachHighCost: 0.45, // 1/м: на сколько тает боковая досягаемость выше 1 м
      diveSpeed: 3.6,     // м/с бокового хода корпуса в броске
      diveTime: 0.55,     // сек полёта корпуса
      // Ручной наклон корпуса ПОВЕРХ клипа. Ноль: клип gk_dive сам кладёт
      // вратаря и сам поднимает (промер по риггу player.glb — бёдра 0.95 →
      // 0.18 → 0.92 м). Наши прежние 1.15 рад складывались с клипом, и
      // вратарь на секунду уходил под газон (фидбек Олега 26.07.2026)
      diveTilt: 0,
      diveHandBonus: 0.15, // м: в броске руки вытянуты чуть дальше, чем стоя
      diveMaxY: 2.45,     // м: потолок в броске (руки над головой в прыжке)
      diveHighFrom: 1.3,  // м: выше этой высоты бросок «верхний»…
      diveHighCost: 0.42, // …и съедает столько доли боковой дальности
      diveLowY: 0.55,     // м: мяч ниже — бросок «в ноги», дальность полная
      lungeSpeed: 6.4,    // м/с рывка на мяч (выход, 1в1) — быстрее приставного
      recover: 0.8,       // сек подъёма после броска
      recoverHold: 0.35,  // сек лёжа после УДАЧНОГО отбоя (быстрее встаёт)
      diveMinSpan: 0.35,  // м: короче — не бросок, а шаг (клип не дёргается)

      // --- ИСХОД КОНТАКТА: ПОЙМАЛ / ОТБИЛ / ВЫРОНИЛ ---
      // Реальность: ~70% ударов в створ отражаются, из них намертво берётся
      // примерно две трети; сильный и неудобный мяч кулаком/ладонью в поле.
      holdBase: 0.94,     // предел «намертво» при идеальном контакте
      holdThreshold: 0.30,// выше — мяч в руках, ниже — отбой. Порог, а не кубик:
                          // правило ресёрча «никакого рандома в момент истины»
      holdSpeedHard: 34,  // м/с: относительная скорость, при которой хват = 0
      suddenWindow: 1.20, // сек с момента удара, за которые «внезапность» тает…
      suddenReflex: 0.40, // …минус столько у рефлексивного кипера
      holdReachDrop: 0.85,// доля хвата, теряемая на пределе вытянутой руки (по rel²)
      holdHighDrop: 0.35, // доля хвата, теряемая на мяче выше головы
      holdLowDrop: 0.10,  // и на мяче у самой земли
      fumble: 0.09,       // вероятность выронить УЖЕ пойманный мяч: ×(1−handling)
      fumbleDrop: 3.2,    // м/с, с которой выроненный мяч уходит вперёд

      // --- ОТБОЙ ---
      // Цель ресёрча по исходам сейва: 45% намертво, 45% безопасный отбой,
      // 9% отбой в опасную зону, 1% выронил. Опасных отбоев должно быть МАЛО:
      // добивания дают 7.6% всех голов в реальном футболе, не больше.
      parryWide: 0.72,    // доля отбоев «правильных»: в сторону от ворот/за линию.
                          // Было 0.88 — почти любой контакт заканчивался
                          // безопасным отбоем, добиваний не существовало
      parrySpeedKeep: 0.42, // доля скорости мяча, остающаяся после отбоя
      parryLift: 2.2,      // подъём отбитого мяча (кулаком вверх)
      parryWideAngle: 62,  // ° разворота отбоя в сторону от створа
      parryLooseAngle: 22, // ° разворота «неудачного» отбоя — мяч в опасную зону
      parryTouchAngle: 14, // ° скользящего контакта кончиками пальцев (на угловой)
      parryTouchFrom: 0.72,// доля вытянутой руки, с которой контакт считается
                          // скользящим (ресёрч 16 §8: кончики пальцев — это
                          // reachFrac > 0.72). Пушка в упор, задетая на пределе,
                          // теперь чаще идёт дальше в ворота, а не «спасается»

      // --- ВЫХОДЫ ---
      // 1в1: соперник ведёт мяч в зоне угрозы и защитники не успевают — кипер
      // сокращает угол. Вышел рано — обыграют, поздно — пробьют: цена решения.
      rushRange: 17,      // м от ворот: владеющий соперник ближе — думаем о выходе
      rushMaxZ: 13,       // м от центра: с острого угла не выбегаем
      rushGap: 3.5,       // м: защитник ближе к сопернику — выход не нужен
      rushStop: 2.6,      // м до соперника: ближе кипер «раскрывается» (spread)
      // …но эта дистанция не может быть КОНСТАНТОЙ. Вплотную раскрытый вратарь
      // проецируется на линию ворот как стена: с 8 м он накрывал 92% створа, с
      // 10 м и дальше — все 100%. Мимо такой стены прицел AI не попадал вовсе.
      // Держим накрытие не выше половины створа: rushStop ≥ rushCoverK · dGoal
      rushCoverK: 0.55,
      rushMaxDepth: 6.0,  // м от линии: дальше вратарь не выбегает на 1в1
      rushSpreadY: 1.1,   // м высоты «звезды» — низом и по центру не пробить
      // Мяч за спину защите (sweeper): выходим, только если ЯВНО успеваем
      sweepRange: 26,     // м от ворот: дальше вратарь за мячом не выбегает
      sweepMargin: 0.35,  // сек запаса перед ближайшим соперником — иначе не идём
      sweepBoxOnly: false,// за штрафную выходить можно (ногой, не руками)
      // Навесы: забираем только реально свой мяч
      claimRadius: 6.0,   // м от точки прилёта: дальше не претендуем
      claimMaxY: 2.9,     // м: выше в прыжке не достать
      claimMargin: 0.30,  // сек запаса перед ближайшим соперником
      claimPunchCrowd: 3, // соперников в 3 м от точки — бьём кулаком, не ловим
      claimError: 0.07,   // вероятность «пролететь мимо» на выходе (флап)

      // --- РУКИ ---
      handleMaxY: 2.45,   // м: выше руками не играем
      handleOutsideBox: false, // за штрафной руками нельзя (правило)
      catchCooldown: 0.25, // сек: свой же отбитый мяч не подбираем мгновенно
      // Поймал — мяч гасится В РУКАХ (не отскок «как от дерева»). AI держит
      // holdTime и выносит с ноги; вратарь ЧЕЛОВЕКА получает управление и сам
      // выбирает — выбить ногой (УДАР) или бросить рукой (ПАС) — в течение
      // holdMaxHuman, иначе выносит автоматически (фидбек Олега 22.07.2026)
      holdTime: 2.0,      // сек мяч в руках у AI-вратаря перед выносом (было 0.9 — слишком резко)
      holdMaxHuman: 6.0,  // сек на выбор у вратаря человека, дальше авто-вынос
      oppKeepAway: 7,     // м: соперники не давят вратаря с мячом в руках (правило +
                          // фикс рикошета выброса в свои ворота, фидбек Олега 22.07)
      releaseGuard: 5,    // м: соперники в этом радиусе от вратаря в момент выброса...
      releaseGuardTime: 0.5, // ...не играют мяч столько сек — он успевает отлететь,
                          // а не рикошетит от прилипшего в упор в наши ворота
      holdY: 1.05,        // высота мяча в руках (полевые не дотянутся)
      clearPower: 23,     // сила выноса ногой
      clearLift: 7,       // подъём выноса ногой (высокая свеча на фланг)
      // Вылет мяча синхронён с КАДРОМ клипа (как во вбрасывании), а не по
      // таймеру: gk_dropkick (2.433 с) — нога встречает мяч на ~1.30 с;
      // gk_throw (1.6 с) — рука выпускает мяч на ~1.03 с (замерено по модели).
      puntClip: { start: 0.85, rate: 1.5, release: 1.30 },  // вынос ногой
      throwClip: { start: 0.5, rate: 1.35, release: 1.03 }, // бросок рукой
      throwPower: 15,     // рукой недалеко и настильно (точный пас в ноги)
      throwLift: 2.6,     // невысокая дуга броска рукой
    },

    // Переключение управляемого игрока (человек)
    switch: {
      cooldown: 0.6,      // сек между авто-переключениями — без дёрганья
      advantage: 0.72,    // сосед ближе к мячу в это число раз — переключаемся
    },
  },

  // ТВ-заставка перед матчем (22.07.2026): крупный план Tricolore на
  // центральной точке → свисток → камера медленно отлетает к паре
  // нападающих → ПАС разыгрывает с центра, камера доезжает в игровое
  // положение уже по живой игре. Кадры заданы для команды, разыгрывающей
  // слева направо; x зеркалится под сторону.
  intro: {
    ballTime: 2.6,        // сек крупного плана мяча (медленный облёт)
    pullTime: 3.2,        // сек отлёта камеры к нападающим
    goTime: 1.4,          // сек доводки в игровую ТВ-позицию после ПАС
    aiWait: 1.6,          // сек ожидания, когда с центра разыгрывает AI
    closeA: { x: 1.9, y: 0.46, z: 2.2 },  // старт облёта: мяч крупно
    closeB: { x: -1.6, y: 0.9, z: 2.7 },  // конец облёта: нападающие в фоне
    closeLookY: 0.2,      // взгляд в центр мяча
    mid: { x: 2.4, y: 2.9, z: 13.8 },     // общий план: пара нападающих у мяча
    midLook: { x: -1.8, y: 1.0, z: -1.8 },
    breath: 0.22,         // «дыхание» камеры оператора в ожидании розыгрыша
    // Бегущая строка с составами в начале матча. Скорость — в пикселях экрана
    // за секунду, а не «столько-то секунд на проход»: иначе длинный состав
    // ехал бы медленнее короткого. Один проход и строка гаснет.
    lineupSpeed: 300,
  },

  camera: {
    fov: 33,              // зум ~30% ближе (было 42, фидбек Олега 18.07.2026:
                          // «игроки мелкие на маленьком экране») — зумим фокусным,
                          // как настоящая телекамера, позицию не двигаем
    height: 27,           // высота ТВ-камеры над газоном (ниже = план крупнее)
    distance: 55,         // отступ за боковую линию — ближняя бровка ещё в кадре
    followFactor: 0.6,    // насколько камера едет за мячом по X (0 — стоит, 1 — прилипла)
    followZ: 0.7,         // прицел тянется по ГЛУБИНЕ за мячом — держит игрока в кадре
                          // и у ближней, и у дальней бровки (без этого ближний выпадал вниз)
    lookHeight: 2,        // высота точки, на которую смотрит камера
    // Живой оператор (дух камеры «Как обычно» из FC 25/26, 22.07.2026):
    // панорама упреждает полёт мяча, но плавно. ВАЖНО (фидбек Олега 22.07:
    // «камера дёрганная»): и упреждение, и цель камеры считаются от СГЛАЖЕННЫХ
    // позиции/скорости мяча — сырые ball.pos/ball.vel скачут на каждом
    // касании/отскоке и рвут кадр. Само сглаживание + мягкая доводка = штиль.
    lerp: 0.05,           // плавность доводки в спокойной игре
    lerpFast: 0.075,      // на быстром мяче камера панорамирует чуть живее
    speedRef: 24,         // м/с мяча, при которых доводка выходит на lerpFast
    lead: 0.4,            // сек: прицел смотрит туда, куда мяч ЛЕТИТ
    leadMax: 5,           // м: потолок упреждения (панорама не убегает от игры)
    followSmooth: 3.2,    // 1/с: фильтр ПОЗИЦИИ мяча для слежения (τ≈0.3 с) —
                          // гасит дрожь от касаний/отскоков, кадр не рвётся
    leadSmooth: 1.8,      // 1/с: фильтр СКОРОСТИ мяча для упреждения (τ≈0.55 с) —
                          // чем меньше, тем плавнее панорама (и ленивее реакция)
    // Подлёт к дальней бровке (принцип FIFA/FC: камера плавно приближается,
    // когда мяч уходит от неё вглубь — игрок на дальней бровке не «мельчает»)
    farApproach: 16,      // насколько сокращается дистанция при мяче на дальней бровке
    farLower: 3.5,        // и насколько камера опускается (ниже = крупнее план)
    // Подлёт в финальную треть: атака у любых ворот показывается крупнее
    attackApproach: 6,    // сокращение дистанции в финальной трети
    attackLower: 1.8,     // и снижение камеры там же
    attackFrom: 0.17,     // доля длины поля от центра, где начинается подлёт
    attackSpan: 0.3,      // за какую долю поля подлёт набирает полную силу
  },

  // Бригада арбитров — чисто визуальные фигуры (src/officials.js).
  // На поле не должно быть ровно 22 тела и никого больше: это первое, что
  // выдаёт «не трансляцию». Свистки и офсайды — отдельная тема (Фаза 5).
  officials: {
    refereeColor: '#22242c',   // классический тёмный комплект арбитра 90-х
    linesmanColor: '#2a2d36',
    refereeSpeed: 6.2,         // м/с: судья держится за темпом, но не бежит наравне
    linesmanSpeed: 6.6,
    accel: 3.2,                // 1/с: мягкий разгон, без рывков
    trailX: 9,                 // м позади мяча по ходу атаки
    diagonalZ: -9,             // м вбок — судейская диагональ. Знак минус: судья
                               // держится на ДАЛЬНЕЙ от ТВ-камеры стороне, иначе
                               // он постоянно маячит перед игроками в кадре
    keepAway: 7.5,             // ближе этого к мячу судья не подходит
    lineOffset: 1.6,           // м за боковой линией стоит лайнсмен
  },

  // Повтор гола: замедленная запись эпизода с другой камеры.
  // Правило Олега (26.07.2026): в повтор попадает ВСЯ КОМБИНАЦИЯ — отматываем
  // назад до момента, когда мяч забрала забившая команда, а не последний удар.
  replay: {
    rate: 30,             // снимков в секунду (в буфер пишем позы, не физику)
    memory: 30,           // сек истории: атака + гол + празднование (≈580 КБ)
    minSeconds: 3.5,      // короче этого повтор не показываем — нечего смотреть
    maxSeconds: 6.5,      // потолок: длинная возня в центре поля никому не нужна
    preRoll: 1.0,         // сек ДО отбора: видно, как мяч перешёл к нам
    afterGoal: 1.3,       // сек ПОСЛЕ гола в кадре: мяч в сетке и первая реакция
    finishLead: 3.0,      // сек до гола, с которых начинается второй повтор
    // Темп как в трансляции: комбинацию гоняют в своей скорости, а завершение
    // уходит в слоу-мо. Равномерное замедление растянуло бы атаку вдвое.
    speed: 1.0,           // скорость показа комбинации
    slowSpeed: 0.5,       // скорость показа завершения
    slowTail: 1.7,        // сек записи до гола, с которых включается слоу-мо
    finishSpeed: 0.8,     // второй повтор (завершение) идёт медленнее с самого начала
    partySpeed: 0.9,      // празднование — почти живой темп, оно и так «крупное»
    partyMax: 3.5,        // сек записи празднования в повторе: дальше зритель заскучает
    // Камера повтора строится ВОКРУГ цели и всегда смотрит на неё, поэтому
    // «объектив в щиты» невозможен. Здесь — только её манеры.
    camSmooth: 6,         // 1/с: сглаживание цели (рикошет не должен дёргать кадр)
    camEase: 3.5,         // 1/с: доводка позиции камеры внутри одного ракурса
    camLead: 0.28,        // сек упреждения взгляда по скорости цели
    camMinDist: 7,        // м: ближе камера не подходит — иначе цель «в объективе»
    camMinY: 1.6,         // м: ниже газона и в газон камера не опускается
    camMaxOut: 12,        // м за пределы поля: дальше начинаются трибуны
  },

  // Празднование гола: без него повтор обрывался ровно на мяче в сетке
  celebration: {
    time: 5.0,            // сек живого празднования до серии повторов
    runTo: 24,            // м по Z: автор бежит к ближней бровке, в сторону камеры
    joinCount: 4,         // сколько партнёров бегут обнимать автора
    joinRadius: 2.2,      // м: на таком расстоянии они его окружают
    jumpEvery: 1.1,       // сек между подскоками автора
    jumpHeight: 0.55,     // м высота подскока
    camHeight: 4.2,       // м: живая камера празднования идёт низко
    camDist: 15,          // м до автора
    camEase: 2.5,         // 1/с доводки
  },

  // Ночная атмосфера трансляции: свет мачт, вспышки фотокамер на трибунах,
  // веер теней под игроками. Чистый визуал — физика и AI сюда не смотрят.
  atmosphere: {
    // Степень износа газона: 0 = ухоженный, 0.5 = золотая середина,
    // 1 = сильно вытоптанный. Пользователь меняет её в настройках.
    pitchWear: {
      default: 0.5,
    },
    // Замкнутая олимпийская чаша. Прямые сектора продолжаются хордами вокруг
    // виражей дорожки: открытых углов и чёрных стыков больше нет.
    stands: {
      height: 17,          // м по наклонной лицевой плоскости сектора
      tilt: -0.42,         // рад: нижний ряд у поля, верхний уходит наружу
      curveSegments: 8,    // хорд на одну полуокружность; 8 уже читаются как овал
      seamOverlap: 4.2,    // м: хватает и верхнему ряду, который шире нижнего на наклоне
      trackInset: 0.8,     // м: нижний ряд чуть заходит на внешнюю отбивку дорожки
      textureWorldWidth: 136, // м на полный оборот 8:1-текстуры при высоте 17 м
    },
    // Мачты освещения. От ЭТИХ координат считаются и столбы, и гало ламп,
    // и пятна света на газоне, и направления теней — источник света один.
    masts: {
      marginX: 4,          // м за зелёной отбивкой по длине поля
      marginZ: 4,          // то же по ширине
      height: 28.5,        // высота ламп над газоном
    },
    // Пятна света на газоне: ночью поле неравномерное — под мачтами ярче,
    // в середине и по дальним углам тусклее. Печатается прямо в текстуру
    // газона, поэтому в рантайме не стоит ни кадра.
    pitchLight: {
      pool: 0.11,          // яркость пятна под одной мачтой (больше — свет «пятнами»)
      poolRadius: 0.78,    // радиус пятна в долях длины поля: широкие мягкие перекрытия
      poolInset: 0.26,     // куда смещён центр пятна от центра поля (доли поля)
      falloff: 0.20,       // насколько темнеют дальние края
    },
    // Гало вокруг ламп: аддитивная засветка, как ореол на кинескопе
    halo: {
      size: 17,            // м: диаметр спрайта
      opacity: 0.42,
    },
    // Вспышки фотокамер на трибунах — фирменная примета вечернего эфира 90-х
    flashes: {
      count: 64,           // сколько «фотоаппаратов» живёт одновременно
      // Темп на ВЕСЬ стадион: в кадре видна примерно четверть трибун,
      // поэтому 22/сек читаются как редкие одиночные щелчки, а не гирлянда.
      rate: 22,
      life: 0.15,          // сек: сколько горит одна
      size: 2.2,           // м: видимый размер пятна
      burstRate: 7,        // во сколько раз чаще щёлкают сразу после гола
      burstTime: 5,        // сек: за столько зал успокаивается
      moodLift: 0.42,      // насколько светлеют сектора на голе (трибуна встала)
      minRow: 0.10,        // не ниже этой доли высоты сектора (там бортик)
      maxRow: 0.94,
    },
    // Тени: под мачтами их четыре, веером — это и есть «ночной футбол».
    // Длина считается геометрией: рост × дистанция до мачты / высота мачты.
    shadows: {
      // 0.50 — подобрано на кадре игры (27.07.2026). Цифра кажется большой, но
      // это непрозрачность ОДНОГО пятна В ПИКЕ маски: по краям она тает почти
      // сразу, а сливаются четыре тени только под самыми ногами — там и должно
      // быть темно. Прежние 0.13 держались на баге: маска не работала вовсе,
      // и пятно было СПЛОШНЫМ прямоугольником — 0.13 просто прятали его.
      opacity: 0.50,       // непрозрачность ОДНОЙ тени из четырёх
      // Тени должны СЛИВАТЬСЯ в общее пятно под ногами и расходиться мягкими
      // хвостами. Узкие и длинные читаются как крест-пропеллер — это хуже,
      // чем одна круглая тень, от которой мы ушли.
      width: 1.15,         // м: ширина пятна под ногами
      widthGrow: 0.26,     // насколько хвост шире с каждым метром длины (полутень)
      minLength: 0.9,      // м: короче не рисуем — под ногами всегда есть пятно
      maxLength: 2.8,      // м: геометрически тень длиннее, но кадр от неё грязный
      lengthScale: 0.44,   // общий множитель длины (ТВ-вкус важнее геометрии)
      // Форма пятна в карте прозрачности. core — доля длины, которую занимает
      // плотное «контактное» ядро под ногами; feather — насколько мягко ядро
      // растворяется вбок; tail — как быстро тает хвост (больше = короче).
      core: 0.30,
      feather: 1.9,
      tail: 2.3,
      spread: 0.85,        // во сколько раз хвост шире ядра (полутень расходится)
    },
    // Дымка ночного стадиона. Дальняя трибуна и дальние ворота обязаны
    // «садиться» в подсвеченный прожекторами воздух — без воздушной
    // перспективы кадр читается как макет, а не как трансляция.
    // Числа считаны от РАБОЧЕЙ ТВ-камеры (0, 27, 55): центр поля от неё в 61 м,
    // дальняя бровка в 93, дальняя трибуна в 112, край отбивки в 155. Значит
    // дымка обязана начинаться около 50 м — иначе она не достаёт вообще ничего
    // из того, что видно в кадре (прежние 130 м именно так и работали).
    haze: {
      color: 0x2a3a5c,     // цвет дымки: зарево мачт, а не чёрная ночь
      near: 50,            // м от камеры: ближе дымки нет
      far: 215,            // м: дальше всё растворилось полностью
    },
    // Серпантин и бумажный дождь на выходе команд. Клочки мелкие, поэтому
    // овердро почти нет — можно позволить сотни частиц там, где дыма хватало
    // на две сотни квадов.
    confetti: {
      max: 320,             // размер пула
      burstCount: 95,       // сколько бросает ОДИН сектор на выходе команд
      goalCount: 55,        // и сколько после гола своей команды
      fallback: '#e8e8e0',  // цвет, если команда его не задала
      whiteShare: 2,        // сколько «белых» долей на один цвет формы
      // Лент немного и они КОРОТКИЕ: длинная лента под случайным углом
      // читается спичкой, и полсотни таких — рассыпанный коробок, а не
      // бумажный дождь (проверено на плане сектора 27.07.2026).
      ribbonShare: 0.22,    // доля лент серпантина (остальное — клочки)
      ribbonWidth: 0.075,   // м
      ribbonLength: 1.5,    // м
      ribbonTilt: 0.85,     // рад: лента висит почти вертикально — она ПАДАЕТ,
                            // а не лежит поперёк воздуха
      ribbonSway: 0.4,      // рад: и покачивается около этого наклона
      ribbonSwayRate: 1.8,  // рад/с
      ribbonLife: 9,        // сек
      ribbonSpin: 1.2,      // рад/с
      bitSize: 0.22,        // м: сторона клочка бумаги
      bitLife: 7,           // сек
      bitSpin: 3.5,         // рад/с
      spread: 3.5,          // м: разброс точки вылета по сектору
      throw: 4.5,           // м/с: бросок к полю
      throwUp: 3.2,         // м/с: и вверх
      gravity: 2.6,         // м/с²: бумага почти не весит
      drag: 1.5,            // 1/с: зато сильно тормозит о воздух
      fallMax: 2.2,         // м/с: предельная скорость падения
      sway: 1.3,            // м/с: качание из стороны в сторону
      swayRate: 2.2,        // рад/с
      flipRate: 5.5,        // рад/с: кувыркание (сжатие видимой ширины)
      edge: 0.12,           // насколько узкой становится лента «с ребра»
      restY: 0.06,          // м: высота, на которой бумага ложится
      white: [0.80, 0.80, 0.76],  // «газетный» белый: чистый бьёт по глазам
      alpha: 0.82,
      fade: 0.22,           // доля жизни на угасание в конце
    },
    // Волна на трибунах. Считается прямо в материале секторов: фронт бежит
    // по углу вокруг центра поля, толпа в нём сдвигается по текстуре вверх
    // и светлеет. Новой геометрии — ноль.
    wave: {
      firstDelay: 150,     // сек до первой волны: сначала пусть идёт игра
      period: 190,         // сек между волнами (±30%)
      speed: 1.25,         // рад/с: обход всей чаши примерно за 5 секунд
      width: 0.5,          // рад: ширина фронта (~1/12 круга — сектор-полтора)
      laps: 2,             // сколько кругов проходит волна
      lapsSpread: 1.6,     // и случайная добавка к ним
      fade: 2.2,           // рад: на входе набирает силу, на выходе рассыпается
      lift: 0.09,          // сдвиг текстуры вверх в долях высоты сектора
      // Ночная трибуна почти чёрная, и осветление на половину на ней просто
      // не читается: 0.55 давали +0.8 средней яркости кадра — глазу ничего.
      // Встающий ряд ловит свет мачт лицами и руками, он ЗАМЕТНО ярче.
      bright: 1.4,
      skew: 0.25,          // рад: верхние ряды встают чуть позже нижних
    },
    // Мошкара у ламп. Ночной стадион под прожекторами всегда в мошкаре, и
    // это такая же примета вечернего эфира, как фотовспышки. Движение целиком
    // считает вершинный шейдер — процессору мошкара не стоит ничего.
    midges: {
      perMast: 70,         // мошек на одну лампу (всего ×4)
      // Размер ПРЕУВЕЛИЧЕН, как разметка поля: настоящая мошка с рабочей
      // ТВ-камеры (125 м до дальней мачты) не заняла бы и десятой пикселя.
      size: 0.16,          // м: видимый размер
      opacity: 0.55,
      spread: 5.5,         // м: радиус облака вокруг лампы
      drop: 9.0,           // м: насколько облако свисает ВНИЗ от лампы —
                           // в конус света, где мошкара и заметна
      orbit: 0.55,         // м: размах блуждания одной мошки
      rate: 1.5,           // рад/с: базовая частота блуждания (у каждой своя)
      flicker: 7.0,        // рад/с: мерцание — поворот крыла к свету
    },
    // Пиротехника на трибунах. Файер в трансляции 98-го — это ПЕРЕСВЕТ на
    // кинескопе: белое ядро, цветной ореол, столб дыма через лучи мачт.
    // Физически честного пламени тут нет и не нужно (см. src/flares.js).
    // Бюджет считаем по ОВЕРДРО, а не по числу частиц: клуб дыма 7 м на
    // дистанции 100 м занимает ~130 пикселей, и триста таких клубов — это
    // несколько полноэкранных проходов на планшете. Отсюда скупые лимиты.
    flares: {
      maxLive: 10,          // сколько файеров горит одновременно
      burstCount: 5,        // сколько зажигается по умолчанию за раз
      introCount: 3,        // на выходе команд — с КАЖДОГО фанатского сектора
      life: 24,             // сек: среднее время горения (±25% на файер)
      igniteSpread: 1.8,    // сек: разброс момента поджига внутри группы
      igniteTime: 0.45,     // сек: выход на полную яркость
      igniteFlash: 2.6,     // во сколько раз ярче пик поджига
      fadeTime: 3.0,        // сек: угасание в конце
      flicker: 11,          // рад/с: частота дрожания пламени
      flickerDepth: 0.14,   // глубина дрожания (доля яркости)
      color: 0xff3a18,      // цвет по умолчанию, если команда не задана
      tintSat: 0.9,         // насыщенность ореола от цвета формы
      tintMinL: 0.42,       // светлее этого: файер — источник СВЕТА, не краска
      coreSize: 0.8,        // м: раскалённое добела ядро
      haloSize: 2.9,        // м: цветной ореол пламени
      haloAlpha: 0.78,
      glowSize: 11.0,       // м: зарево на секторе вокруг факела
      glowAlpha: 0.2,       // файер освещает соседей — без этого он «наклейка»
      fogKill: 0.75,        // насколько дымка гасит огонь на дальней трибуне
      // Где сидит фанатский сектор: виражи за воротами, ближе к углу
      curveFrom: 0.55,      // доля straightHalf: ближе к центру секторы не берём
      spotsPerSection: 16,  // мест в фанатском секторе (из них выбираем без повторов)
      groupWidth: 0.34,     // доля ширины полотна: группа компактная, не по всей трибуне
      standOffset: 3.0,     // м перед полотном: больше перекрытия секторов (4.2 м
                            // по наклону ≈ 2.7 м по горизонтали), иначе соседнее
                            // полотно режет дым резкой диагональю
      minRow: 0.18,         // не ниже этой доли высоты сектора
      maxRow: 0.62,         // и не выше: файеры держат в руках, а не на крыше
      // Дым
      fireMax: 30,          // квадов огня (3 на файер)
      smokeMax: 200,        // квадов дыма — главный расход, трогать осторожно
      smokeRate: 4.2,       // клубов в секунду с одного файера
      smokeLife: 7.0,       // сек жизни клуба
      smokeSize: 1.5,       // м при рождении
      smokeGrow: 7.5,       // м к концу жизни
      // Плотность подобрана на ИГРОВОМ плане (ТВ-камера, ~110 м до сектора),
      // а не на крупном: на 30 м любая цифра выглядит прилично, а в реальном
      // кадре дым просто пропадал.
      smokeAlpha: 0.38,     // пиковая плотность клуба
      // Плотность обязана набираться БЫСТРО: пока клуб прозрачный, он успевает
      // подняться, и столб дыма отрывается от факела заметным зазором.
      smokeRise: 0.05,      // доля жизни на набор плотности
      smokeUp: 1.5,         // м/с: стартовый подъём
      smokePush: 1.1,       // м/с: стартовый толчок в сторону поля
      smokeSpin: 0.7,       // рад/с: разброс вращения клуба
      buoyancy: 0.5,        // м/с²: тёплый дым разгоняется вверх
      windGrab: 0.5,        // 1/с: как быстро клуб перенимает ветер
      wind: { x: 2.2, z: 0.8 },  // м/с: ветер стадиона сносит дым на поле
      windVary: 0.55,       // доля разброса ветра между клубами: столб живой,
                            // а не струя из шланга — все клубы летят по-разному
      swirl: 0.5,           // м/с: боковое блуждание клуба (турбулентность)
      swirlRate: 0.9,       // рад/с: как быстро блуждание меняет знак
      // Ночью дым НЕ БЕЛЫЙ: снизу его красит сам факел, выше остаётся тёмная
      // масса, слегка подсвеченная мачтами. Светлый дым читается паром и
      // оказывается ярче трибуны, на фоне которой стоит.
      smokeLow: [0.68, 0.34, 0.15],   // цвет клуба у факела (подкрашен огнём)
      smokeHigh: [0.63, 0.62, 0.64],  // и выше, где его красят только мачты
      // Пелена: остывший дым сползает с трибуны и стелется по газону.
      // Квады ЛЕЖАЧИЕ (см. GROUND_VERT) — и правдивее, и вдвое-втрое дешевле
      // вертикальных того же размера, потому что ТВ-камера смотрит сверху.
      hazeMax: 26,          // квадов пелены — она большая, бюджет считаем строго
      hazeRate: 0.16,       // штук в секунду с одного файера (редко!)
      hazeLife: 26,         // сек: живёт втрое дольше клуба дыма
      hazeY: 0.35,          // м над газоном: пелена лежит, а не висит
      hazeSize: 8,          // м при рождении
      hazeGrow: 17,         // м к концу жизни — расползается
      // Плотность и цвет: разрежённый остаток дыма. 0.26 при белом цвете дали
      // «неестественно белый» дым на газоне (фидбек Олега 27.07.2026), 0.14
      // не читались вовсе. Рабочая точка — заметно прозрачнее и НЕ белый:
      // ночью дым красят мачты, а они дают холодный свет, а не чистый белый.
      hazeAlpha: 0.17,
      hazeRise: 0.22,       // доля жизни на набор плотности (медленно проступает)
      hazeFade: 0.3,        // и на угасание в конце
      // От сектора до бровки поля около 20 м, и пелена обязана их пройти:
      // дым, залёгший на дорожке, в кадр просто не попадает. Путь затухающего
      // дрейфа считается как drift/drag — здесь 2.2/0.12 ≈ 18 м.
      hazeStart: 8,         // м от сектора к полю: точка, где дым уже сполз вниз
      hazeStartSpread: 12,  // и разброс этой точки
      hazeDrift: 2.2,       // м/с: начальный ход к полю
      hazeWind: 0.35,       // какую долю общего ветра пелена берёт себе
      hazeDrag: 0.12,       // 1/с: трение о газон — пелена расползается и встаёт
      hazeSpin: 0.12,       // рад/с: медленное вращение пятна
      hazeColor: [0.57, 0.59, 0.64],
    },
    // Конусы света под лампами мачт: в сыром вечернем воздухе луч виден
    // у самой лампы и гаснет, не долетая до газона. Луч ДО земли выглядит
    // как мультик и упирается в поле резким срезом.
    shafts: {
      enabled: true,
      opacity: 0.32,       // пик яркости у лампы (аддитивно)
      radius: 13,          // м: радиус конуса у нижнего среза
      length: 30,          // м: длина луча вниз-внутрь от лампы
      fade: 1.7,           // степень затухания к низу (больше = короче видимый луч)
      edge: 1.5,           // мягкость силуэта конуса (больше = тоньше «стенка»)
    },
  },

  render: {
    // Высота внутреннего рендера; ширина — по аспекту.
    // 1080 — выбор Олега (27.07.2026): на iPad «летает и не греет», значит
    // держим максимум. Строчность и зерно от разрешения не зависят
    // (scanLines / grainRes ниже), поэтому ретро-дух от этого не страдает.
    // Ступени ниже остаются рычагом на слабом железе — клавиша ЧЁТКОСТЬ.
    targetHeight: 1080,
    maxAspect: 2.2,
    // Ступени селектора качества: [высота, подпись]
    heights: [
      [240, '240p · PS1'],
      [360, '360p'],
      [480, '480p · ТВ 90-х'],
      [720, '720p · чётко'],
      [1080, '1080p · максимум'],
    ],
    // Строчность и зерно ОТВЯЗАНЫ от разрешения рендера. Иначе подъём до 720p
    // делает полосы вдвое тоньше пикселя монитора и превращает их в муар,
    // а зерно — в еле заметную пыль. Кинескоп 90-х — это ~480 строк, сколько
    // бы пикселей мы ни рисовали.
    // Оверскан кинескопа: во сколько раз растр шире видимой части стекла на
    // единицу кривизны. Без него выпуклый пресет оставляет чёрные поля внутри
    // рамки корпуса, и картинка читается как «экран меньше стекла».
    overscan: 1.35,
    scanLines: 480,
    grainRes: 420,
    // Halation кинескопа: белые футболки, разметка и лампы «расплываются»
    // в стекле экрана. Считается на четверти разрешения — три микропрохода
    // (яркая часть + два размытия) стоят меньше 5% от главного CRT-прохода.
    bloom: {
      enabled: true,
      downscale: 4,        // во сколько раз меньше главного буфера
      threshold: 0.62,     // ниже этой яркости свечение не рождается
      knee: 0.28,          // мягкость порога: без неё видна ступенька
      radius: 1.35,        // множитель шага размытия (шире = мягче ореол)
      strength: 0.55,      // сила по умолчанию; пресет ТВ может переопределить
    },
  },
};

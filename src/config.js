// Все константы игры и баланса — только здесь (наш аналог Params.ini из Simple Soccer).
// Менять числа — можно смело; хардкодить их в логике — нельзя.

export const CONFIG = {
  field: {
    length: 105,          // метры, по оси X (стандарт ФИФА)
    width: 68,            // метры, по оси Z
    apron: 12,            // зелёная зона за разметкой
  },

  goal: {
    width: 7.32,          // между штангами (по Z)
    height: 2.44,
    postRadius: 0.09,     // чуть толще реальных, чтобы читались издалека
  },

  ball: {
    radius: 0.45,         // крупнее реального — ретро-игры так делали, мяч должен читаться
    gravity: -22,         // «аркадная» гравитация: мяч падает бодрее настоящего
    bounce: 0.55,         // сколько скорости остаётся после отскока от газона
    rollFriction: 0.988,  // затухание качения за кадр (при 60 fps)
    airFriction: 0.998,   // сопротивление воздуха
    stopSpeed: 0.12,      // ниже этой скорости мяч останавливается
    kickPower: 26,        // базовая сила удара-тапа
    kickLift: 6,          // подъём мяча при ударе
  },

  camera: {
    fov: 40,
    height: 30,           // высота ТВ-камеры над газоном
    distance: 48,         // отступ от центра поля за боковую линию
    followFactor: 0.55,   // насколько камера едет за мячом по X (0 — стоит, 1 — прилипла)
    lerp: 0.04,           // плавность доводки камеры за кадр
  },

  render: {
    targetHeight: 240,    // высота внутреннего рендера — «разрешение PS1»; ширина по аспекту
    maxAspect: 2.2,
  },
};

# Грабли и приёмы — «Футбол 98»

Рабочий журнал: **проблема → причина → решение.** Пополняется по ходу разработки. Стартовый набор — из ресёрча 17.07.2026 и опыта «Новоселья», по мере проверки на практике помечаем ✅.

## Звук
- **OGG не играет в Safari.** Все звуки и клипы комментатора — только MP3 (mono 64 kbps для голоса достаточно).
- **iOS: звук стартует только после первого касания экрана.** Автоплей заблокирован системой. Howler.js разблокирует аудио по первому тапу сам — но проверить на реальном iPad.

## Three.js и рендер
- ✅ **Внешняя PNG-текстура на glTF-модели переворачивается и мылится без явных настроек (23.07.2026).** Встроенная glTF-карта уже подготовлена загрузчиком, а подменная `TextureLoader`-карта приходит с браузерными дефолтами. Для пиксельного атласа формы обязательно ставить `flipY=false`, `NearestFilter` на min/mag, `generateMipmaps=false`, `SRGBColorSpace`; путь хранить в JSON команды, а текстуру кэшировать по пути, иначе 22 игрока создадут 22 одинаковые загрузки.
- **Современный Three.js — только ES-модули.** Старый `<script src=...>` не работает: нужен `importmap` + CDN (jsdelivr/unpkg). Держать локальную копию three.module.js на случай проблем с CDN.
- ✅ **Three.js — это ДВА файла (проверено 17.07.2026).** С версии ~r167 сборка разделена: `three.module.js` внутри импортирует `./three.core.js`. Скачал только первый — весь граф модулей молча падает с «Failed to fetch dynamically imported module». Качать оба в `vendor/`.
- ✅ **Ошибки модулей — молчаливые.** Упавший `<script type="module">` не всегда виден в консоли. В index.html стоит ловушка `window.addEventListener('error', ...)`, которая выводит сбой прямо на экран в #hint — не удалять её.
- **CRT-постобработка дорогая на планшете.** Каждый полноэкранный проход бьёт по мобильному GPU. Решение: ОДИН объединённый проход (scanlines+виньетка+кривизна) + рендер сцены в ~320×240 — что заодно и есть стиль PS1.
- **WebGL-контекст на iOS может «теряться»** (страница в фоне, нехватка памяти). Нужен обработчик `webglcontextlost`/`webglcontextrestored`, иначе — чёрный экран без ошибок.

## Рендер и муар
- ✅ **Разметка бьётся в пунктир и мигает (грабля от Олега, 17.07.2026).** Причина: тонкие линии (1 px в текстуре) + NearestFilter при уменьшении = муар. Лечение: у текстур поля/трибун `minFilter: LinearMipmapLinearFilter` + `anisotropy: 4` (вблизи можно оставить Nearest — PS1-пиксели не страдают), линии разметки рисовать толще реальных (0.3 м).
- ✅ **Хорошая AI-текстура может выдать швы после тайлинга (18.07.2026).** Даже если картинка заявлена seamless, крайние пиксели часто различаются по тону. Лечение для органических поверхностей: чередовать обычную и зеркальную плитку (`MirroredRepeatWrapping` на GPU или зеркальный `drawImage` на canvas) — на каждом стыке встречается один и тот же край. Обязательно проверять не исходный PNG, а весь газон с рабочей ТВ-камеры.
- ✅ **Текстура, нормальная без фильтра, может почти исчезнуть после CRT (18.07.2026).** Сгенерированный травяной albedo имел натуральную тёмно-оливковую яркость, но цепочка sRGB → низкое разрешение → scanlines/виньетка превращала поле в ночное пятно. Лечение: калибровать материал только через финальный «Эфир-98»; для запечённого газона добавить мягкую `screen`-подсветку цвета прожекторов на canvas, не поднимать яркость всего CRT-пресета (иначе пересветятся табло и игроки).
- ✅ **Бесшовная плитка не умеет правдоподобно изнашивать ЦЕЛОЕ футбольное поле (26.07.2026).** Она повторяет одну и ту же траву у ворот, в центре и за бровкой; добавленные кодом одинаковые эллипсы выглядят слишком аккуратно. Лечение: для игровой части — одна полноразмерная albedo-карта в пропорции 105×68 с локальным износом, для внешней травы — отдельная бесшовная плитка. Точные линии, слабые неровные полосы покоса и свет мачт оставлять кодом. Проверять общий центр и обе штрафные в конечном низком ТВ-рендере.
- ✅ **Ползунок между текстурами не должен одновременно быть ползунком яркости (26.07.2026).** Даже три травяных albedo из одной зелёной гаммы имели заметно разную среднюю экспозицию: «золотая середина» выглядела просто тёмным полем. Перед смешиванием привести каждый источник к одной воспринимаемой яркости своей `screen`-калибровкой, затем интерполировать 0→50→100 %. Проверять три значения на ОДНОМ ракурсе; иначе камера и свет маскируют ошибку.
- ✅ **Размер рекламного атласа ≠ физический размер одного щита (18.07.2026).** Если простыню из шести логотипов повторять раз в 8 м, каждый бренд занимает ~1,3 м и в 240p превращается в цветной шум. Полный шестибрендовый цикл должен занимать около 48 м: один логотип ≈ один крупный 8-метровый щит. Масштаб проверять в метрах геометрии, а не «repeat выглядит разумно».
- ✅ **Даже резкий PNG щитов может размыться до CRT из-за неверной пропорции mipmap (18.07.2026).** Атлас `1024×128`, растянутый на `48×1 м`, имел примерно в шесть раз разную плотность texel/м по осям; trilinear заранее усреднял тонкие буквы. Лечение: canvas `1024×32` ближе к физической пропорции, anisotropy 8 и `LinearMipmapNearestFilter` только для щитов. В широком плане допустимо отдельно преувеличить визуальную высоту, не меняя физическую.
- ✅ **Декоративная дорожка вторым прозрачным слоем создаёт больше проблем, чем геометрии (18.07.2026).** Почти совпадающие плоскости дают z-fighting, прозрачность требует сортировки и добавляет draw call; вдобавок поздняя загрузка травяного PNG может стереть уже нарисованный декор. Лечение: рисовать траву, восьмиполосный овал и его износ одной функцией в общей непрозрачной CanvasTexture, а после `img.onload` повторять всю композицию. Реальные 5-см линии исчезают в 480p — допустимо ТВ-преувеличение до 0,18 м. Если под наклонной трибуной остаётся «чёрная земля», проверить геометрию: у нас это оказалось ночное небо в пятиметровом проёме. Не маскировать его отдельной сплошной стеной — в ТВ-кадре она превращается в огромный забор. Правильно опустить сам наклонный фасад до покрытия, сохранив размер сектора и масштаб текстуры толпы. Торцевой сектор перед наклоном требует `rotation.order = 'YXZ'`, иначе его нижняя кромка идёт по диагонали.
- ✅ **Превью-вкладка Claude может быть «скрытой» (document.hidden=true) — тогда requestAnimationFrame спит и игра выглядит намертво зависшей.** Это НЕ баг игры: на реальном устройстве всё работает. Проверка логики в таком состоянии — синхронный прогон `DBG.input/player/ball.update()` + ручной `DBG.crt.render()` через вставленный в страницу script.
- ✅ **Обратная сторона той же грабли (17.07.2026): вкладка может ОЖИТЬ посреди синхронного теста** — тогда родной rAF-цикл игры дублирует твой прогон (input.update дважды за кадр, двойная физика) и даёт фантомные результаты (у стоячего игрока «удар с лёта»). Замеры доверять только когда логгер внутри самой функции показывает её реальные входы; interval-таймеры в скрытой вкладке к тому же душатся до 1 раза/мин — весь тест держать одним синхронным for-циклом в одном вызове.
- ✅ **Vector3.project(camera) берёт УСТАРЕВШУЮ матрицу (17.07.2026).** `project()` читает `camera.matrixWorldInverse` без пересчёта. После ручного `camera.position.set()`+`lookAt()` (вне цикла рендера) она стара → NDC-числа врут (получал −1.2 там, где должно быть −0.7). Перед проверкой камеры-фрейминга: `camera.updateMatrixWorld(true); camera.matrixWorldInverse.copy(camera.matrixWorld).invert();` — потом project. Так подбирал позицию камеры по NDC точно.

## Ночной свет и атмосфера
- ✅ **Ровный свет — главный признак «примитивной» картинки (26.07.2026).** Однотонный газон, одна круглая тень под игроком, неподвижные трибуны. Лечение дешёвое: пятна прожекторов впечатать прямо в текстуру газона (в рантайме 0 мс), тень заменить веером из четырёх (по числу мачт), на трибуны добавить редкие вспышки фотокамер. Координаты мачт при этом должны жить в ОДНОМ месте — иначе свет, гало и тени начнут расходиться.
- ✅ **Геометрически честная длина тени делает кадр грязным (26.07.2026).** Лампа на 28,5 м, игрок в 85 м от неё → тень 5–7 м; четыре такие тени у 22 игроков превращают поле в паутину. Брать половину расчётной длины, ставить потолок ~3,4 м, хвост расширять и быстро гасить (альфа ∝ (1−v)²·²) — «чёткая тень только под ногами».
- ✅ **Вспышки на трибунах: темп считается на ВЕСЬ стадион, а видна четверть (26.07.2026).** При 11 вспышках/сек в кадре хоть одна горела лишь в четверти моментов, и эффекта не было видно вовсе — казалось, что система не работает. Проверять не глазами по скриншоту, а проекцией живых точек в NDC камеры (`v.project(camera)`, доля кадров с попаданием). Рабочий темп получился вдвое выше расчётного.
- ✅ **Много одинаковых пятен — это один InstancedMesh, а не N мешей (26.07.2026).** 88 теней = 1 draw call. Чтобы не переписывать чужой код, менеджер выдаёт «якорь» — пустой `THREE.Object3D`: старый код продолжает двигать `shadow.position.x/z`, а матрицы инстансов собирает менеджер. Замер цены — не «на глаз по FPS» (в превью-вкладке rAF задушен), а прямым прогоном `for (10) renderer.render()` + `gl.finish()`.

- ✅ **Необязательное украшение уронило основное тело (26.07.2026).** `attachHair` читал `this.look.hairColor`, а у арбитров `look` нет вовсе — исключение прерывало `attachModel` на середине: модель уже создана, но капсула-заглушка не спрятана и микшер анимаций не создан. Судья выглядел чёрным овалом, а в консоли врало «модель не загрузилась». Два урока: (1) украшения (причёски, аксессуары) ставить ПОСЛЕ основного тела, чтобы их поломка не забирала анимации с собой; (2) любые данные из JSON читать через `const L = this.look || {}`.
- ✅ **Повтор: писать ПОЗЫ, а не переигрывать физику (26.07.2026).** Кольцевой буфер на 30 снимков/сек × 20 сек для 22 тел — всего ≈390 КБ Float32Array (позиция, поворот, наклон, индекс клипа и время внутри клипа + мяч + владелец). Воспроизведение просто расставляет тела и ставит `action.paused = true; action.time = записанное` + `mixer.update(0)` — тогда замедление остаётся замедлением, а не ускоренной перемоткой ног. Имя клипа хранится ОДНИМ числом: порядок `Object.keys(actions)` одинаков у всех клонов одной модели. Время клипа между кадрами интерполировать нельзя — на стыке клипов оно прыгает с нуля.
- ✅ **Равномерное замедление повтора делает его вдвое длиннее (26.07.2026).** 13 секунд атаки при speed 0.55 — это 24 секунды показа, зритель уснёт. Работает телевизионная схема: комбинация в своей скорости, последние ~2,5 секунды (выход на удар) плавно уходят в слоу-мо. Позицию воспроизведения тогда считать в КАДРАХ записи (`pos += dt * rate * speed`), а не во времени.

- ✅ **Камера повтора должна строиться ВОКРУГ цели, а не вокруг события (26.07.2026).** Первая версия считала позицию и взгляд от точки гола — на угловом объектив утыкался в рекламные щиты, пока мяч летел в штрафную. Лечение: ракурс задаёт ТОЛЬКО геометрию точки съёмки относительно цели (смещение, высота, дистанция), а смотрит камера всегда на саму цель — сглаженную позицию мяча (или автора гола) с упреждением по её скорости. Тогда «камера не туда смотрит» невозможно по построению. Обязательные рамки: минимальная высота, максимальный вынос за пределы поля (иначе камера уезжает в трибуну) и минимальная дистанция до цели (иначе объект влезает в объектив).
- ✅ **«Последнее касание» в момент гола — это НЕ автор гола (26.07.2026).** `lastTouch` обновляется по ближайшему к мячу игроку в радиусе касания, а когда мяч пересекает линию, ближайшим почти всегда оказывается вратарь или защитник. Поэтому автор не находился, титр показывал название команды, а камера празднования уезжала в фолбэк — центр поля, где просто бегут игроки. Лечение: журнал последних касаний (`match.touchLog`, 12 записей) и поиск с конца первого ПОЛЕВОГО игрока забившей команды; фолбэк — ближайший к воротам форвард, а не координата (0,0). Общий урок: у камеры, которая «ведёт героя», фолбэк никогда не должен быть центром сцены — это выглядит как поломка.
- ✅ **Один и тот же ракурс на каждом голе приедается за два гола (26.07.2026).** Дешёвое лекарство: пул ракурсов, тасовка Фишера–Йетса при старте серии и запрет повторять ракурс внутри одной серии. Плюс серия из трёх сегментов (комбинация → завершение → празднование) вместо одного длинного повтора: каждый кусок берёт свой отрезок записи и свой темп.

## UI и тач
- ✅ **Google Fonts с кириллицей — проверять до выбора шрифта (26.07.2026).** Jersey 10, записанный в правилах проекта, кириллицу не поддерживает: русские надписи молча падают на системный fallback. Быстрая проверка — `fetch('https://fonts.googleapis.com/css2?family=<Имя>&display=swap')` и поиск блока `cyrillic` в ответе. Проверено: Oswald, PT Sans Narrow, Rubik Mono One — кириллица есть.
- ✅ **Спаны внутри тач-кнопок ломают детект `e.target` (18.07.2026).** Обработчики жестов проверяют `e.target.classList.contains('tbtn')` — когда в кнопку вложили `<span>` с контекстной подписью (ПАС/КОРПУС), целью события стал спан, и кнопка перестала распознаваться как кнопка. Лечение: `.tbtn span { pointer-events: none; }` — целью события всегда остаётся сама кнопка. Урок: вкладываешь разметку в интерактивный элемент — сразу отключай ей pointer-events (или используй `closest()`).

## Геймплей и AI
- ✅ **Буфер удара, созданный только после отпускания кнопки, опаздывает на спринте (19.07.2026).** У навеса между отпусканием A и готовым событием есть 0,3-секундное окно дополнительных тапов; пас и удар тоже сначала копят шкалу. Если в это время стрелки резко меняют прицел, обычное движение успевает увести футболиста от отдельно катящегося мяча, и поздний `pendingStrike` уже не догоняет его. Лечение: обязательство контакта (`strikeContactLock`) вооружать сразу во время замаха у текущего владельца, хранить через окно тапов и последующий `pendingStrike`, а снимать только после исполнения/отмены. Пока замок жив, не разрешать параллельный спринтерский толчок по направлению прицела. Обязательно тестировать все действия при 30/60/120 FPS и отдельно оборону без владения, чтобы S не превратился в бесплатную автопогоню.
- ✅ **Врывания под навес умирали в момент подачи (18.07.2026).** Рывки в штрафную (`boxRuns`) назначались по условию «мяч во фланговом коридоре» — но как только навес исполнен, мяч ПОКИДАЕТ коридор, условие рушится и тренер снимал рывки на следующем же такте: форварды разворачивались посреди врывания. Лечение: таймер `crossAir` (время полёта подачи из `predictLanding` + запас) — пока подача в воздухе, назначенные рывки неприкосновенны. Урок: условие, зависящее от позиции мяча, нельзя проверять во время события, которое сам мяч и запускает.
- ✅ **За летящим верхом мячом нельзя гнаться «по тени» (18.07.2026).** `pursuitBall` упреждает по горизонтальной скорости мяча — для навеса это уводит игрока ЗА точку приземления (мяч-то остановится там, где упадёт). Всё, что играет на прилёте (замыкающий, защитник на выносе, добегание под удар), должно бежать в `predictLanding(ball, h)`, а не за мячом.
- ✅ **Событие игры по таймеру реального времени рассинхронится с анимацией (фидбек Олега, 22.07.2026).** Аут: мяч вылетал из рук по таймеру `releaseDelay`, посчитанному под длину клипа 2,17 с из комментария — а реальный клип `throw in` длится 1,733 с (бросок рук на ~1,03 с). Мяч покидал руки на ~0,25 с ПОЗЖЕ броскового движения. Лечение: выпуск привязан к КАДРУ клипа (`action.time >= releaseClip`), а не к отдельному таймеру — руки и мяч синхронны при любом темпе; то же для выноса/броска вратаря (`gk_dropkick` удар ногой 1,30 с, `gk_throw` выпуск 1,03 с). Урок: тайминг игрового события сверять с РЕАЛЬНОЙ длительностью клипа.
- ✅ **Мяч, привязанный к анимации (кисти вратаря), пересекает линии БЕЗ движения — арбитраж линий сходит с ума (фидбек Олега 22.07.2026: «вместо гола угловой»).** Позиционная привязка мяча к костям (holdBallInHands) телепортирует его за лицевую при ловле в падении у линии; checkOutOfPlay видит «мяч за линией, гола нет» → угловой из ничего. Лечение: мяч в руках кипера мёртв для арбитража линий (гейт в checkOutOfPlay), гол остаётся только за goal.js. Урок: любой объект, чью позицию ПИШУТ напрямую (анимация, руки, магнит), надо явно исключать из зон-триггеров, которые предполагают ДВИЖЕНИЕ по физике.
- ✅ **Разворот на спринте терял мяч по ТРЁМ причинам сразу — чинить пришлось все (фидбек Олега 22.07.2026).** (1) Толчок дриблинга бил мяч в сторону НОВОГО курса на полной силе — гасить углом разворота (доля силы `sprintTurnPushMin` при 90°+); (2) кап скорости дриблинга действовал по всему гистерезису hasBall (2.4 м) — игрок физически не догонял свой толчок: кап держать только при мяче у ноги (<stickyRadius); (3) сорванная разворотом липучка не переустанавливала погоню — reclaim-правило «эпизод жив + мяч ничей рядом → обязан вернуться к мячу». Урок: жалоба «убегает от мяча» = минимум три независимых механизма, проверять КАЖДЫЙ телеметрией по кадрам, а не чинить первый найденный.
- ✅ **Ловушка-сеттер на ball.vel.x поймала «виновника» лучше любых логов (22.07.2026).** Когда неясно, КТО пишет в скорость мяча: `Object.defineProperty(ball.vel,'x',{set(v){ if (скачок) hits.push(new Error().stack) }})` + патч `Player.prototype.aiKick` с записью team/homeIdx — мгновенно показали, что «загадочный рывок мяча» был честным AI-навесом добежавшего соперника, а не багом. Сначала телеметрия по кадрам, потом ловушка на запись — гадание по логам дороже.
- ✅ **Грязный ввод между синхронными тестами в одной странице = фантомные баги (22.07.2026).** Несъеденный `input.pass._edge` от скипа интро выстреливал пасом на первом кадре следующего теста — «мяч сам улетел». Каждый тест начинать с полного flush ввода (cancel всех ChargeAction + сброс cross/swipe) и помнить: state страницы живёт между вызовами javascript_tool.
- ✅ **Оттолкнутый на спринте мяч числился «свободным» → авто-переключение уводило курсор к партнёру (фидбек Олега 22.07.2026, «убегает при ведении по диагонали»).** Дриблинг толчками выносит мяч за `controlKeepRadius`, `updateToucher` даёт `toucher=null` почти весь спринт, и переключение «мяч свободен → сосед ближе» отдавало курсор партнёру, ближе к летящему мячу; ведущий оставался с мячом, но без курсора — читалось как убегание (диагональ обостряет: мяч уходит вбок). Лечение: пока жив «эпизод владения» и ведущий — ближайший к мячу в пределах `dribbleReclaim` (5.5 м), он сохраняет `toucher` (мяч не свободен). Проверять регрессию: на реально свободный мяч (ownEpisodeT=0 у всех) авто-переключение обязано остаться живым. Урок: «владение» в арбитраже нельзя мерить только мгновенным радиусом — спринт-толчок физически отрывает мяч, но эпизод продолжается.
- ✅ **Контактный ассист замаха нельзя вешать на строгое владение — спринт-ведение толчками его срывает (фидбек Олега 22.07.2026, «убегает от мяча при навесе/беге»).** Дриблинг на спринте пинает мяч вперёд, и в момент замаха навеса/удара мяч часто оттолкнут ЗА зону контроля → `isToucher`/`hasBall`/`controlling` на миг все `false` → `strikeContactLock` не ставится, а `ballApproach` при замахе не переназначается (требует `hasBall`) → боковой стик-прицел уводит игрока от мяча. Лечение: (1) «эпизод владения» `ownEpisodeT` (~1.4 с после последнего касания) — игрок ещё хозяин мяча для замаха, пока мяч рядом/низом/не у соперника; (2) при замахе на бегу переназначать тот же `beginBallApproach('dribble')`, что и в обычном ведении (безлимитная погоня доводит до мяча); (3) не ограничивать погоню замаха узким `strikePursuitRange` — сильный толчок из неё вырывается. Урок: любое «удержание у мяча» должно переживать спринт-толчок, когда мяч физически далеко от ног.
- ✅ **Скриптовый пас в АДРЕСАТА-которого-делаешь-controlled = «адресат убегает» (фидбек Олега 22.07.2026, розыгрыш с центра).** Три сцепленных грабли автопаса: (1) направление считай ИЗ позиции мяча в цель, а не между двумя игроками — иначе мяч летит мимо; (2) адресат должен остаться AI-`receiver` (сам бежит навстречу и принимает), а НЕ стать сразу `controlled` — у управляемого receiver-автоприём выключен, он стоит/уходит; (3) `switchCd` после `setControlled` блокирует авто-переход курсора на принявшего (приём случается раньше снятия cd) — ставь cd=0, а курсор в полёте держи отдельной защитой «пока наш пас летит адресату и мяча никто не коснулся — не переключаемся». Тогда курсор сам перейдёт на принявшего в момент приёма.
- ✅ **Вратарский выброс «рикошетит» в свои ворота от прилипшего соперника (фидбек Олега 22.07.2026).** В движке НЕТ физической коллизии мяч-игрок — «рикошет» это соперник, который ИГРАЕТ выброшенный в упор мяч (удар/голова) в наши же ворота. Он стоял вплотную, потому что прессинг давил вратаря с мячом как обычного владельца. Лечение: (1) соперники не давят вратаря с мячом в руках — расступаются на keepAway и ждут ввода; (2) выброшенному/выбитому мячу — короткая неприкосновенность: соперникам рядом ставим `kickCooldown` на ~0.5 с (глушит и удар, и подбор — мяч успевает уйти); (3) страховка: если соперник на курсе в упор, перекинуть через него (lift). Урок: «мяч отскочил от игрока» в этом движке = игрок его сыграл; чинится глушением игры мяча, а не физикой отскока.
- ✅ **`seek` к статичной точке = вибрация на месте (фидбек Олега 22.07.2026: «адресат паса дёргается»).** `seek` всегда отдаёт полный газ к цели — у самой точки игрок проскакивает её и разворачивается каждые пару кадров. Для любого «встань там и жди» — только `arrive` (радиус торможения), у точки — стоять с `face` на мяч. `seek` оставлять лишь для целей, до которых игрок гарантированно не добежит за жизнь назначения (рывки, погоня).
- ✅ **Один слишком строгий порог может выключить ЦЕЛУЮ подсистему — и это видно только по воронке (24.07.2026).** Жалоба звучала как «AI не подкатывается и не принимает мяч», а корень был совсем в другом месте: пас отвергался, если соперник ближе 2.6 м к линии передачи. В компактном блоке такого коридора не бывает — `choosePass` возвращал null в 80% вызовов, AI отдавал 3 паса за 15 минут и 88% времени просто вёл мяч. Отсюда автоматически следовало и всё остальное: нет пасов → некому принимать → нет приёмов; владелец не расстаётся с мячом → нет «плохих касаний» → окно подката не открывается никогда. Урок: прежде чем чинить симптом, снять ВОРОНКУ условий по кадрам (сколько раз выполнилось каждое) — она показывает единственное узкое место вместо трёх мнимых.
- ✅ **Проходимость паса нельзя мерить плоским коридором — только временем (24.07.2026).** «Соперник ближе N метров к линии» не различает медленный пас на 30 м и прострел на 8 м. Рабочая модель: пока мяч летит до проекции соперника на линию, тот бежит поперёк — но не мгновенно (реакция ~0.35 с) и не всем спринтом (он ещё разворачивается). Запас = поперечная дистанция минус его досягаемость. Под прессингом порог смягчается: в жизни мяч отдают и в плотный коридор, потому что деваться некуда.
- ✅ **Событие, которого система никогда не производит, — мёртвый триггер (24.07.2026).** Подкат AI ждал «плохого касания» владельца, но AI-ведение (`aiDribble`) держит мяч ровно в `dribbleAhead` от ноги: отскока не бывает конструктивно, замер дал 0.1% кадров. Триггеры на «ошибку» надо привязывать к тому, что система реально порождает, либо заменять на расчёт возможности («достанет ли слайд мяч»). Проверять — счётчиком срабатываний, а не рассуждением.
- ✅ **Погоня с упреждением ломает приём верхового мяча на последних метрах (24.07.2026).** Адресат бежал к точке прилёта, но при `bp.y < 1.2` переключался на `pursuitBall`, который целится ВПЕРЁД мяча по его горизонтальной скорости, — и уходил с точки ровно тогда, когда мяч опускался ему на грудь. Правило: пока мяч в воздухе, ноги стоят на пересчитываемой точке прилёта; погоня включается, только когда мяч уже низом.
- ✅ **JSON.stringify по объекту с ссылкой на игрока вешает вкладку намертво (24.07.2026).** В отладочном зонде запись о пасе хранила `pass.mate` — а это Player со всем графом Three.js. Сериализация уходила в бесконечность, и это выглядело как зависший игровой цикл; полчаса ушло на поиски несуществующего бага в игре. В зондах ссылки на объекты сцены удалять ДО сериализации. Отдельно: во вкладке без фокуса `setTimeout` душится до ~1 раза в минуту — длинную симуляцию гонять одним синхронным вызовом, а не через таймер.

- ✅ **«Мяч отскакивает от игрока как от дерева» = касание считается по РАДИУСУ, а не по телу (24.07.2026).** Приём срабатывал по условию «мяч ниже потолка зоны и ближе 1.5 м»: замер показал контакт на высоте 2.33 м (выше головы) в 1.1–1.4 м от груди, со сбросом скорости в фиксированные 3.4 м/с и разворотом направления до 157° — мяч менял курс в пустоте и улетал назад в пасующего. Лечение: точка касания берётся НА КОРПУСЕ (вертикальный отрезок стопа→лоб, сдвинутый чуть вперёд по взгляду), горизонталь и рост проверяются ОТДЕЛЬНО (единый 3D-радиус ловит мяч в 40 см над головой), мяч ставится на точку касания и сходит ПО СВОЕМУ ходу плюс доля бега игрока — разворот назад запрещён конструкцией. Стало: контакт на 0.95–1.76 м в 0.2–0.7 м от игрока, поворот 0°.
- ✅ **Подкат надо наводить в МЯЧ и мерить ногой, а не корпусом (24.07.2026).** Слайд целился в соперника с упреждением: из пяти честных ситуаций две проваливались полностью, одна давала фол на ровном месте, а при попадании нога оказывалась в 0.56–0.67 м от мяча. Плюс `reachOwner` мерил дистанцию БЕЗ учёта сближения — самый естественный подкат (шаг навстречу прущему форварду с 4.5 м при сближении 9 м/с) движок отказывался исполнять. Лечение: (1) прицел — точка, где мяч БУДЕТ к приходу вытянутой ноги (итерация в три шага: время зависит от дистанции, дистанция от времени); (2) контакт — от носка бьющей ноги ИЗ СКЕЛЕТА (в клипе `tackle` метёт ПРАВАЯ, в `kick` бьёт ЛЕВАЯ — проверять по риггу, а не по интуиции); (3) дистанция решения — с учётом сближения; (4) клип стартует на входе в фазу подметания (замерено: подошва низко и вынесена с 0.45 до 0.92 с), а темп подбирается так, чтобы фаза заняла ровно длину слайда. Стало 4 из 5 с мячом, ноль фолов, нога в 0.38–0.51 м.
- ✅ **Баллистика «формула параболы × поправочный коэффициент» мажет на метры (24.07.2026).** Адресный верховой пас считал силу как sqrt(g·d / 2tanθ) × fudge — и клал мяч в 0.8–2.0 м мимо партнёра, потому что квадратичный drag съедает дальность нелинейно, а один множитель под все дистанции не подобрать. Лечение: искать силу бисекцией по ТОЙ ЖЕ мини-симуляции, что и `predictLanding` (26 итераций — доли миллисекунды), и целиться в ВЫСОТУ ПРИЁМА (грудь), а не в газон: иначе мяч честно садится в двух метрах за спиной адресата. Стало 0.17–0.54 м. Сам `predictLanding` при этом точен (0.0–0.56 м) — проверять надо именно решатель силы.
- ✅ **Подставные объекты-пустышки взрываются, когда игрок доигрывает действие в паузу (24.07.2026).** На паузах AI строится к фантомному `_centerBall` (только `mesh.position` и `vel`). Игрок, застигнутый паузой в подкате, честно доводил слайд и вызывал `ball.strike` — «is not a function». Ловится только длинной симуляцией матча, в короткой не воспроизводится. Правило: у любого подставного объекта должны быть ЗАГЛУШКИ всех методов настоящего, иначе он живёт бомбой замедленного действия.

- ✅ **Удар «в кадре контакта» невозможно попасть подбором темпа наугад — тайминг надо ВЫЧИСЛЯТЬ от прогноза встречи (24.07.2026).** Замыкание играло клип фиксированным темпом, а мяч перенаправлялся, когда входил в радиус: у головы клип стоял на 0.10–0.20 с (фаза приседа и отвода головы назад) при реальном кадре удара 0.62 — кивок шёл вслед улетевшему мячу, расхождение до 0.52 с клипа. Рабочая схема: (1) кадр контакта КАЖДОГО клипа замерить по риггу прямой кинематикой (максимальный вынос носка/лба) и положить в конфиг; (2) в момент заказа удара посчитать прогноз встречи мяча с игроком той же физикой, что в `ball.update`; (3) подобрать `timeScale` и стартовый кадр клипа так, чтобы кадр контакта пришёлся ровно на прогноз (мяч близко — срезать начало замаха, далеко — стартовать клип позже); (4) каждый кадр подстраивать темп под уточнённый прогноз (сервопривод), иначе drag и доворот бега уводят момент встречи. Итог: расхождение меньше одного кадра рендера.
- ✅ **Прогноз встречи надо вести до ТОЧКИ УДАРА, а не до центра игрока (24.07.2026).** Бутса в кадре удара вынесена вперёд почти на метр, лоб стоит над корпусом. Пока прогноз мерил дистанцию «мяч ↔ центр», игрок вставал ровно в точку прилёта, мяч приходил ему в живот, и нога била в метре от мяча. Лечение: считать расстояние до реальной точки удара (позиция + вынос по направлению удара, высота по риггу) и ставить ноги на шаг НАЗАД от точки прилёта — тогда мяч сам приходит на бутсу. Зазор упал с 1.2–1.7 м до 0.07–0.37 м.
- ✅ **Порог «головой или ногой» должен идти от РИГГА, а не от здравого смысла (24.07.2026).** Порог 1.15 м выглядел логично, но в клипе `kick` бутса поднимается лишь на ~0.52 м, а лоб в `header` стоит на 1.83 м: мяч на груди (1.2–1.5 м) назначался «головой», и лоб проходил на полметра ВЫШЕ мяча. Порог надо ставить посередине между потолком ноги (с подскоком) и высотой лба, а высоту выпрыга считать по высоте контакта — прыгать ровно настолько, чтобы точка удара пришла на мяч, а не «всегда на полную».
- ✅ **Прицельная стойка не должна применяться к ЛЕТЯЩЕМУ мячу (24.07.2026).** Удержание кнопки удара тормозит игрока (`brake`) — это верно для размеренного удара с газона, но под навесом означало «вкопался и смотрит, как мяч падает в метре рядом». Правило: если мяч в воздухе и идёт на игрока, зажатая кнопка удара = заказ замыкания, и ноги обязаны бежать к точке встречи. Тот же принцип для AI: на замахе он раньше стоял столбом, а замыкание в PES живёт врыванием.
- ✅ **Замах начинать только если прогноз нашёл НАСТОЯЩИЙ контакт; иначе — честный промах (24.07.2026).** Когда AI начал замахиваться с 3 м (вместо 1.5), 5 замахов из 6 кончались «выстрелом из воздуха»: страховочные ветки (мяч прошёл мимо / таймаут) били по мячу в 1.3–3 м от ноги. Лечение: (1) вход в замах требует, чтобы прогноз реально нашёл контакт (не просто «ближайшую точку»); (2) если в кадре удара мяч дальше порога промаха — замах доигрывает вхолостую, мяч летит своим курсом. Стало 9 ударов и 1 промах за 10 минут матча.

- ✅ **Реальные тайминги анимаций надо мерить, а не доверять комментариям (22.07.2026).** Длительность и момент «контакта» клипа glTF читаются прямо из файла без запуска игры: JSON-chunk → длительность анимации = макс. `accessors[sampler.input].max[0]`; момент удара/выпуска = пик угловой скорости ключевой кости (кисть/стопа) по соседним кадрам `sampler.output` (кватернионы). Так замерены throwin 1,733 (бросок 1,03), gk_dropkick 2,433 (удар 1,30), gk_throw 1,6 (выпуск 1,03) — питон-парсер glb в истории сессии 22.07.

## Камера
- ✅ **Наклон камеры вниз обнажил «недостроенный» стадион (грабля от Олега, 17.07.2026).** Как только камера стала доворачиваться к ближней бровке (followZ), в кадр попала область за отбивкой — а там пусто (чёрная стена), и ближняя трибуна показывала тёмную изнанку, потому что камера (z=58) оказалась ПОЗАДИ неё (z=50). Правило: **камера должна стоять ВНУТРИ ближней трибуны, как настоящая телекамера.** Лечение: (1) газон-отбивку сделать большой (F.length+150 × F.width+130), дальний край растворяется в fog — низ кадра всегда трава; (2) трибуны вынести наружу за камеру (dz = F.width/2+apron+20 = 66 > 58) и повыше (17 м) — чаша стадиона. Урок на будущее: меняешь камеру — проверяй, что она видит по краям, там может быть недостроенная геометрия.
- ✅ **Ближний игрок выпадал за нижний край кадра (грабля от Олега, 17.07.2026).** ТВ-камера стояла близко (distance 48) при height 30 — ближняя бровка (z≈+34, к камере) оказывалась почти под камерой, под углом ~64° вниз, за пределами вертикального fov. Лечение: (1) отодвинуть и чуть опустить камеру (distance 48→58, height 30→29, fov 40→42) — площе, как реальная телекамера; (2) **прицел следит по глубине** `look.z = ball.z * followZ` (0.7): когда мяч у ближней бровки, камера доворачивается вниз-к-нему и поднимает игрока с нижнего края (NDC −0.56 вместо −1.1). Подбор — по NDC ног/головы игрока в крайних точках (z=±34, ±37). Всё в `config.js` → `camera`.

## Браузер и кэш
- **Негативный кэш 404 (грабля из «Новоселья»).** Если код запрашивал файл, которого ещё не было, браузер запоминает 404. После добавления файла — жёсткое обновление (Ctrl+Shift+R; на планшете — очистить кэш/потянуть страницу), иначе файл «не появится».
- ✅ **`python3 -m http.server` кэширует насмерть (проверено 17.07.2026).** Отдаёт 304, браузер держит старые модули — после правки config.js страница гоняла старую версию. Лечение: свой `serve.py` с заголовком `Cache-Control: no-store` — запускать локалку только через него.
- ✅ **Занятый порт может незаметно показывать игру из другой worktree (18.07.2026).** `serve.py` не запустился на 8123, но ссылка открывалась — там остался сервер другой ветки, поэтому свежий HTML «не менялся» при любых перезагрузках. Перед визуальной проверкой при занятом порте смотреть рабочую папку процесса (`lsof -a -p <PID> -d cwd`); свою ветку запускать на свободном порте, а не тестировать чужую сборку.
- ✅ **Отладка через браузер-панель Claude: JS-инструмент живёт в «изолированном мире».** `window.*` страницы оттуда не видно (window.DBG «нет», хотя он есть), а dynamic import не видит importmap — можно час ловить фантомные ошибки. Надёжный способ достучаться до мира страницы: вставить в DOM настоящий `<script>` элемент, который пишет результат в видимый элемент (#hint).

## Blender и Mixamo (сессия модели игрока, 17.07.2026)
- ✅ **Авто-риггер Mixamo падает с «Unknown error while generating motion», если в модели есть висящие в воздухе детали.** У нас кисти рук оказались в 5 мм от запястий — глазу не видно, риггеру смертельно. Формат (FBX/OBJ) и текстуры ни при чём. Лечение: **«сварить» модель в один цельный меш** (boolean union деталей по одной, base = самый большой кусок; выбрать Skeleton LOD **No Fingers** для варежек; текстуры из FBX убрать — path_mode='STRIP'). Проверка перед отправкой: посчитать связные куски меша — должен быть ровно 1.
- ✅ **Маркер паха (groin) в Mixamo обязан попадать в геометрию.** Use Symmetry прибивает его к осевой линии — если между бёдрами щель, центр маркера в пустоте и «Please place all markers on the character», сколько ни двигай. Лечение: сплошная перемычка-«ткань» в шагу (box между бёдрами).
- ✅ **Авто-риггеру нужна асимметрия перед/зад.** Симметричная болванка без лица = «не знаю, где перёд» (первый «выдало ошибку»). Нос-клин решает, заодно в игре видно, куда смотрит игрок.
- ✅ **Blender 5.1: у Action больше нет `.fcurves`** — новая слоёная система анимаций. Кривые теперь: `act.layers[0].strips[0].channelbag(act.slots[0]).fcurves`. Старые сниппеты из интернета молча падают с AttributeError.
- ✅ **Root motion анимаций Mixamo: оси location таза** — index 2 = вперёд-назад, index 0 = вбок, index 1 = вертикаль. «Бег на месте» = вычесть линейный тренд из горизонтальных осей (0 и 2), НЕ трогая вертикаль (иначе падения зависнут в воздухе). Порог дрейфа 0.3 м отделяет локомоцию от стоек.
- ✅ **`bpy.ops.mesh.intersect_boolean` на всём выделении «съедает» модель** (union требует пары выделено/невыделено). Правильно: разделить на куски (separate LOOSE) и приваривать Boolean-модификатором по одному к самому большому.

## Анимация: локомоция и удары (сессия 33, 26.07.2026)
- ✅ **«Роботы» — это НЕ стиль клипа, а несовпадение темпа ног со скоростью газона.** Один клип трусцы растягивался на все скорости (`timeScale = speed/4.0`, зажим 0.6…1.9). Замер по риггу: настоящая скорость клипа **4.57 м/с**, а не 4.0; нижний зажим 0.6 достигается уже на 2.4 м/с, и вся полоса 0.6–2.4 м/с играется ОДНИМ темпом. Метрика проскальзывания (мировая скорость стопы в самом глубоком кадре контакта, делённая на скорость игрока): на 1–2 м/с было **10.25**, стало **2.3–2.8**. Урок: прежде чем менять клипы, померьте, с какой скоростью клип «едет» — это скорость ОПОРНОЙ стопы, и её надо совместить со скоростью тела.
- ✅ **Один делитель на четыре клипа = разная поломка в каждую сторону.** Замер: run 4.57, run_back 4.23, strafe_l 5.34, strafe_r 3.99 м/с. С общим делителем шаг влево скользил на треть сильнее шага вправо — глаз ловит такую асимметрию мгновенно, хотя «в среднем» всё выглядит настроенным.
- ✅ **Масштабирование размаха анимации ОТРЫВАЕТ фигуру от земли.** Если умножить повороты костей ног вокруг позы покоя (чтобы получить из бега ходьбу и спринт), меняется и вертикальный вынос ноги от таза: ходьба с `amp 0.66` утонула в газон на 2 см, спринт с `amp 1.55` завис над ним на 4 см. Опорной фазы не остаётся, и любая привязка темпа к длине шага теряет смысл. Лечение: после сборки клипа замерить нижнюю точку стопы и сдвинуть дорожку таза до исходной высоты. Ось «вверх» у дорожки таза Mixamo не угадывать (оси костей повёрнуты) — определять пробным сдвигом каждой компоненты.
- ✅ **Порог «стопа на земле» обязан быть свой у каждого клипа.** Фиксированный порог (`y < 0.09`) на производных клипах ловил не опору, а быстрый мах — медиана «скорости опоры» завышалась, темп получался заниженным. Правильно: полоса над САМОЙ НИЖНЕЙ точкой этого клипа.
- ✅ **Смешивать две походки можно только с общей нормированной фазой.** Клипы разной длины при одинаковом `timeScale` расходятся, и вместо ног получается каша. Решение: одна фаза 0..1 на игрока, у каждой ступени `timeScale = 0`, а `action.time = фаза × длительность_ступени`. Три.js честно эвалуирует клип при нулевом темпе (`_updateTime(0)` возвращает выставленное время) — но `action.isRunning()` при этом ВРЁТ (false), для проверок использовать `mixer._isActiveAction(a)`.
- ✅ **Темп смеси считать по ФАКТИЧЕСКИМ весам, а не по целевым.** Веса догоняют цель ~0.07 с, и при торможении с бега в позе ещё висят спринт и бег (замер: 15 % веса в полосе 1–2 м/с). Если считать длину шага по цели, ноги едут быстрее, чем рисует смесь. Стойка входит в среднее с нулём законно: подмешанная неподвижная поза укорачивает видимый шаг, и клип обязан крутиться быстрее.
- ✅ **Направление локомоции нужно с гистерезисом И с порогом в м/с.** Около нуля знак боковой скорости дребезжит, и в позе одновременно висели приставной шаг влево и вправо. Кроме порога по ДОЛЕ боковой скорости нужен абсолютный порог (у нас 1.1 м/с) — иначе шум решает, какой ногой шагать.
- ✅ **Медленное движение вбок/спиной требует СВОИХ медленных ступеней.** Без них игрок на 1 м/с был на 94 % стойкой и просто ехал по газону стоя — «полупарализованный» в чистом виде. Одной лестницы «вперёд» мало: у бега спиной и приставных шагов должны быть свои медленные варианты.
- ✅ **Цикл в glb может быть не замкнут.** Замер по glb: у `strafe_l` последний кадр расходится с первым на **18.3°**, у `strafe_r` — на **20.5°** (у `run` — 0.0°). Каждые 0.4 с фигура дёргалась. Лечение: размазать разрыв линейно нарастающей поправкой по всей длине клипа, а не резать в одном кадре.
- ✅ **Клип падения `fallen` не был в наборе ONE_SHOT — и это блокировало анимацию НАВСЕГДА.** Он оставался `LoopRepeat`, событие `finished` не приходило никогда, поэтому `this.oneShot` у сбитого игрока не сбрасывался до следующего удара, а выбор шагового клипа всё это время был заблокирован. Игрок бегал по полю в позе падения. Урок: любой клип, запускаемый через `playOneShot` без `endAt`, обязан быть в ONE_SHOT.
- ✅ **Имя клипа в паке ничего не говорит о том, какой ногой он бьёт.** Замер по риггу (размах носка за клип): `kick` — левая (правая неподвижна, размах 0.002 м), `kick_run` и `penalty` — правая. Игра при этом уже вычисляла бьющую ногу физически (`kickFoot`), но анимация об этом не знала и всегда играла левоногий `kick`; два правоногих клипа лежали в модели и не проигрывались ни разу, а AI-канал (`aiKick`) вообще не считал ногу.
- ✅ **Кадр контакта ищется по ПИКУ скорости носка, а не по имени файла.** Замер: `kick` 0.131 с (пик 11.1 м/с), `kick_run` 0.262 с (27.6 м/с), `penalty` 0.467 с (19 м/с). Стартовать клип ровно с кадра контакта (как было) — значит показать только проводку: удар читается как судорога. Ставить старт на 2–3 кадра раньше.
- ✅ **Замер поз в ручном шаге без рендера врёт: мировые матрицы протухают.** `model.updateMatrixWorld(true)` НЕ пересчитывает родителя, а в headless-цикле (`match.update` без `renderer.render`) матрица группы игрока остаётся с прошлого кадра — стопа «отставала» от группы, и получалось фантомное проскальзывание в метр. Обновлять надо от ГРУППЫ: `p.group.updateMatrixWorld(true)`.
- ✅ **`Array.prototype.map` передаёт в колбэк ИНДЕКС вторым аргументом.** `['a','b'].map(hit)` вызывает `hit('a', 0)` — и необязательный параметр «сколько кадров сэмплить» молча становится нулём. Классика, но в отладочных однострочниках ловится долго: писать `.map(c => hit(c))`.

- ✅ **Импорт FBX переключает fps сцены — и молча ускоряет ВСЕ уже импортированные клипы (26.07.2026).** glTF-импорт ставит сцене 24 fps, Mixamo-FBX — 30. Номера кадров у ранее импортированных действий при этом не двигаются, поэтому 22 старых клипа стали короче на четверть, а я это заметил только по длительностям в готовом glb. Лечение: собирать в тайм-коде 30 fps и переводить кадры импортированного glb (×1.25). Бонус: в 30 fps все номера кадров становятся ЦЕЛЫМИ (12.8 → 16, 43.2 → 54), а экспортёр сэмплирует по целым кадрам и иначе срезает у каждого клипа хвост до кадра — как раз там, где живут вымеренные константы.
- ✅ **Проверка пересборки glb: длительности + контрольная кривая.** После экспорта сравнить длительности всех старых клипов со старым файлом (у нас совпали 20 из 22, у `idle` и `tackle` разница ровно в кадр) и покадрово сверить кватернионы одной кости в одном клипе — у нас максимальное расхождение вышло 0.00000, то есть анимация прошла round-trip без потерь. Без этой проверки «вроде работает» ничего не значит: поломка проявится через неделю на вымеренном кадре удара.
- ✅ **`export_optimize_animation_size=False` раздувает файл втрое.** С выключенной оптимизацией экспортёр пишет ключ на каждый кадр для каждой кости: 25 клипов дали 67 725 ключей и 1.39 МБ против 21 755 ключей и 894 КБ с включённой. На картинку это не влияет.

## Git и раздача
- **GitHub Pages не отдаёт файлы из Git LFS.** LFS в этом проекте не использовать; большие ассеты просто жать (наш бюджет и так маленький — примитивы + mp3).
- **Пути только относительные** (`./textures/...`), потому что на Pages сайт живёт в подпапке `/имя-репо/` — абсолютные пути `/textures/...` сломаются.

## Атмосфера и ассеты
- **FX-библиотека Олега** — `~/Documents/Рабочее/Ютуб/Клипы /Пыль на кассете/FX-Library/overlays/video/Loop (оверлеи)/` (47 луп-оверлеев). Для ТВ-пресетов пригодятся: `VHS-01/02/03`, `crt-scanlines`, `film-grain`, `vhs-head-switch`, `camcorder-datecode`, `tape-unspool`. Это 4K mp4 — перед использованием в игре пережать в ~640×360 low-bitrate и подмешивать `<video>` поверх канваса. Пока CRT чисто шейдерный (легче для планшета).
- **PS1-приём: свет «запечён».** Газон и мяч — MeshBasicMaterial (без расчёта света): ярко, дёшево и аутентично. Лампы/Lambert — только для объёмных объектов (ворота, трибуны).
- ✅ **Ворота «делает» сетка, а не штанги (17.07.2026).** Голый каркас из тонких цилиндров с ТВ-дистанции почти невидим и кажется маленьким, хотя размеры точные (7.32×2.44). Лечение: 4 полупрозрачные панели (зад, крыша, боковины) с канвас-решёткой (ячейка 0.3 м, opacity 0.45) — белое пятно сетки читается с любого расстояния, как в трансляции.

## Геймплей
- ✅ **Физически заметная деформация может выглядеть статичной с рабочей ТВ-камеры (17.07.2026).** Честные 0,3–0,7 м продавливания превращаются на экране в один-два пикселя, особенно у дальних ворот. Поэтому проверка только чисел и крупного плана недостаточна. Решение: разделить физическую глубину контакта и визуальный ход узлов; локально усиливать экранный «мешок», не меняя торможение и траекторию мяча, и обязательно сравнивать кадры с настоящей игровой камеры.
- ✅ **Большой импульс узлам ещё не делает сетку визуально «живой».** Если мяч сначала отражается от неподвижной плоскости, а сетка начинает колебаться после него, глаз всё равно читает её как стену с декоративной дрожью. Решение: мягкий контакт во времени — разрешить мячу войти в полотно, связать глубину ближайших узлов с реальным проникновением сферы, тормозить мяч пружиной и демпфером маленькими подшагами 1/120 с. Тогда тянется именно место попадания, а волна начинается после выхода мяча.
- ✅ **Линейная реакция сетки визуально уравнивает слабые и сильные удары.** На ТВ-камере разница импульсов плохо читается. Решение: степенная кривая `impulse ∝ speed^1.45`, увеличенный запас растяжения и более слабое затухание. Слабый мяч только качает ячейки, мощный создаёт глубокий мешок и долгую волну; экстремальную скорость ограничивать отдельно, чтобы пружины не взрывались в отладке.
- ✅ **Гол нельзя определять по конечной позиции мяча (17.07.2026).** Проверка «центр мяча уже за линией и по ширине в створе» засчитывает мяч, попавший за ворота с внешней стороны, и ошибается у стоек/перекладины. Правильно: хранить отрезок движения внутри кадра, считать только направленный переход из поля внутрь и ждать полного пересечения сферы (`линия + радиус`). В точке перехода вся сфера должна помещаться в чистый проём.
- ✅ **Быстрый мяч туннелирует сквозь тонкую штангу при дискретной проверке.** За кадр 1/30 с удар 30 м/с проходит 1 метр, а суммарный радиус мяча и стойки — 0,28 м. Проверять пересечение после перемещения поздно. Решение: непрерывный sweep — решить квадратное уравнение для движущегося центра относительно оси цилиндра, переместить мяч ровно в момент контакта, отразить нормальную скорость и доиграть остаток кадра. Стресс-тест 90 м/с проходит.
- ✅ **7,32 × 2,44 м — это чистый проём, не расстояние между осями труб.** Центры штанг стоят на `width/2 + postRadius`, ось перекладины — на `height + postRadius`; иначе визуально и физически ворота получаются меньше стандарта.
- ✅ **Мгновенный сброс после гола убивает физику сетки.** Даже правильная сетка не успевает принять импульс, если `ball.reset()` вызывается в том же кадре. Оставлять мяч в воротах примерно 1,35 с, блокировать повторный гол флагом пересечения, затем сбрасывать мяч, игрока и деформацию сетки.
- ✅ **«Floaty»-мяч делает неправильное сопротивление воздуха (17.07.2026).** Долевое трение «за кадр» (`vel *= 0.998`) одинаково тормозит быстрый и медленный мяч — это буквально баг старых FIFA. Правильно: квадратичный drag `a = −k·|v|·v` (у нас `dragK: 0.013`) — прострел на 30 м/с честно теряет треть скорости, а катящийся пас почти не тормозится воздухом. Это и есть «прострельность» PES.
- ✅ **Гравитация и характер дуг связаны.** С аркадной g=−22 «свеча» (lift 5–9) поднималась лишь на 1–1.8 м — на деле была прострелом. Смягчили до g=−14 и дуги задаём углом вылета: `lift = power·tan(angle)` (свеча 30°, настил 16°, прострел 6°) — как в ресёрче PES.
- ✅ **Тапы одной кнопкой = стейт-машина с окном «замаха ноги».** Три типа навеса (A×1/×2/×3, как круг в PES) нельзя считать «нажатиями во время удержания» — клавишу нельзя нажать, не отпустив. Решение: полоска (удержание) → окно 0.3 с после отпускания, где доп. тапы повышают тип → исполнение. Заодно даёт PES-овскую задержку замаха.
- ✅ **Прицельный удар — это баллистика, а не «сила+подъём» (17.07.2026).** Чтобы мяч прилетал в выбранную точку створа, вертикальную скорость считаем из уравнения полёта: `vy = (h−y0)/t − 0.5·g·t`, где `t = dist/(power·0.93)` — поправка 0.93 компенсирует сопротивление воздуха, без неё сильные удары не добирают высоту (цель 2.0 м → факт 0.6 м).
- ✅ **Конфликт «стрелки = бег или прицел?»** Во время замаха удара стрелка уводила игрока в разворот, и он бил мимо. Решение из PES: пока держится кнопка удара, бег стрелками отключён (игрок тормозит, взгляд заморожен) — стрелки только целят. Плюс прицел запоминается на время замаха: отпустил стрелку чуть раньше кнопки — удар всё равно в угол.
- ✅ **Навес «по направлению бега» ломает фланговую игру (17.07.2026).** Если навес летит по facing, бегущий по бровке подаёт в аут, а для подачи в штрафную надо остановиться и развернуться — темп атаки умирает (путь FIFA). Решение из PES 5/6: из флангового коридора чужой половины навес НАВОДИТСЯ в штрафную сам, независимо от бега; полоска силы выбирает адрес (ближняя → центр → дальняя, передержка — за дальнюю), скорость мяча считает баллистика под адрес (в PES полоска — зона, а не м/с), стрелки в момент исполнения лишь уточняют точку. Бег не прерывается вообще.
- ✅ **Два зеркальных бага знаков компенсировали друг друга (17.07.2026).** `kickFoot` возвращал 'L' при мяче СПРАВА от корпуса, а комментарий к curl уверял «>0 = влево», хотя по формуле Магнуса >0 уводит ВПРАВО от направления. Ошибки взаимно гасились: выбор «щёчка/шведка» и наказания работали верно, и баг жил незамеченным — пока честная дуга навеса «от ноги» не потребовала правильных знаков поодиночке. Урок: знаковые конвенции проверять формулой на конкретном векторе (v=(1,0): dvz = +vx·spin → spin>0 крутит вправо), а не верить комментариям; парная ошибка в тестах поведения невидима.
- ✅ **Мягкий контакт с одной панелью сетки «выключал» остальные (18.07.2026).** Гол с острого угла: мяч входил в мягкий контакт с задней сеткой, скользил вдоль неё вбок и проходил сквозь боковую панель насквозь — пока контакт жив, столкновения с другими панелями не проверялись, а после его смерти `findNetHit` пропускал плоскость, которую центр мяча уже пересёк («мяч удаляется» = «не трогаем»). Лечение тройное: (1) в подшагах контакта пружинят ВСЕ продавленные панели (угловой «карман»); (2) при смерти контакта он передаётся самой продавленной соседней панели, а не обрывается; (3) findNetHit ловит «побег» — мяч, уходящий наружу, пока полотно ещё касается сферы. Урок: у составной преграды (короб из панелей) швы и скольжение вдоль граней — главный источник туннелей; тестировать не перпендикулярные удары, а именно косые и угловые.
- ✅ **Помощь в прицеле не должна стягивать все спасённые удары в одну точку.** Формула «промах ≤ прощения → прицел на край створа» давала шесть одинаковых мячей впритирку к штанге — читается как робот. Лечение: остаток прощения конвертировать в глубину (`tuck`): чем меньше был промах, тем дальше от штанги ложится мяч — вариативность возвращается, а помощь остаётся незаметной.
- ✅ **Упреждение дуги — не чистая теория хорды.** Целиться против сноса Магнуса «на половину полного поворота» (0.5·ω·t) — верно лишь для незатухающей крутки; у нас spinDecay съедает ~40% за полёт, и фактический снос меньше теоретического. Коэффициент упреждения (`cross.curlComp: 0.33`) подбирается симуляцией: прогнать `ball.update()` синхронным циклом, замерить точку приземления, поделить фактический снос на теоретический.
- **Разгромные счета в аркадах** берутся из трёх вещей: удары всегда точные, вратарь пассивный, оборона не сжимает пространство. Лечится параметрами в config.js: точность удара, вероятностная модель гола, компактность обороны, перехват пасов. Баланс проверять автосимуляцией 100 матчей, не «на глаз».
- ✅ **Пасующий сам «снимает» назначение на приём паса (18.07.2026).** Логика «приём завершён, когда мяч коснулся своего» ломалась в момент самого паса: первые пару кадров после удара мяч ещё в радиусе контроля пасующего, он числится «ближайшим» (toucher), и назначение адресата снималось мгновенно — принимающий даже не начинал бежать навстречу. Лечение: приём считать состоявшимся только когда мяча коснулся ИМЕННО адресат (`toucher === receiver`), а не «кто-то из своих». Урок: у событий «после удара» всегда есть хвост в 2–3 кадра, где бьющий формально владеет мячом.
- ✅ **Мягкий blend к мячу не гарантирует касание, а смена курсора обрывает AI-погоню (18.07.2026).** При спринтерском толчке боковой ввод начинал действовать ещё до ноги; после switch выбранный исключался из AI `chaser` и сразу становился полностью ручным. Лечение единое: короткий latch `ballApproach` переносит существующий `pursuitBall` в канал человека, буферизует будущий курс и снимается только после физической проверки контакта ПОСЛЕ движения либо при проигранной гонке. Не увеличивать `controlKeepRadius`: это приклеивает мяч. Регрессию гонять в полном `Match`, потому что `updateToucher()` до движения запаздывает на кадр, и изолированный тест Player даёт ложную уверенность.
- ✅ **Невооружённый contact-latch может запереть руль, хотя мяч уже у бутсы (18.07.2026).** После слабого спринтерского толчка `ballApproach` ждал обязательного удаления до `departRadius`, но мяч при сходных скоростях или рикошете мог никогда туда не выйти. Формальное владение уже было, а повторный контакт оставался запрещён до TTL. Лечение: дать слабому толчку короткое окно отделиться; физический контакт различать по радиальной скорости, а расширенную зону устойчивого контроля — по непрерывному реальному времени `isToucher` и малой полной 3D-скорости относительно игрока. Считать кадры нельзя: 2 кадра = 67 мс при 30 FPS и 17 мс при 120 FPS; игнорировать `vel.y` тоже нельзя — низкий отскок ошибочно покажется спокойным. Пока latch активен, не запускать параллельно sticky-дриблинг; спринтерское касание проверять по полной дистанции, не только по проекции `ahead`.
- ✅ **Невычищенный root motion клипа портит дальность, тень/маркер И физику разом (21.07.2026).** Новый клип подката `soccer tackle (2)` добавили в glb сырым — его горизонтальный root motion (кость Hips уезжает вперёд на ~3.75 м в анимации) остался. Симптомы выглядели как три разных бага: (1) слайд «уезжает слишком далеко» (root motion модели + движение группы физикой складываются, ~7 м визуально); (2) тень и звезда-маркер «отстают и телепортируются» (они привязаны к позиции ГРУППЫ, а видимая модель уехала на 3.75 м, в конце клипа root motion сбросился — модель прыгнула назад); (3) выбивание мяча рассинхронено (считается от группы, отстающей от модели). Первопричина одна: у остальных клипов root motion вычищен при первой сборке glb, а у дозагруженного — нет. Лечение: пересобрать glb, занулив forward-ось Hips location (в этом риге — `array_index==2`, размах ~462 в единицах Mixamo) до значения первого кадра — клип становится in-place, как `run`/`idle`. Урок: любой сырой Mixamo-клип несёт root motion; при добавлении в общий glb его обязательно чистить, иначе всё, что привязано к позиции группы (тень, курсор, физические радиусы), рассинхронится с видимой моделью.
- ✅ **Клип с театральным падением не спасти таймингом — нужен другой клип (21.07.2026).** `soccer tackle.fbx` из Mixamo — это подкат, где игрок падает НА СПИНУ ногами вверх и катится; никаким окном/стартом фазы из него не сделать «скользит ногой вперёд». Диагностика — headless Blender: наш риг + все FBX из `models/mixamo/`, покадровый рендер каждого клипа с камерой, следящей за Hips (root motion уносит фигуру из кадра, поэтому статичная камера показывает пустоту). Нашёлся `soccer tackle (2).fbx` — настоящий slide. Урок: три «soccer tackle» в паке — разные движения; проверять надо все варианты глазами, а не брать первый по имени.
- ✅ **Blender 5.1 slotted actions: без назначения action_slot поза не эвалуируется (21.07.2026).** `arm.animation_data.action = act` в 5.1 недостаточно — нужно ещё `arm.animation_data.action_slot = act.slots[0]`, иначе риг остаётся в bind pose (все кадры одинаковы). Проявилось дважды: при рендере диагностики (все клипы «застыли») и потенциально при экспорте. Также `Action.fcurves` в 5.1 убран — fcurves лежат в `action.layers[].strips[].channelbag(slot).fcurves`. При re-export glb через NLA-треки клипы слегка меняют длину (frame_range интерпретируется иначе) — не баг, но `clipStart`/`clipRate` надо перемерять под новый клип.
- ✅ **Длинный анимационный клип поверх мгновенного игрового события рассинхронит мяч и жест (21.07.2026).** Вбрасывание: код выпускал мяч по фиксированной задержке 0.27 с, а бросок в клипе `throw in` (рука из-над-головы вперёд) — на ~1.3 с; мяч улетал в фазе замаха, потом игрок «кидал невидимый мяч», а хвост клипа с шагами читался как «выбегает на поле». Лечение: (1) найти точный момент выпуска по кости (кисть идёт вниз-вперёд — просканировать worldPos кисти по клипу), (2) стартовать клип с фазы замаха и синхронизировать выпуск мяча с этим моментом (`releaseDelay`), (3) после короткого follow-through ПОГАСИТЬ клип (`fadeOut` + `oneShot=null`), не давая хвосту играть. Общий принцип: у «клип длиннее действия» всегда три параметра — старт фазы, момент триггера события, обрезка хвоста.
- ✅ **Длинный клип, стартованный с нуля, показывает не ту фазу — и «диагноз по началу» врёт (21.07.2026).** Подкат: клип Mixamo `tackle` длится 2.77 с (разбег 0–0.8 → скольжение 0.85–1.6 → вставание 1.7–2.77), а геймплейный слайд — 0.55 с. Клип, запущенный с t=0.1, за слайд показывал только стоячий разбег, и я ошибочно счёл его «стоячим выпадом», после чего наложил поверх программный наклон корпуса назад — получилось «игрока сбили, он упал на спину» (фидбек Олега). Лечение: стартовать клип сразу с фазы скольжения (`clipStart` 0.82 с), подобрать `clipRate` так, чтобы слайд + вставание уложились в остаток клипа, и убрать ВЕСЬ ручной наклон — полноценный клип сам рисует силуэт. Урок: прежде чем «дорисовывать» анимацию кодом, посмотри весь клип по кадрам (в живой сцене с увеличенной моделью: заглушить `p.update`, вручную гонять `action.time`) — возможно, нужная поза уже в нём, просто в другой фазе. Программная добавка поверх хорошего клипа обычно его ломает.
- ✅ **Замороженная для теста жертва не проигрывает свою анимацию падения (21.07.2026).** Оверрайд `p.aiUpdate = () => {}` (чтобы цель контакта не «уезжала») заодно отключает `_updateAnim` — поэтому в тесте `victimTilt` наклона падения оставался 0, хотя `downT` честно выставлен. Это артефакт стенда, не баг: в реальном матче `fieldplayer` вызывает `aiUpdate` → `_updateAnim` → наклон применяется. Проверять визуал падения жертвы надо на живом (не замороженном) игроке или отдельным замером `downT`/`downTiltAmp`, а не `rotation.x`.
- ✅ **Два порога контакта пересекаются в один кадр — и дискретность решает не в ту сторону (21.07.2026).** Подкат: зона тела жертвы (0.85 м) и зона ноги до мяча (1.35 м) при мяче У НОГ владельца пересекаются в одном кадре; на 60 FPS проверка тела успевала первой, и честный подкат сбоку-в-мяч считался грубым сносом. Лечение: в кадре контакта тела нога получает приоритет — если мяч в досягаемости слайда (×1.15 запас на кадр) и НЕ экранирован корпусом (не сзади), сначала исполняется выбивание, потом оценка чисто/грубо. Урок: у пары «мягкая зона + жёсткая зона» всегда прописывай разрешение конфликта в одном кадре явно, не полагайся на порядок проверок между кадрами.
- ✅ **AI-жертвы «уезжают» из-под синтетических тестов (21.07.2026).** Сценарии контактов (подкат, навал) невозможно проверить на живых AI-игроках: кипер рвёт на фланг за мячом, полевой бежит домой, владелец пасует — цель покидает точку за доли секунды, и тест ложно проваливается. Приём: замораживать жертву на время теста инстанс-оверрайдами `p.aiUpdate = () => {}; p.aiKick = () => {}; p.aiDribble = () => {}; p.ai = { decideCd: 1e9 }` (и снимать `delete p.…` после). Заморозка только ног недостаточна — «мозг» withBall продолжает пасовать.
- ✅ **crossSolution выводит сторону атаки из взгляда — с угловой точки это ловушка (21.07.2026).** Решатель навеса берёт атакуемые ворота из `facing.x`, а исполнитель углового смотрит «в поле»: x-компонента взгляда направлена ОТ ворот, и навод целился бы в противоположную штрафную (в реальности тихо срабатывал fallback-заброс вместо подачи). Лечение: перед `doCross` корпус ставится строго поперёк поля (`f.x ≈ 0`) — включается запасная ветка `Math.sign(pos.x)`, и сторона атаки берётся из позиции. Урок: у «контекстных» решателей проверяй, из чего они выводят контекст, прежде чем звать их из нового места.
- ✅ **Исполнитель стандарта через полполя убивает темп — лечится «монтажной склейкой» ТВ (21.07.2026).** Ближний к точке аута/углового игрок может оказаться в 60 м (вингер после своей атаки). Ждать пробежку 7+ секунд нельзя, а исполнять по таймауту «с чужого места» ещё хуже: мяч летит от точки, игрок бьёт воздух вдалеке. Решение в духе трансляций 90-х: после свистка и паузы установки дальний (>14 м) исполнитель просто «оказывается в кадре» у точки — камера всё равно панорамирует туда, склейка невидима. Пешком доходят только ближние.
- ✅ **THREE.Color конвертирует hex в linear — на canvas цвет выходит темнее (18.07.2026).** Перекраска атласа формы: взял `new THREE.Color('#3a62d8').r*255` для записи пикселей в canvas — цвета получаются заметно темнее задуманных, потому что с включённым ColorManagement (по умолчанию в r152+) компоненты хранятся в linear, а canvas ждёт sRGB. Лечение: для пиксельной работы парсить hex напрямую (`parseInt(hex, 16)` → байты R/G/B). И не забыть у CanvasTexture: `flipY = false` (glTF-развёртка), `colorSpace = SRGBColorSpace`.
- ✅ **«Сломанная анимация приёма» может вообще не быть анимацией приёма (23.07.2026).** Жалоба «игрок упрыгивает от мяча/тени/маркера при приёме навеса» выглядела как битый клип `receive`, но замер по костям показал: клип чистый (root motion ~1 см, бёдра ровно). Настоящих причин было две, обе игровые: (1) клип длится 3 с и на всё время блокировал локомоцию — принявший «залипал» в позе и скользил по газону; (2) адресат передачи на мяче выше 1,75 м играл ветку «скидка в прыжке» (`jumpT`) — меш взлетал, а тень и звезда оставались на газоне. Лечение: у приёма вообще нет клипа (игрок остаётся в обычной локомоции, мяч гасится под ноги), а адресат принимает на ЛЮБОЙ досягаемой высоте вместо скидки. Урок: прежде чем винить анимацию — замерить root motion по костям и проверить, какая ИГРОВАЯ ветка исполнилась в кадре жалобы.
- ✅ **Короткой перекидке нужны крутая дуга и «пол» силы ниже минимума типа (23.07.2026).** Свеча 30° при минимальной силе типа (13 м/с) летит ~12 м — «перекинуть соперника и отдать ближнему в 8 м» физически невозможно, мяч всегда перелетал. Плюс надбавка на drag (`fudge 1.12`) на медленной крутой дуге не нужна и давала чистый +25% перелёта, а упреждение на бег адресата растягивало короткий пас в заброс на ход. Лечение — всё масштабируется степенью «короткости» (chipT): угол 50°→угол типа, fudge 1→1.12, упреждение 0.3×→1×, пол силы 6.5 вместо powerMin. Точку приёма после удара пересчитывать честным predictLanding — кламп силы смещает реальное приземление от идеальной параболы.
- ✅ **Удар и его анимация должны стартовать с РАЗНЫХ моментов клипа (23.07.2026).** Мгновенное замыкание (голова/с лёта) применяло скорость мяча и запускало клип в один кадр, а клип стартовал с кадра контакта (animAt) — визуально мяч улетал раньше «замаха», рассинхрон. Правильно, как у вратарского выноса/вбрасывания: клип играть С НАЧАЛА (замах), а мяч отпускать, когда клип дойдёт до кадра контакта (`windup = animAt/animTs`, ~0.08 с). Мяч на это время замереть у точки контакта. Обрабатывать в оркестраторе (Match) ДО движения игроков, чтобы работало и для человека, и для AI, и при смене управляемого. Прыжок под удар головой запускать с замаха — игрок встречает мяч в высшей точке.
- ✅ **Порог «принять верховой мяч» по горизонтальной скорости душит крутые перекидки (23.07.2026).** Свеча/перекидка падает почти вертикально: горизонтальная скорость < порога, и приём/замыкание не срабатывали — мяч «отскакивал» мимо ног. Считать порог по ПОЛНОЙ скорости (`vel.length()`), тогда быстрое падение проходит гейт. И у человека (trap), и у AI (aerial-гейт в fieldplayer).
- ✅ **Авто-переключение «по касанию» опаздывает на быстрых передачах (23.07.2026).** Курсор переходил на приёмщика только когда тот СТАЛ владельцем — на перекидке мяч успевал «отскочить» до смены. Решение как у навеса в штрафную: адресный верховой пас (loftedPass) сразу переключает управление на адресата, а `receiverMove` ведёт его к точке приёма без ввода. Автопереключение в updateSwitching во время полёта не трогает курсор (receiver + receiveTimer), так что ручная установка держится.
- ✅ **Синхрон удара с анимацией через ЗАМОРОЗКУ мяча ломает ощущение первого касания (23.07.2026).** Чтобы мяч не улетал раньше анимации, мяч замораживался у игрока на время замаха (windup) и потом стартовал — но это читалось как «мяч зависает возле игрока, и он бьёт». В PES 6 удар с лёта — это ПЕРЕНАПРАВЛЕНИЕ приходящего мяча в одно касание: мяч НЕ останавливается. Правильно: замах стартует, пока мяч подлетает (prepareRadius), мяч летит сам, и в момент реального контакта (contactRadius / ближайшая точка / таймаут) он перенаправляется в ворота из текущей позиции. Часть скорости приходящего мяча вкладывается в силу (oneTouchMomentum) — сильный прострел замыкается мощнее. Ни одного кадра заморозки. Тестировать по непрерывности `ball.mesh.position` (максимальная длина «стоп-кадров» = 0).
- ⚠️ **Инъекционные тесты замыкания копят состояние между прогонами (23.07.2026).** Мяч, забитый предыдущим тестом, уводит матч в goalpause / вратарь ловит и держит мяч у себя — следующий тест меряет «мяч в 9 м, скорость 0.2, висит на y≈1.3» (это вратарь с мячом в руках, а не твой). Перед каждым замером замыкания — перезагрузка страницы, иначе цифры врут. Признак загрязнения: dist до игрока ~9-10 м и стабильная малая скорость мяча.
- ✅ **Приём «съедал» волей, пока держишь кнопку удара (24.07.2026).** Событие удара (D) выходит только на ОТПУСКАНИИ (ChargeAction — заряд на удержании, edge на релизе), поэтому пока игрок держит D для замаха волея, `wantShot` ещё false — и авто-приём верхового мяча срабатывал первым, гася мяч «грудью» до волея. Лечение: приём не срабатывает, пока `input.strikeCommitted` (любая боевая кнопка нажата/ждёт); плюс замыкание волея триггерится и по УДЕРЖАНИЮ D (заряд копится, пока мяч летит), а событие отпускания глотается флагом (`_ignoreShotEdge`), чтобы не вылетел второй удар.
- ✅ **Тычок (toe) душил первое касание низкого прострела (24.07.2026).** `strikeStyle` возвращал 'toe' на любой короткий тап РАНЬШЕ проверки скорости мяча — низкий прострел, севший под 0.6 м к контакту, выходил слабым тычком. Правильно: проверку «приходящий мяч на скорости» (rel ≥ instep.minBallRel) делать ДО тычка — первое касание прострела бьётся подъёмом (driven, мощно), даже тапом.

## Вратарь: контакт считать в момент НАИБОЛЬШЕГО СБЛИЖЕНИЯ (26.07.2026)

**Проблема.** Переработанный вратарь пропускал даже удары в живот: мяч проходил сквозь него.
**Причина.** Контакт срабатывал на ВХОДЕ мяча в радиус рук (0.95 м). Мяч влетал в зону, будучи ещё в 0.8 м от корпуса, качество контакта выходило «кончики пальцев», и мяч летел дальше своей дорогой.
**Решение.** Считать точку наибольшего сближения двух тел по относительной скорости (`tca = −(r·v)/|v|²`). Пока `tca > dt` — мяч ещё сближается, играть рано. Тот же приём годится любому «касанию телом».

## Math.max вместо присваивания в скорости броска (26.07.2026)

**Проблема.** Вратарь в броске накрывал руками весь створ и вытаскивал идеальные девятки с 30 м.
**Причина.** `if (this.diveT > 0) maxSpeed = Math.max(maxSpeed, this.diveSpeed)` — обычная скорость бега (6.4) была БОЛЬШЕ вратарской (3.4), и максимум всегда выбирал бег.
**Решение.** В броске скорость ЗАДАЁТСЯ: `maxSpeed = this.diveSpeed || …`. Урок общий: «взять максимум» — не то же самое, что «переопределить».

## Отбой вратаря обязан целиться ЗА ШТАНГУ (26.07.2026)

**Проблема.** «Широкий» отбой центрального удара регулярно заканчивался в собственных воротах.
**Причина.** Направление задавалось фиксированным вектором (доля назад + доля вбок). С позиции в метре перед линией такой вектор выводит мяч ровно на штангу.
**Решение.** Считать направление НА ТОЧКУ: линия ворот, `|z| = полуширина + 1.8…3.2 м`. Отбой всегда уходит за лицевую — угловой.

## Стенд автосимуляции: сбрасывать ВСЁ состояние матча (26.07.2026)

**Проблема.** Замеры вратаря показывали 100 % голов, хотя код был верен.
**Причины (три подряд, каждая маскировала следующую):**
1. Баллистика в стенде считала НАВЕСНОЕ решение вместо настильного — мяч перелетал вратаря. Для драйва нужен низкий корень: `tanθ = (v² − √(v⁴ − g(gd² + 2hv²)))/(gd)`.
2. Не сбрасывался `match.restart` — вратарь вечно «исполнял удар от ворот» и не играл.
3. Не сбрасывались игровые часы — матч уходил в `fulltime`, и AI получал ПОДСТАВНОЙ мяч (`_centerBall`), у которого нет ни скорости, ни `seq`.
**Решение.** В стенде сбрасывать `state`, `restart`, `stateTimer`, `clock`, `score`, `introCam` и позы игроков. И: для AI vs AI обязательно `humanTeam = null`, иначе стандарты «человеческой» команды ждут ввода, и матч 87 % времени стоит в `restart`.

## Клип уже содержит падение — не дублируй его поворотом группы (26.07.2026)

**Проблема (фидбек Олега).** Вратарь при броске «на секунду проваливается под газон и исчезает».
**Причина.** `_updateAnim` крутил всю группу игрока на `rotation.x` до `dive.tiltMax` = 1.15 рад (66°) — а клип `gk_dive` САМ кладёт вратаря на газон и сам поднимает (промер по риггу `player.glb`: бёдра 0.95 → 1.06 на толчке → 0.18 в фазе лёжа → 0.92 в конце). Два поворота складывались. Origin модели в ногах, поэтому `y′ = y·cosθ − z·sinθ`: при θ = 1.15 (cos 0.41, sin 0.91) любая точка впереди корпуса улетает вниз почти на всю свою длину. Замер: голова на −1.44 м, таз ниже нуля 0.63 с подряд, весь провал ~1.1 с.
**Решение.** Амплитуда наклона — СВОЙСТВО броска, а не общая константа: `player.diveTilt`. У полевой «ласточки» (клипы `kick`/`header` — стоячие) остаётся `dive.tiltMax`, у вратаря `CONFIG.ai.keeper.diveTilt = 0`. Тот же баг был у сноса: `startFall` играет лежачий клип `fallen` и наклон ему тоже не нужен. И клип `getup` подкладывать только тому, кого мы сами положили наклоном (`downTiltAmp > 0`).
**Урок общий.** Перед тем как «дорисовывать» силуэт кодом, промерить клип форвард-кинематикой прямо из GLB: разобрать JSON-чанк и BIN-чанк, пройти иерархию нод и посмотреть мировую высоту `mixamorig:Hips` по кадрам. Половина «поломанных анимаций» — это ручная поправка поверх того, что в клипе уже есть.

## Опекун ЗА спиной адресата считался перехватчиком паса (26.07.2026)

**Проблема.** AI отдавал 180 пасов за матч, но НИ ОДНОГО в штрафную: мяч катался поперёк, 3 удара за матч, счёт 0:0.
**Причина.** В `Team.passComplete` окно проверки было `a > D + 1.5 → пропустить`, а координата вдоль линии зажималась `Math.min(D, a)`. Персональный опекун стоит goal-side, то есть в ~1.2 м ЗА адресатом: он попадал в окно, получал `a = D` и «время добегания ноль» — модель выдавала 69 % перехвата на мяч, который придёт к адресату РАНЬШЕ, чем к нему. Проходимость паса в штрафную падала с 0.95 до 0.30, `P^safety` добивала остальное, и вариант отсекался порогом ещё до софтмакса.
**Решение.** Последний метр перед целью принадлежит адресату: `if (a > D − passModel.receiverOwn) continue`. Урок: в моделях перехвата всегда явно исключать зону приёма — иначе собственный опекун адресата становится аргументом против паса ему же.

## Триггер по радиусу от центра ворот съедает ширина (26.07.2026)

**Проблема.** В момент навеса с фланга штрафная всегда пустая — подавать некому.
**Причина.** `updateBoxRuns` включался при `hypot(goalX − ballX, ballZ) < 27`. Классическая позиция для подачи (17 м от лицевой, 25 м от оси) даёт гипотенузу 30.5 — врывания выключены. При |z| = 23 нужно было подойти к лицевой ближе 14 м.
**Решение.** Триггеры «команда в финальной трети» считать по ГЛУБИНЕ (`side * ball.x > длина/2 − X`), а не радиусом. Радиус уместен только там, где реально важна дистанция до ворот (удар).

## Вратарь играл мяч руками ДО того, как среагировал (26.07.2026)

**Проблема (фидбек Олега).** «Тянет мощные удары впритык, но случайные отскоки чуть ли не с центра поля пропускает».
**Причины (три независимых, все — геометрия, а не рандом):**
1. Проверка контакта не смотрела на `gk.reactLeft` вообще: время реакции гасило только РЕШЕНИЕ о броске, а руки работали с первого кадра. Цилиндр 1.9 × 2.45 м = 26 % площади ворот (реальный силуэт вратаря — 6–8 %), причём потолок рук равнялся высоте ворот, то есть верхних углов физически не существовало.
2. Порог «это удар» стоял на 4 м/с — четверть мячей, реально влетающих в ворота, вратарь вообще не читал как удар и просто стоял на дуге.
3. Вся геометрия решения бралась НА ЛИНИИ ВОРОТ, хотя тело вратаря в 5–9 м впереди: расхождение по z до 2.3 м, по времени 0.21 с (почти всё время реакции), по высоте до 2 м.
**Решение.** Пока идёт реакция — играет только корпус (`bodyReach`); выше метра боковая досягаемость тает (`reachHighCost`), стоя выше `standMaxY` мяч не берётся вовсе. Порог «удара» — 1.2 м/с. Решение принимается по СВОЕЙ плоскости: `predictGoalPlane(ball, keeper.x, …)`, линия ворот остаётся только для ответа «летит ли в створ». Плюс шаг под удар — бюджет ПО ВРЕМЕНИ (`setSpeed · (t − react)`), а не фиксированные полметра на любой полёт.

## Софтмакс нельзя сравнивать с порогом (26.07.2026)

**Проблема.** «Приход в ноги» (партнёр показывается накоротке под прессингом) включался по броску монеты.
**Причина.** `tryComingShort` спрашивал `choosePass(owner)` и сравнивал `score` с порогом «есть ли надёжная опция». Но `choosePass` возвращает вариант, ВЫТЯНУТЫЙ СОФТМАКСОМ, а не лучший: при вариантах 1.2 и 0.7 функция в трети случаев отдавала 0.7. Плюс это был второй полный проход по одиннадцати партнёрам каждый такт тренера.
**Решение.** Максимум запоминается отдельно (`team._passBest`) до отсева и софтмакса; все, кому нужно «а есть ли вообще хороший пас», читают его. Софтмакс — это выбор ДЕЙСТВИЯ, а не оценка ситуации.
